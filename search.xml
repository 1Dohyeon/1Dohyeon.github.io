<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Database, AWS 란?</title>
    <url>/2024/02/05/db/databaseANDaws/</url>
    <content><![CDATA[<p>NestJS 프레임워크로 웹 개발을 공부하면서 데이터베이스에 대해서 공부하기 시작했다. 이때 MongoDB, MySQL, AWS 등 갑자기 많은 용어들이 나오고, 이 프로그램에 대한 자세한 설명없이 따라만 하게 되는 수업 영상들이 많은 것 같아서, 이 것들에 대해 자세히 공부할 필요성을 느꼈다.</p>
<span id="more"></span>

<hr>
<h3 id="Database란"><a href="#Database란" class="headerlink" title="Database란?"></a>Database란?</h3><blockquote>
<p>데이터베이스는 구조화된 정보 또는 데이터의 조직화된 모음으로서 일반적으로 컴퓨터 시스템에 전자적으로 저장됩니다 데이터베이스는 일반적으로 <a href="https://www.oracle.com/kr/database/what-is-database/#WhatIsDBMS">데이터베이스 관리 시스템(DBMS)</a>에 의해 제어됩니다. 연결된 애플리케이션과 함께 데이터와 DBMS를 하나로 묶어 데이터베이스 시스템이라고 하며 단축하여 데이터베이스라고도 합니다.</p>
<p>오늘날 운영되고 있는 가장 일반적인 유형의 데이터베이스에서 데이터는 일반적으로 처리 및 데이터 쿼리를 효율적으로 수행하기 위해 일련의 테이블에서 행과 열로 모델링됩니다. 그러면 데이터에 쉽게 액세스하고 관리, 수정, 업데이트, 제어 및 구성할 수 있습니다. 대부분의 데이터베이스는 데이터 작성 및 쿼리에 SQL(Structured Query Language)을 사용합니다.</p>
<ul>
<li><a href="https://www.oracle.com/kr/database/what-is-database/">오라클</a> -</li>
</ul>
</blockquote>
<p>위 글은 오라클에 작성 되어있는 데이터베이스의 정의이다. 어렵게 설명되어 있어서, 여러 블로그나 유튜브를 통해서 공부하였다. 따라서 정확한 정보라고는 보장하기 힘들 것 같다.</p>
<p>우선 내가 이해한 바로 데이터베이스란 말 그대로 <strong>데이터 기지</strong>, 즉 여러 <strong>데이터들의 모임</strong>이다. 이런 데이터베이스를 관리하는 시스템을 바로 <strong>DBMS</strong> 라고 하는데 의미 그대로 DataBase Management System 의 약자이다.</p>
<hr>
<h3 id="DBMS"><a href="#DBMS" class="headerlink" title="DBMS"></a>DBMS</h3><p>DBMS는 데이터베이스에 접근할 수 있도록 해주는 소프트웨어이다. 대표적으로 MySQL이라는 소프트웨어를 많이 들어봤을 것이다. <del>나는 그냥 이게 데이터베이스 그 자체인 줄 알았다. 데이터 관리도 하고 저장까지 하는 일체형(?) 시스템인줄 알았다.</del></p>
<p>이런 DBMS를 사용하는 이유는 사용자가 직접적으로 데이터베이스에 접근할 수 없기 때문이다. 사용자는 무조건 DBMS를 통해서 DB에 접근할 수있다. 이런 DBMS는 여러 종류가 존재한다 :</p>
<ul>
<li>계층형 데이터베이스 ( Hierarchical DataBase )</li>
<li>네트워크형 데이터베이스 ( Network DataBase )</li>
<li>관계형 데이터베이스 ( Relational DataBase )</li>
<li>객체지향형 데이터베이스 ( Object-Oriented DataBase )</li>
<li>NoSQL</li>
<li>NewSQL</li>
</ul>
<p>이런 것들이 있다를 다루는 글이기 때문에 위 종류에 대해 모두 자세히 설명하지는 않고, 이 글에서는 특정 DBMS에 대해서만 다룬다. 우선 위 종류에 SQL 이라는 단어가 언급된다. SQL은 무엇일까?</p>
<hr>
<h3 id="SQL"><a href="#SQL" class="headerlink" title="SQL"></a>SQL</h3><p>SQL이란 여러 DBMS에서 사용되는 용어이다. SQL은 특정 회사에서 만드는 것이 아니라 국제표준화기구에서 SQL에 대한 표준을 정해서 발표하고 있다. 이를 표준 SQL이라고 하며, DBMS를 만드는 회사에서는 되도록 표준 SQL을 준수하되, 각 제품의 특성을 반영한 SQL을 사용한다.</p>
<p>SQL을 사용하는 DBMS에서 MySQL 위 DBMS의 종류 중 관계형 데이터 베이스 (RDBMS)에 속한다. RDBMS 는 어떤식으로 동작하는 것일까?</p>
<hr>
<h3 id="RDBMS-MySQL"><a href="#RDBMS-MySQL" class="headerlink" title="RDBMS (MySQL)"></a>RDBMS (MySQL)</h3><p>관계형 데이터 베이스 관리 시스템 (RDBMS)는 엑셀을 생각하면 이해하기 편하다. 엑셀처럼 테이블 구조로 열과 행을 통해서 데이터를 관리한다. 열(row)에는 하나의 속성 정보가 저장되고, 행(column)에는 열의 속성의 맞는 형태의 데이터가 저장된다.</p>
<p>한 테이블의 여러 열들의 속성 중에 특정 속성인 Primary Key가 하나 존재한다. 그 테이블의 고유 속성이라고 생각하면 된다. RDBMS 에서는 이런 한 테이블의 Primary key를 Foreign Key로 하는 다른 테이블과 연결하여 사용된다. 즉 테이블의 연결을 통해 여러 데이터를 관리하는 것이 RDBMS 이다.</p>
<p>관리할 때 SQL 문법을 사용하며 장점은 다음과 같다 :</p>
<ul>
<li>데이터 무결성 보장</li>
<li>데이터를 중복 없이 한 번만 저장 가능하다. (id, email 등은 고유값이 유지되는 것이 중요)</li>
<li>스키마가 명확히 정의되어 있다.</li>
</ul>
<p>여기서 데이터 무결성은 <strong>데이터의 정확성, 일관성, 유효성이 유지되는 것</strong>을 의미한다. 정확성이란 중복이나 누락이 없는 상태를 뜻하고, 일관성은 원인과 결과의 의미가 연속적으로 보장되어 변하지 않는 상태를 뜻한다.</p>
<p>RDBMS의 단점으로는 테이블의 구조와 데이터 타입을 사전에 정의해야 하기 때문에 알맞은 형태의 데이터만 삽입할 수 있다. 이게 장점이 되어 데이터를 정확히 입력했다면 수월하게 데이터 삽입이 가능하지만, 단점으로는 유연성에 작용된다. 스키마를 사전에 계획하므로 추후 수정이 어렵고, 수직적인 확장만 가능하다는 특징이 있다.</p>
<hr>
<h3 id="NoSQL-Mongodb"><a href="#NoSQL-Mongodb" class="headerlink" title="NoSQL(Mongodb)"></a>NoSQL(Mongodb)</h3><p>NoSQL은 비관계형 데이터베이스로 SQL, 즉, 관계형 데이터베이스를 제외한 나머지 유형을 말한다. NoSQL 데이터베이스는 테이블 형식이 아니며, 관계형 테이블과는 다른 방식으로 데이터를 저장한다.</p>
<p>주요 유형으로는 문서, 키 값, 와이드 컬럼, 그래프 등이 있다. NoSQL이라고 해서 꼭 스키마가 없는 것은 아닙니다. 유연한 스키마를 제공하며, 대량의 데이터와 높은 사용자 부하에서도 손쉽게 확장할 수 있다는 점이 큰 장점이다.</p>
<p>Mongodb는 데이터를 JSON 타입의 Document 방식으로 다루는 NoSQL이다. Mongodb를 공부하다가 mongoose 라는 단어도 많이 들어봤을 것이다. 이건 어렵게 생각할 필요없이 node.js와 Mongodb를 매핑하기 위한 ODM(Object Data Mapping) 라이브러리이다.</p>
<p>JSON 타입 Document란 다음 예시와 같이 생긴 데이터 스타일이다 :</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;2JID92JIA2FOSPF&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Dohyeon&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;age&quot;</span><span class="punctuation">:</span> <span class="number">22</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>지금까지 데이터베이스의 종류들, 그리고 종류들 중에 관계형 데이터베이스와 NoSQL에 대해서 다루었다. 그렇다면 이 데이터들을 과연 어디에 저장할까? 개발자의 컴퓨터 로컬에 직접 저장하기에는 많은 양의 데이터를 수용하기 힘들 것이다. 이런 어려움을 덜어내기 위해서 AWS를 사용한다.</p>
<hr>
<h3 id="AWS"><a href="#AWS" class="headerlink" title="AWS"></a>AWS</h3><p>AWS는 Amazon Web Services의 약자이며 아마존 기업이 제공하는 클라우드 컴퓨팅 서비스이다. AWS는 다양한 서비스와 기능을 제공한다. 대체로 컴퓨팅, 스토리지, 데이터베이스, 네트워킹, 인공지능, 머신 러닝 등 다양한 분야의 서비스를 제공한다.</p>
<p>그렇기에 이를 통해서 DBMS 에서 다룬 데이터를 따로 저장할 수 있다. 위에서 언급한 AWS의 기능들은 <a href="">여기(깔끔하게 정리해준 컴모아님 블로그)</a>에서 확인 할 수 있다. 이 글에서는AWS의 개념인 클라우드 컴퓨팅에 대해서만 다루겠다.</p>
<hr>
<h3 id="클라우드-컴퓨팅"><a href="#클라우드-컴퓨팅" class="headerlink" title="클라우드 컴퓨팅"></a>클라우드 컴퓨팅</h3><p>클라우드 컴퓨팅이란 데이터들만 저장하는 것이 아니라 인터넷 상의 서버를 통해서 IT 서비스를 한번에 사용할 수 있는 컴퓨팅 환경이다. 쉽게 말해서 가상의 컴퓨터를 이용하는 것이다.</p>
<p>쓰는만큼만 값을 지불하기에 매우 유용한 확장성을 가진다는 특징이 있다. 그렇기에 비용면에서도 효율적이고, 무엇보다 필요한 인프라 용량을 추정할 필요가 없게된다.</p>
<p>이러한 서비스를 제공하는 것이 바로 아마존의 자회사인 AWS(Amazon Web Services) 이다. 이 글에서는 대충 개념만 다루었고, 나중에 따로 자세하게 공부해볼 계획이다.</p>
<hr>
<h3 id="마무리"><a href="#마무리" class="headerlink" title="마무리"></a>마무리</h3><p>블로그에 작성된 글은 공식문서를 포함한 여러 웹 사이트에서 공부한 것을 저만의 방식으로 이해하여 쓴 글입니다. 따라서 틀린 정보가 공유될 수도 있으니 <strong>꼭 공식문서를 통해서 공부하시길 바랍니다!</strong></p>
<hr>
<h3 id="참고-URL"><a href="#참고-URL" class="headerlink" title="참고 URL"></a>참고 URL</h3><p><a href="https://cocoon1787.tistory.com/778">참고 사이트1</a><br><a href="https://www.whatap.io/ko/blog/173/">참고 사이트2</a></p>
]]></content>
      <categories>
        <category>database</category>
      </categories>
      <tags>
        <tag>database</tag>
        <tag>aws</tag>
        <tag>sql</tag>
        <tag>dbms</tag>
        <tag>rdbms</tag>
        <tag>nosql</tag>
      </tags>
  </entry>
  <entry>
    <title>Leetcode 2677. Chunk Array, slice 함수</title>
    <url>/2024/02/06/js/Leetcode%202677.Chunk%20Array-slice/</url>
    <content><![CDATA[<p>맨날 알고리즘 문제를 파이썬 또는 자바로만 풀다가 처음 leetcode에서 자바스크립트를 통해서 풀게 되었다. <a href="https://leetcode.com/problems/chunk-array/?envType=study-plan-v2&envId=30-days-of-javascript">2677. Chunk Array, slice 함수</a> 문제는 배열을 원하는 크기 만큼 분할하여 배열에 저장하는 문제인데, 처음에는 <code>slice()</code> 라는 함수의 존재 여부도 모르고 문제를 풀었다. 다른 사람들의 풀이를 보다가 <code>slice()</code> 라는 메서드의 존재 여부를 알게되었다. 따라서 이번 글에서는 이 문제의 풀이와 <code>slice()</code> 메서드의 작동방식을 비슷하게 구현해볼 것이다.</p>
<span id="more"></span>

<h3 id="문제"><a href="#문제" class="headerlink" title="문제"></a>문제</h3><p>문제는 <a href="https://leetcode.com/problems/chunk-array/?envType=study-plan-v2&envId=30-days-of-javascript">여기(Leetcode 2677)</a>에서 확인할 수 있다. 이 문제는 앞서 언급했듯이 배열을 원하는 크기 만큼 분할하여 배열에 저장하는 문제이다.</p>
<p>예를 들어 설명하자면 배열과 분할 할 때 원하는 크기를 다음과 같이 입력 받았다고 가정하자.(<code>arr = [1,9,6,3,2], size = 3</code>)</p>
<p>이때 output으로는 <code>[[1,9,6],[3,2]]</code> 가 반환이 된다. 이 예시처럼 size 대로 분할하고 모자르면 모자른 대로 그룹을 만들면 된다. 만약 <code>size&gt;=arr.length</code> 라면 <code>arr</code>을 그대로 담은 배열(즉, 2차원 배열)을 반환하면 된다.</p>
<h3 id="풀이-코드와-문제-접근"><a href="#풀이-코드와-문제-접근" class="headerlink" title="풀이 코드와 문제 접근"></a>풀이 코드와 문제 접근</h3><p>풀이 코드부터 바로 보자면 다음과 같다 :</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> &#123;<span class="type">Array</span>&#125; <span class="variable">arr</span></span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> &#123;<span class="type">number</span>&#125; <span class="variable">size</span></span></span><br><span class="line"><span class="comment"> * <span class="doctag">@return</span> &#123;<span class="type">Array</span>&#125;</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">var</span> chunk = <span class="keyword">function</span> (<span class="params">arr, size</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> result = [];</span><br><span class="line"></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; arr.<span class="property">length</span>; i += size) &#123;</span><br><span class="line">    result.<span class="title function_">push</span>(arr.<span class="title function_">slice</span>(i, i + size));</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> result;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>문제의 핵심 요소는 반복문을 돌 때 <code>i</code>를 1씩 증가하는 것이 아니라 <code>size</code> 만큼 증가시켜서 하나하나 계산하지 않고, <code>i</code> ~ <code>i+size</code> 까지만 배열에 담는 것이다.</p>
<p>이때 나는 <code>slice()</code> 라는 메서드의 개념을 몰라서 아래와 같이 코드를 짰었다 :</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> &#123;<span class="type">Array</span>&#125; <span class="variable">arr</span></span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> &#123;<span class="type">number</span>&#125; <span class="variable">size</span></span></span><br><span class="line"><span class="comment"> * <span class="doctag">@return</span> &#123;<span class="type">Array</span>&#125;</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> chunk = <span class="keyword">function</span> (<span class="params">arr, size</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> result = [];</span><br><span class="line"></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; arr.<span class="property">length</span>; i += size) &#123;</span><br><span class="line">    result.<span class="title function_">push</span>(<span class="title function_">arrSlice</span>(arr, i, i + size));</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> result;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> <span class="title function_">arrSlice</span> = (<span class="params">arr, start, end</span>) =&gt; &#123;</span><br><span class="line">  <span class="keyword">let</span> output = [];</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = start; i &lt; end; i++) &#123;</span><br><span class="line">    <span class="keyword">if</span> (arr[i] != <span class="literal">null</span>) output.<span class="title function_">push</span>(arr[i]);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> output;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>처음에는 <code>if (arr[i] != null)</code> 없이 그냥 <code>push</code> 했지만, <code>null</code>이 배열에 담기는 상황을 보고 조건을 추가하였다. 완벽하게 <code>slice</code> 를 구현한 것은 아니지만, 문제 풀이에 지장이 없을 정도로만 비슷하게 구현하였다.</p>
<hr>
<h3 id="마무리"><a href="#마무리" class="headerlink" title="마무리"></a>마무리</h3><p>블로그에 작성된 글은 공부한 것을 바탐으로 저만의 방식으로 이해하여 쓴 글입니다. 따라서 틀린 정보가 공유될 수도 있으니 <strong>꼭 공식문서를 통해서 공부하시길 바랍니다!</strong></p>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>javascript</tag>
        <tag>algorithm</tag>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>비동기 함수(async function), Promise 란?</title>
    <url>/2024/01/31/js/async/</url>
    <content><![CDATA[<p>자바스크립트를 공부하다 보면 비동기함수, Promise 라는 단어를 많이 들어봤을 것이다. 나는 NestJS를 공부하면서 비동기 함수를 접하게 되었는 되었는데, 이때 “백그라운드에서 실행되는 메서드” 라는 간단한 개념만 인지한채로 공부하였다.</p>
<p>그러다가 DB에서 데이터를 요청하는 repository 파일에 코드를 작성하던 중 <code>async existsByEmail(email: string): Promise&lt;boolean&gt; ~</code> 와 같은 비동기 함수를 많이 접하게 되었다. 이때부터 비동기 함수와 이 함수를 따라다니는 Promise가 무엇인지 제대로 파악할 필요를 느꼈다.</p>
<span id="more"></span>

<hr>
<h3 id="비동기란"><a href="#비동기란" class="headerlink" title="비동기란?"></a>비동기란?</h3><p>비동기에 대해 알기 전에 “동기”의 뜻을 먼저 설명하자면, 동기란 프로그램이 직렬적으로 수행되는 과정이라고 보면 된다. 요청이 오면 요청에 대한 응답을 받은 후에 다음 동작이 이루어진다. 이때 다른 프로그램은 대기하게 되는데 이러한 방식을 동기라고 한다.</p>
<p>비동기는 위와 반대로 병렬로 프로그램이 실행된다고 보면 된다. 요청의 응답 여부와는 상관없이 다른 프로그램이 동작한다. 이렇게 이론적인 설명만 들으면 비동기 방식이 동기적인 패턴에 비해서 시간면으로도 이득 같아 보인다.</p>
<p>하지만 비동기 함수도 이에 따른 단점이 존재한다. 비동기 처리를 위해서 콜백 패턴을 사용하게 된다면, 처리 순서 보장을 위해서 여러개의 콜백 함수가 중첩되는 현상인 콜백 헬(Callback Hell) 이 발생하게 된다.</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;1&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="built_in">setTimeout</span>(<span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;2&quot;</span>);</span><br><span class="line">&#125;, <span class="number">2000</span>);</span><br><span class="line"></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;3&quot;</span>);</span><br></pre></td></tr></table></figure>

<p>위 코드는 단순히 1, 2, 3을 출력하는 코드이다. 하지만 2를 출력하는 부분은 <code>setTimeout</code>이라는 함수를 통해 비동기 처리를 하여 2를 2초 뒤에 출력시킨다. 이런 비동기 함수의 매개변수에 다른 콜백 함수를 중첩해서 사용한다면 콜백 헬에 걸리게 될 것이다.</p>
<hr>
<h3 id="Promise"><a href="#Promise" class="headerlink" title="Promise"></a>Promise</h3><p>Promise는 위에서 이야기한 콜백 헬로 인해 발생한 에러 처리에 대한 문제점을 해결하기 위해 비동기 처리를 위한 또 다른 패턴이다. Promise가 어떻게 비동기 패턴의 콜백 헬 단점을 보완할 수 있는 것일까?</p>
<p>Promise는 비동기 처리 시점을 “표현”할 수 있다. romise는 상태(state) 정보를 가지고 있고, 이 상태를 표현하여 비동기 처리 성공, 실패 여부를 알려준다. Promise 생성자 함수를 통해 인스턴스화 하며, 작업을 수행할 콜백 함수를 인자로 전달받는다. 이때 콜백 함수는 resolve와 reject 함수를 인자로 받는다.</p>
<p>이때 resolve, reject는 비동기 작업의 수행이 성공했느냐 실패했느냐에의 차이이다. 위에서 언급했듯 Promise는 상태(state) 정보를 가지고 있고, 이 상태에 따라 resolve 또는 reject가 실행된다. 상태는 다음과 같다 :</p>
<ul>
<li><code>pending</code>: 비동기 처리가 아직 수행되지 않은 상태</li>
<li><code>fulfilled</code>: 비동기 처리가 수행된 상태 (성공)</li>
<li><code>rejected</code>: 비동기 처리가 수행된 상태 (실패)</li>
<li><code>settled</code>: 비동기 처리가 수행된 상태 (성공 또는 실패)</li>
</ul>
<p>또한 Promise 는 다음과 같이 사용할 수 있다 :</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Promise 객체 생성</span></span><br><span class="line"><span class="keyword">const</span> promise = <span class="keyword">new</span> <span class="title class_">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="comment">/* 성공 */</span>) &#123;</span><br><span class="line">    <span class="title function_">resolve</span>(<span class="string">&#x27;result&#x27;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">else</span> &#123; <span class="comment">// 실패</span></span><br><span class="line">    <span class="title function_">reject</span>(<span class="string">&#x27;failure reason&#x27;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>또한 Promise는 <code>.then</code> 을 이용하여 체이닝(chaining)을 할 수 있는데, 체이닝은 아래 예제처럼 여러개의 Promise를 이을 수 있다.</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">new</span> <span class="title class_">Promise</span>(<span class="keyword">function</span> (<span class="params">resolve, reject</span>) &#123;</span><br><span class="line">  <span class="built_in">setTimeout</span>(<span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">    <span class="title function_">resolve</span>(<span class="number">0</span>);</span><br><span class="line">  &#125;, <span class="number">1000</span>);</span><br><span class="line">&#125;)</span><br><span class="line">  .<span class="title function_">then</span>(<span class="keyword">function</span> (<span class="params">val</span>) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(val); <span class="comment">// output: 1</span></span><br><span class="line">    <span class="keyword">return</span> val + <span class="number">1</span>;</span><br><span class="line">  &#125;)</span><br><span class="line">  .<span class="title function_">then</span>(<span class="keyword">function</span> (<span class="params">val</span>) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(val); <span class="comment">// output: 2</span></span><br><span class="line">  &#125;);</span><br></pre></td></tr></table></figure>

<hr>
<h3 id="async-await"><a href="#async-await" class="headerlink" title="async await"></a>async await</h3><p>async와 await도 콜백 헬을 없애줄 수 있는 비동기 방식이다. 보통 아래처럼 함수 앞에 <code>async</code>를 붙이고 비동기 처리를 할 코드 앞 부분에 <code>await</code>를 사용한다 :</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="title function_">asyncFunc</span> = <span class="keyword">async</span> (<span class="params"></span>) =&gt; &#123;</span><br><span class="line">  <span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;1&quot;</span>);</span><br><span class="line">    <span class="keyword">await</span> <span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;2&quot;</span>), <span class="number">2000</span>);</span><br><span class="line">  &#125; <span class="keyword">catch</span> (err) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(err);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;finish&quot;</span>);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="title function_">asyncFunc</span>();</span><br></pre></td></tr></table></figure>

<p>위 코드는 1이 먼저 찍히고 finish가 찍힌 후 2초 후에 2가 찍히는 비동기 함수임을 알 수 있다.</p>
<hr>
<h3 id="마무리"><a href="#마무리" class="headerlink" title="마무리"></a>마무리</h3><p>블로그에 작성된 글은 공식문서를 포함한 여러 웹 사이트에서 공부한 것을 저만의 방식으로 이해하여 쓴 글입니다. 따라서 틀린 정보가 공유될 수도 있으니 <strong>꼭 공식문서를 통해서 공부하시길 바랍니다!</strong></p>
]]></content>
      <categories>
        <category>javascript</category>
      </categories>
      <tags>
        <tag>javascript</tag>
        <tag>async</tag>
      </tags>
  </entry>
  <entry>
    <title>순환 참조 문제 해결(NestJS)</title>
    <url>/2024/02/18/nestJS/%EC%88%9C%ED%99%98%EC%B0%B8%EC%A1%B0%EB%AC%B8%EC%A0%9C/</url>
    <content><![CDATA[<p>NestJS 프레임워크를 공부하기 위해서 개인 프로젝트 개발에 도전하였다. 가볍게 대중적인 게시판 만들기부터 시작하였다. 이 게시판 개인 프로젝트에서 articles 모듈과, users 모듈 사이에 순환 참조 문제가 발생하여 어떻게 해결할지 고민했던 과거를 공유하고자 글을 작성하게 되었다.</p>
<span id="more"></span>

<hr>
<h3 id="기존-프로젝트-모듈-구조"><a href="#기존-프로젝트-모듈-구조" class="headerlink" title="기존 프로젝트 모듈 구조"></a>기존 프로젝트 모듈 구조</h3><p>개인 프로젝트에서 처음에는 크게 3가지 모듈을 나누어 작업을 하였다. 모듈은 다음과 같은 기능을 한다 :</p>
<ul>
<li><strong>AuthModule</strong> : JWT 인증을 위한 모듈. 회원가입, 로그인을 담당함. 회원가입 시에 <code> @Post(&#39;/register&#39;)</code> 엔드포인트에서 UsersModule에 접근하여 User 객체 생성.</li>
<li><strong>UsersModule</strong> : 직접적으로 User 객체를 생성. Repository 디자인 패턴을 이용함.</li>
<li><strong>ArticlesModule</strong> : 직접적으로 Article 객체를 생성. <code>@Post(&#39;/article&#39;)</code> 엔드포인트를 통해서 ArticlesModule에 접근하여 Article 객체 생성. Repository 디자인 패턴을 이용함.</li>
</ul>
<p>ArticlesModule의 <code>@Post(&#39;/article&#39;)</code>에서 article 객체를 생성한다. article 객체를 생성할 때, 하나의 article 인스턴스는 authorId 정보를 갖게 되는데, 이는 article 객체를 생성한 user의 id 정보이다. 따라서 ArticlesModule는 UsersModule을 import 해야하는 상황이 되었다.</p>
<p>ArticlesModule이 UsersModule을 참조하는 것은 문제가 되지 않지만, 기존에 UsersModule 또한 ArticlesModule을 import 하고 있었다. user 인스턴스는 자신이 만든 aritlce의 <del>아이디들(지금은 삭제)</del> 갯수를 저장하고 있기 때문이다.</p>
<p><img src="https://github.com/1Dohyeon/Study-NestJS/raw/main/01_Docs/imgs/%EB%AA%A8%EB%93%88%EA%B5%AC%EC%A1%B02.png" alt="[모듈구조2.png]"></p>
<p>따라서 순환 참조 문제가 발생하여 문제를 해결하기 위해서 두 모듈 사이의 상호작용을 처리하는 모듈인 <strong>UserArticleInteractionModule</strong> 을 만들기 시작했다.</p>
<hr>
<h3 id="순환-참조-문제-해결"><a href="#순환-참조-문제-해결" class="headerlink" title="순환 참조 문제 해결"></a>순환 참조 문제 해결</h3><p>이 모듈은 다른 두 모듈의 repository에 직접적으로 접근하지 않고, service에 접근하여 aritlce 생성 시에 userId 정보를 넘기고, user 에 PRIVATE or PUBLIC article 의 갯수 정보를 넘긴다.</p>
<p><img src="https://github.com/1Dohyeon/Study-NestJS/raw/main/01_Docs/imgs/%EB%AA%A8%EB%93%88%EA%B5%AC%EC%A1%B01.png" alt="[모듈구조1.png]"></p>
<p>쉽게 말해서 UserArticleInteractionModule에서만 ArticlesModule, UsersModule를 import하고, UserArticleInteractionService에서는 각 ArticlesService, UsersService를 불러와서 순환 참조를 없애었다.(ArticlesModule, UsersModule은 이제 서로를 import하지 않음.)</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">UserArticleInteractionService</span> &#123;</span><br><span class="line">  <span class="title function_">constructor</span>(<span class="params"></span></span><br><span class="line"><span class="params">    <span class="keyword">private</span> <span class="keyword">readonly</span> usersService: UsersService,</span></span><br><span class="line"><span class="params">    <span class="keyword">private</span> <span class="keyword">readonly</span> articlesService: ArticlesService,</span></span><br><span class="line"><span class="params">  </span>) &#123;&#125;</span><br><span class="line"> ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>따라서 기존에 article 객체를 직접적으로 생성하는 것을 ArticleModule에서 했지만, 순환 모듈 없이 userId와 articleId를 공유하기 위해서 아래와 같이 에서 article CRUD를 만들게 되었다.</p>
<p>article CREATE :</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line">  <span class="keyword">async</span> <span class="title function_">createArticleForUser</span>(<span class="params"></span></span><br><span class="line"><span class="params">    userId: <span class="built_in">string</span>,</span></span><br><span class="line"><span class="params">    createArticleDto: CreateArticleDto,</span></span><br><span class="line"><span class="params">  </span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> newArticle = <span class="keyword">await</span> <span class="variable language_">this</span>.<span class="property">articlesService</span>.<span class="title function_">create</span>(createArticleDto);</span><br><span class="line">    <span class="keyword">if</span> (newArticle) &#123;</span><br><span class="line">      <span class="comment">// service</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> newArticle;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>각 모듈은 독립적으로 책임을 분리하는 것이 원칙적으로 좋지만, 어쩔 수 없는 경우는 순환 참조를 피하기 위해서 두 개의 모듈이 상호작용을 하는 또 다른 모듈을 만드는 방법을 사용하는 것도 개인적으로 나쁘지 않은 것 같다.</p>
]]></content>
      <categories>
        <category>nestJS</category>
      </categories>
      <tags>
        <tag>nestJS</tag>
        <tag>circular-reference</tag>
      </tags>
  </entry>
  <entry>
    <title>프론트와 백엔드 연결하기(NestJS, React)</title>
    <url>/2024/02/19/nestJS/nestjs-react-connact/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>nestJS</category>
      </categories>
      <tags>
        <tag>nestJS</tag>
        <tag>react</tag>
      </tags>
  </entry>
</search>
