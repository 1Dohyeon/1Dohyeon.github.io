<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>알고리즘(Algorithms) - Selection 선택 알고리즘 정리 + median of medians</title>
    <url>/2024/01/22/Algorithms2/</url>
    <content><![CDATA[<h3 id="2-1-퀵-정렬-방식을-이용한-선택-알고리즘"><a href="#2-1-퀵-정렬-방식을-이용한-선택-알고리즘" class="headerlink" title="2.1 퀵 정렬 방식을 이용한 선택 알고리즘"></a>2.1 퀵 정렬 방식을 이용한 선택 알고리즘</h3><p><strong>2.1.1 선택 알리즘 정의</strong></p>
<p>선택 알고리즘이란 배열 <code>A[1...n]</code> 에서 i 번째로 크거나 작은 원소를 찾는 알고리즘을 의미한다. 선택 알고리즘의 수행 시간 범위는 NlogN ~ N 으로 정렬 후에 i 번째 원소를 고르는 방법은 NlogN 이 걸린다.</p>
<span id="more"></span>

<p>N(선형) 시간이 걸리는 알고리즘은 N 개의 원소 각각을 한 번씩만 보는 알고리즘으로 <a href="https://github.com/1Dohyeon/Study-DataStructure/blob/master/06_sort_with_java/D1_Sort/Quick.java">퀵 정렬(Quick Sort)</a> 을 기반으로 하는 알고리즘이다.</p>
<p><strong>2.1.2 알고리즘 예시</strong></p>
<p><img src="https://github.com/1Dohyeon/blogIMG/blob/master/nest1-datastructure-algorithms/%ED%80%B5%EC%A0%95%EB%A0%AC%EC%9D%B4%EC%9A%A9%EC%84%A0%ED%83%9D%EC%95%8C%EA%B3%A0%EB%A6%AC%EC%A6%98.png?raw=true" alt="퀵정렬이용선택알고리즘.png"></p>
<p>예를 들어 2 번째로 작은 원소를 찾는다고 가정해보자:</p>
<ul>
<li>퀵 정렬의 기준 원소를 기준으로 왼쪽에는 기준보다 작은 값, 오른쪽은 기준보다 큰 값을 둔다.</li>
<li>그리고 기준 원소를 중간으로 옮기고 기준 원소의 인덱스를 알아낸다.</li>
<li>기준 원소의 인덱스가 4일 때 이제 오른쪽에 모아둔 원소들을 볼 필요 없이, 왼쪽 원소들로만 퀵 정렬을 하고, 이렇게 반복하여 2 번째로 작은 원소를 찾을 수 있다.</li>
</ul>
<h3 id="2-2-중앙값의-중앙값을-이용한-선택-알고리즘"><a href="#2-2-중앙값의-중앙값을-이용한-선택-알고리즘" class="headerlink" title="2.2 중앙값의 중앙값을 이용한 선택 알고리즘"></a>2.2 중앙값의 중앙값을 이용한 선택 알고리즘</h3><p><strong>2.2.1 알고리즘 핵심</strong></p>
<p>예를 들어 A 라는 배열이 있을 때 이 배열을 쪼개서 5개의 원소끼리 그룹화를 한다. 각 그룹의 5개의 원소를 정렬한다. 이때 5개의 원소를 정렬하는 것은 상수의 시간이 걸린다.</p>
<p>각 그룹의 3번째 원소가 그룹의 중앙값이 될 것이다. 이 중앙값들의 중앙값이 배열 <code>A</code> 전체 원소들의 중앙값이 될 것이다. 따라서 이 중앙값을 찾는 과정이 <code>Θ(n)</code> 의 시간복잡도를 가지게 된다.</p>
]]></content>
      <categories>
        <category>Algorithms</category>
      </categories>
      <tags>
        <tag>study</tag>
        <tag>Algorithms</tag>
      </tags>
  </entry>
  <entry>
    <title>알고리즘(Algorithms) - Sort(계수정렬, 기수정렬)</title>
    <url>/2024/01/21/Algorithms1/</url>
    <content><![CDATA[<h3 id="1-1-계수-정렬"><a href="#1-1-계수-정렬" class="headerlink" title="1.1 계수 정렬"></a>1.1 계수 정렬</h3><p><strong>1.1.1 계수 정렬 사용 조건</strong></p>
<p>정렬하고자 하는 원소들의 값이 O(N) 을 넘지 않는 경우에 사용하는 정렬이다. 즉, 11개의 원소를 정렬하고자 할때 각 원소들을 가진 배열의 최댓값은 11 이하여야 한다.</p>
<span id="more"></span>

<p><strong>1.1.2 계수 정렬 정의</strong></p>
<p>계수 정렬(Counting Sort)은 정렬 알고리즘 중 하나로, 정수 또는 정수 형태의 데이터를 정렬하는 데 사용된다. 이름 그대로 <strong>계수(빈도수)를 가지고 정렬을 하는 방식</strong>이다.</p>
<p>계수 정렬은 다음과 같은 특징을 갖는다 :</p>
<ul>
<li>비교 기반 정렬 알고리즘이 아님</li>
<li>입력 데이터의 범위가 제한되어 있을 때 효과적으로 동작함</li>
<li>중복된 값을 가지는 데이터에 대해 잘 동작함</li>
</ul>
<p>아래는 계수 정렬의 기본 동작 과정이다 :</p>
<ol>
<li>입력 배열을 순회하면서 각 데이터의 출현 빈도수를 세어 “카운트 배열”에 저장한다. 이때, 카운트 배열의 인덱스는 입력 데이터의 값이며, 각 인덱스의 값은 해당 값의 출현 빈도수를 나타낸다.</li>
<li>카운트 배열을 이용하여 정렬된 결과를 새로운 배열에 저장한다. 이때, 입력 배열의 순서를 유지하면서 정렬한다. 예를 들어, 입력 배열에서 같은 값이 여러 번 나온 경우, 정렬된 결과에서도 같은 값이 여러 번 나오게 된다.</li>
<li>정렬된 결과를 새로운 배열에 저장한 후, 이를 다시 입력 배열에 복사한다.</li>
</ol>
<p><code>A</code> 라는 배열을 정렬해야할 때, 임시 배열 <code>B, C</code> 를 만든다. <code>B</code>에는 <code>A</code> 배열의 빈도수를 담으면 된다. 예를 들어 <code>A</code> 배열에 0이 1개, 1이 3개, 2가 2개 있다면 <code>B[0] = 1, B[1] = 3, B[2] = 2</code> 가 될 것이다. 빈도수가 0이라면 당연히 0이 채워진다.</p>
<p>위와 같이 B 배열을 채웠다면 인덱스를 통해서 C 배열에 채워 넣으면 된다. C 배열은 <code>C[] = [0, 1, 1, 1, 2, 2]</code> 가 된다. 0, 1의 개수의 합은 4이다. 이 누적 인덱스를 통해서 <code>C[4]</code> 자리에는 2가 채워진다.</p>
<h3 id="1-2-기수-정렬"><a href="#1-2-기수-정렬" class="headerlink" title="1.2 기수 정렬"></a>1.2 기수 정렬</h3><p><strong>1.2.1 기수 정렬 정의</strong></p>
<p>기수 정렬(Radix Sort)은 비교 기반 정렬 알고리즘이 아닌, 정수나 문자열과 같은 특정한 자료형을 정렬하는 데 사용되는 비교 정렬이 아닌 정렬 알고리즘 중 하나이다. 자릿수(또는 문자의 자릿수)를 기반으로 배열을 여러 번 정렬하는 방식으로 동작한다.</p>
<p>기수 정렬의 주요 아이디어는 가장 낮은 자릿수부터 가장 높은 자릿수까지 반복적으로 정렬하는 것이다. 각 자릿수를 기준으로 정렬할 때는 안정적인 정렬 알고리즘(예: 버블 정렬, 삽입 정렬)을 사용한다.</p>
<p>일반적으로 LSD(Lowest Significant Digit) 방식과 MSD(Most Significant Digit) 방식으로 구현된다. LSD 기수 정렬은 가장 낮은 자릿수부터 시작하여 가장 높은 자릿수까지 순차적으로 정렬하는 방식이며, 숫자의 낮은 자릿수부터 정렬하므로 작은 값부터 큰 값 순으로 정렬된다.</p>
<p>MSD 기수 정렬은 가장 높은 자릿수부터 시작하여 가장 낮은 자릿수까지 역순으로 정렬하는 방식이며, 큰 값부터 작은 값 순으로 정렬된다.</p>
<p>이 방법은 효율적인 특수 정렬 방식으로 시간 복잡도는 N이 걸린다. 하지만 “radixSort(A[ ], n, k)” 형태로 원소들이 각각 최대 k 자리수인 배열을 정렬하기에 k가 logN 사이즈 이상이 되어버리면, NlogN 의 시간 복잡도를 가지는 병합, 퀵 정렬보다 효율적이지 않게된다.</p>
<p>그리고 각각의 자릿수를 맞추기 위해 zero-padding(ex: 493 -&gt; 0493)을 해야하는 번거러움이 있다. 문자열일 경우 zero-padding을 문자열 뒤에다가 해주면 된다.</p>
]]></content>
      <categories>
        <category>Algorithms</category>
      </categories>
      <tags>
        <tag>study</tag>
        <tag>Algorithms</tag>
      </tags>
  </entry>
  <entry>
    <title>알고리즘(Algorithms) - 2-3Tree, B트리, 레드블랙트리(red-black tree)</title>
    <url>/2024/01/24/Algorithms3/</url>
    <content><![CDATA[<h3 id="3-1-2-3트리"><a href="#3-1-2-3트리" class="headerlink" title="3.1 2-3트리"></a>3.1 2-3트리</h3><p><strong>3.1.1 정의</strong></p>
<p><img src="https://github.com/1Dohyeon/blogIMG/blob/master/nest1-datastructure-algorithms/2-3%EC%98%88%EC%A0%9C.png?raw=true" alt="[2-3예제.png]"></p>
<span id="more"></span>

<ol>
<li>완전 균형 트리(Completely Balanced Tree)<ul>
<li>완전한 균형을 유지하기 때문에, root에서 leaf에 이르는 모든 경로의 길이가 완전히 동일함</li>
</ul>
</li>
<li>이진트리와는 달리 자식노드를 두개만 가질 수 있는 2노드 형태뿐만 아니라 자식을 3개 가질 수 있는 3노드 형태도 가능함.<ul>
<li>3노드 형태의 key값은 2개임.</li>
<li>왼쪽 자식&#x2F;중간 자식&#x2F;오른쪽 자식이 존재하며, 왼쪽 자식은 작은 값, 중간 자식은 사잇값, 오른쪽 자식은 큰 값을 가진다.</li>
</ul>
</li>
</ol>
<p><strong>3.1.2 Insertion(삽입): split</strong></p>
<p><img src="https://github.com/1Dohyeon/blogIMG/blob/master/nest1-datastructure-algorithms/2-3%EC%82%BD%EC%9E%85.png?raw=true" alt="[2-3삽입.png]"><br>삽입을 통해서 노드 안에 key가 세개가 된다면 중앙값을 위로 올리고 3노드를 2노드로 분리한다.</p>
<p><strong>3.1.3 Deletion: share(형제 노드에 여유가 있는 경우), join(여유있는 형제 노드가 없는 경우)</strong></p>
<p><img src="https://github.com/1Dohyeon/blogIMG/blob/master/nest1-datastructure-algorithms/2-3%EC%82%AD%EC%A0%9C.png?raw=true" alt="[2-3삭제.png]"><br>삭제 후 형제 트리에 공간이 있을 경우 share하여 값을 옮긴다. 이때 트리의 조건에 맞추기 위해서 부모의 원소를 내리고 자식 원소를 올린다.</p>
<p>join은 부모 원소를 자식 노드에 옮겨서 빈자리를 매꾸는 방식이다.</p>
<h3 id="3-2-B트리"><a href="#3-2-B트리" class="headerlink" title="3.2 B트리"></a>3.2 B트리</h3><p><strong>3.2.1 정의</strong></p>
<ul>
<li><p>B-트리는 균형잡힌 다진검색트리로 다음의 성질을 만족한다</p>
<ul>
<li>루트를 제외한 모든 노드는 k&#x2F;2 ~ k 개의 키를 갖는다</li>
<li>모든 리프 노드는 같은 깊이를 가진다</li>
</ul>
</li>
<li><p>B-트리의 노드 구조<br><img src="https://github.com/1Dohyeon/blogIMG/blob/master/nest1-datastructure-algorithms/b%ED%8A%B8%EB%A6%AC%EC%98%88%EC%A0%9C.png?raw=true" alt="[b트리예제.png]"></p>
</li>
<li><p>B-트리의 삽입 삭제시에, 2-3트리와 비슷하게 split과 join을 한다.</p>
</li>
</ul>
<p><strong>3.2.2 Insertion(삽입)</strong></p>
<p><img src="https://github.com/1Dohyeon/blogIMG/blob/master/nest1-datastructure-algorithms/b%ED%8A%B8%EB%A6%AC%EC%82%BD%EC%9E%851.png?raw=true" alt="[b트리삽입1.png]"><br>오버플로우가 발생하면 형제노드를 살펴보고 형제노드가 포화상태가 아니라면 형제 노드에 원래 노드의 최댓값 or 최솟값을 넘겨준다. 그 다음 형제 노드의 최댓값 or 최솟값을 부모 노드 최댓값 or 최솟값과 바꾼다.</p>
<p><img src="https://github.com/1Dohyeon/blogIMG/blob/master/nest1-datastructure-algorithms/b%ED%8A%B8%EB%A6%AC%EC%82%BD%EC%9E%852.png?raw=true" alt="[b트리삽입2.png]"><br>위 그림에서는 39를 삽입하자 오버플로우가 발생하고 형제 노드도 포화 상태임을 알 수 있다. 이때 split을 하고 중앙값을 부모 노드로 올린다.</p>
<p><img src="https://github.com/1Dohyeon/blogIMG/blob/master/nest1-datastructure-algorithms/b%ED%8A%B8%EB%A6%AC%EC%82%BD%EC%9E%853.png?raw=true" alt="[b트리삽입3.png]"><br>위 그림을 보면 32를 삽입하고 오버플로우가 나서 split 후에 부모 노드로 중앙값을 올렸지만, 부모노드도 오버플로우가 난 것을 알 수 있다. 부모 노드의 형제노드가 없으므로 이때 부모노드를 split하면 된다.</p>
<h3 id="3-3-레드블랙트리"><a href="#3-3-레드블랙트리" class="headerlink" title="3.3 레드블랙트리"></a>3.3 레드블랙트리</h3><p><strong>3.3.1 정의</strong></p>
<ul>
<li>Red와 Black의 색상을 이용해서 트리의 균형을 유지</li>
<li>다음과 같은 조건을 만족해야함.<ol>
<li>루트는 블랙이다.</li>
<li>모든 리프는 블랙이다.</li>
<li>노드가 레드이면 그 노드의 자식은 반드시 블랙이다.</li>
<li>루트 노드에서 임의의 리프 노드에 이르는 경로에서 만나는 블랙 노드의 수는 모두 같다.</li>
</ol>
</li>
</ul>
<p><img src="https://github.com/1Dohyeon/blogIMG/blob/master/nest1-datastructure-algorithms/%EB%A0%88%EB%93%9C%EB%B8%94%EB%9E%99%ED%8A%B8%EB%A6%AC%EC%98%88%EC%A0%9C.png?raw=true" alt="[레드블랙트리예제.png]"></p>
<p>바로 위와 같이 생긴 트리가 레드블랙트리의 조건을 만족한다. 여기서 리프 노드가 빨간색인데 어떻게 만족하는지에 대한 의문을 가질 수도 있다.</p>
<p>자식이 없는 모든 노드는 NIL(블랙) 노드를 자식으로 가지기 때문에 사실상, 2번 조건은 무시해도 된다.</p>
<p><strong>3.3.2 삽입</strong></p>
<p>삽입의 경우 여러가지 경우가 있다. 우선, 삽입할 노드 x(레드)의 부모 노드 p가 블랙이라면 아무런 문제가 없다.</p>
<p>두번째 경우로 삽입할 노드 x(레드)의 부모 노드 p가 레드라면 또 여러가지를 고려해야한다. 첫번째 경우는 아래 그림과 같다.<br><img src="https://github.com/1Dohyeon/blogIMG/blob/master/nest1-datastructure-algorithms/%EB%A0%88%EB%93%9C%EB%B8%94%EB%9E%99%ED%8A%B8%EB%A6%AC%EC%82%BD%EC%9E%851.png?raw=true" alt="[레드블랙트리삽입1.png]"></p>
<p>이때는 바로 저 흰색 부분의 색깔에 의해서 경우가 나누어진다. 흰색 부분을 s라고 할때, 흰색 노드가 레드라면 LLr 조건이다.</p>
<p>LLr 조건을 설명하자면, p의 부모노드를 pp라고 할 때, pp로부터 왼쪽 노드, 그리고 그 왼쪽 노드로 삽입되었을 경우 LL 이고, pp의 오른쪽 자식이 레드일경우 r, 블랙일 경우 b이다.</p>
<p>따라서 x가 p의 오른쪽 자식으로 들어왔다면 LRr이 될 수 있음을 유추할 수 있다. 그리고 p가 pp의 오른쪽 자식이고 또 p의 오른쪽 자식으로 노드 x를 삽입했을 경우 RR임을 알 수 있다.</p>
<p>이렇게 앞에 두 대문자가 방향을 표시하고 뒤에 소문자가 삽입 노드의 부모 노드의 형제 색을 표시한다. 이 부모의 형제 노드에 의해서 CASE가 나누어지고, LL이든 RR이든 반대로 회전만 하면 되므로 설명은 한 번만 하겠다.</p>
<p>s가 레드일 경우 그냥 색만 바꾸면 된다. p와 s를 블랙, pp를 레드로 바꾸면 된다.<br><img src="https://github.com/1Dohyeon/blogIMG/blob/master/nest1-datastructure-algorithms/%EB%A0%88%EB%93%9C%EB%B8%94%EB%9E%99%ED%8A%B8%EB%A6%AC%EC%82%BD%EC%9E%852.png?raw=true" alt="[레드블랙트리삽입2.png]"></p>
<p>s가 블랙일 경우는 <a href="https://github.com/1Dohyeon/Study-DataStructure/tree/master/03_Tree_with_java">AVL 트리(3.3)</a>처럼 회전을 시킨 후 색을 바꾸면 된다.<br><img src="https://github.com/1Dohyeon/blogIMG/blob/master/nest1-datastructure-algorithms/%EB%A0%88%EB%93%9C%EB%B8%94%EB%9E%99%ED%8A%B8%EB%A6%AC%EC%82%BD%EC%9E%853.png?raw=true" alt="[레드블랙트리삽입3.png]"><br>색을 바꾸는데에는 상수시간, 색을 바꿔야할 곳까지 탐색하는 것은 트리의 높이에 비례함을 알 수 있다. 따라서 위 알고리즘의 시간복잡도는 log2N 임을 알 수 있다.</p>
]]></content>
      <categories>
        <category>Algorithms</category>
      </categories>
      <tags>
        <tag>study</tag>
        <tag>Algorithms</tag>
      </tags>
  </entry>
  <entry>
    <title>자료구조(data structure) - dynamic array, linked list</title>
    <url>/2024/01/05/dataStructure1/</url>
    <content><![CDATA[<h3 id="1-1-Dynamic-Array"><a href="#1-1-Dynamic-Array" class="headerlink" title="1.1 Dynamic Array"></a>1.1 Dynamic Array</h3><p><strong>1.1.1 개념</strong></p>
<p>Dynamic Array(동적 배열) 은 프로그래밍이 실행되는 동안 할당된 메모리 공간을 확장 또는 축소하는 배열이다. 기본 배열은 공간을 전부 차지하면 직접 공간을 늘려줘야했다.</p>
<span id="more"></span>

<p>하지만 동적 배열은 <code>resize()</code> 메서드에 의해서 동적으로 크기를 조절한다. 배열 크기와 원소의 갯수가 같아진다면 2배 증가시키고, 원소의 갯수가 배열 크기의 1&#x2F;4 된다면 배열 크기를 1&#x2F;2로 줄여준다.</p>
<p>동적 배열은 배열을 이용한 자료구조이기 때문에 원소에 접근할 때는 O(1)의 시간복잡도를 가진다. 하지만 배열에 원소를 삽입할 때나 삭제할 때, 중간에 있는 원소를 건들이게 될 경우 원소들의 위치를 밀거나 당겨야함으로 최악의 경우 O(N)의 시간복잡도를 가지게 된다.</p>
<p>또한 크기를 동적으로 바꿀 때는 O(N)의 시간이 걸리게 된다.(1.1.2 참고)</p>
<p><strong>1.1.2 resize()</strong></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">resize</span>(<span class="params">self, newSize</span>):</span><br><span class="line">        tempArr = [<span class="literal">None</span> <span class="keyword">for</span> _ <span class="keyword">in</span> <span class="built_in">range</span>(newSize)]</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(self.size):</span><br><span class="line">            tempArr[i] = self.arr[i]</span><br><span class="line"></span><br><span class="line">        self.arr = tempArr</span><br></pre></td></tr></table></figure>

<p>위 코드는 파이썬으로 작성한 예시 코드이다. 우선 <code>newSize</code> 라는 새로운 배열의 크기를 매개변수로 받는다.</p>
<p>위 변수의 크기를 갖는 임시 배열 <code>tempArr</code> 을 생성하여 기존 배열의 원소들을 복사해준다. 이제 기존 배열은 임시 배열의 값으로 업데이트 하면 된다.</p>
<p>원소를 삭제할 때 원소의 크기가 배열의 1&#x2F;4 이하인지 확인하고, 이하가 맞다면 <code>newSize</code> 는 현재 <code>size</code> 의 절반 값을 갖게 된다.</p>
<p>또한 원소를 삽입하는 과정에서 <code>size</code> 가 배열의 크기와 같아진다면 <code>newSize</code> 의 값은 <code>size</code>의 두배가 된다.</p>
<p>코드 전체 : <a href="https://github.com/1Dohyeon/Study-DataStructure/blob/master/01_Lists_with_py/01_DynamicArray.py">Code</a></p>
<hr>
<h3 id="1-2-Singly-Linked-List"><a href="#1-2-Singly-Linked-List" class="headerlink" title="1.2 (Singly)Linked List"></a>1.2 (Singly)Linked List</h3><p><strong>1.2.1 개념</strong></p>
<p>(Singly)Linked List(단일 연결 리스트)는 값을 배열에 담지 않는다. 동적 메모리 할당을 받아서 노드를 저장하고, 노드는 레퍼런스를 이용하여 다음 노드를 가리키도록 만들어서 하나의 줄로 연결된 것처럼 보이게 하는 리스트이다.</p>
<p>즉, (아래 그림처럼)노드 객체에 데이터를 담고, 다른 데이터를 가진 노드 객체와 연결하는 방식이다.</p>
<p><img src="https://github.com/1Dohyeon/blogIMG/blob/master/imgs/linkedList%EB%85%B8%EB%93%9C%EC%84%A4%EB%AA%85%EC%9D%B4%EB%AF%B8%EC%A7%80.png?raw=true"></p>
<p><strong>1.2.2 class Node</strong></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Node</span>:</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, newItem, nextNode</span>):</span><br><span class="line">        self.item = newItem</span><br><span class="line">        self.<span class="built_in">next</span> = nextNode</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">getItem</span>(<span class="params">self</span>): <span class="keyword">return</span> self.item</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">getNext</span>(<span class="params">self</span>): <span class="keyword">return</span> self.<span class="built_in">next</span></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">setItem</span>(<span class="params">self, newItem</span>): self.item = newItem</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">setNext</span>(<span class="params">self, nextNode</span>): self.<span class="built_in">next</span> = nextNode</span><br></pre></td></tr></table></figure>

<p>원소를 담을 필드 <code>item</code> 과 다음 노드를 가리키는 <code>next</code> 필드를 가진 객체 클래스이다.</p>
<p><strong>1.2.3 Search</strong></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">search</span>(<span class="params">self, target</span>):</span><br><span class="line">    <span class="string">&#x27;&#x27;&#x27;target이라는 원소를 가진 index를 return함.&#x27;&#x27;&#x27;</span></span><br><span class="line">    p = self.head</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> k <span class="keyword">in</span> <span class="built_in">range</span>(self.size):</span><br><span class="line">        <span class="keyword">if</span> target==p.getItem(): <span class="keyword">return</span> k</span><br><span class="line">        p = p.getNext()</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> -<span class="number">1</span></span><br></pre></td></tr></table></figure>

<p>위 코드를 보면 알 수 있듯이 원소에 접근할 때, 첫번째 노드부터 <code>next</code>를 따라서 리스트를 탐색하는 것을 알 수 있다. 그렇기에 동적 배열과는 달리 시간복잡도가 O(N)이 나옴을 알 수 있다.</p>
<p><strong>1.2.4 insertFront</strong></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">insertFront</span>(<span class="params">self, newItem</span>):</span><br><span class="line">	<span class="string">&#x27;&#x27;&#x27;head Node에 newItem을 삽입&#x27;&#x27;&#x27;</span></span><br><span class="line">	self.head = Node(newItem, self.head)</span><br><span class="line">	self.size += <span class="number">1</span></span><br></pre></td></tr></table></figure>

<p>리스트의 맨 처음 위치에 원소를 삽입할 때는 동적배열과는 달리 원소를 모두 뒤로 밀 필요가 없다. 따라서 이 경우에 시간 복잡도가 O(1)이 나온다.</p>
<p>코드 전체 : <a href="https://github.com/1Dohyeon/Study-DataStructure/blob/master/01_Lists_with_py/02_SinglyLinkedList.py">Code</a></p>
]]></content>
      <categories>
        <category>data structure</category>
      </categories>
      <tags>
        <tag>study</tag>
        <tag>data structure</tag>
      </tags>
  </entry>
  <entry>
    <title>자료구조(data structure) - Stack, Queue</title>
    <url>/2024/01/06/dataStructure2/</url>
    <content><![CDATA[<h3 id="2-1-Stack-Array"><a href="#2-1-Stack-Array" class="headerlink" title="2.1 Stack (Array)"></a>2.1 Stack (Array)</h3><p><strong>2.1.1 개념</strong></p>
<p>Stack(스택) 은 직역하면 <strong>쌓아 올린</strong> 것이다. 데이터들을 아날로그인 물컵에 쌓아올린다고 생각해보자. 가장 마지막에 넣은 데이터가 컵의 가장 위쪽에 있을 것이다. 또한 데이터를 빼낼 때는 안쪽부터가 아니라 컵 가장 위에 있는 데이터, 즉 마지막으로 들어온 데이터부터 쉽게 빼낼 수 있을 것이다.</p>
<span id="more"></span>

<p>스택은 이와 같은 개념으로 <a href="https://github.com/1Dohyeon/Study-DataStructure/blob/master/01_Lists_with_py/01_DynamicArray.py">1.1 Dynamic Array</a>에서 배운 동적 배열을 이용하여 설명하자면 데이터를 동적배열에 하나씩 담을 때는 배열의 가장 마지막에 원소가 담기게 될 것이다. 또한 배열에서 데이터를 삭제할 때는 배열의 가장 마지막 원소가 삭제되게 된다.</p>
<p><strong>2.1.2 peek()</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ArrayStack</span>&lt;E&gt; &#123;</span><br><span class="line">	<span class="keyword">private</span> E s[];</span><br><span class="line">	<span class="keyword">private</span> <span class="type">int</span> topIndex;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">public</span> <span class="title function_">ArrayStack</span><span class="params">()</span> &#123;</span><br><span class="line">		s = (E[]) <span class="keyword">new</span> <span class="title class_">Object</span>[<span class="number">1</span>];</span><br><span class="line">		topIndex = -<span class="number">1</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">isEmpty</span><span class="params">()</span> &#123;</span><br><span class="line">		<span class="keyword">return</span> (topIndex == -<span class="number">1</span>);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">public</span> E <span class="title function_">peek</span><span class="params">()</span> &#123;</span><br><span class="line">		<span class="keyword">if</span> (isEmpty()) &#123;</span><br><span class="line">			<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">EmptyStackException</span>();</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> s[topIndex];</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>배열의 인덱스는 0부터 시작하기 때문에 <code>topIndex</code> 의 초기값은 -1 로 지정하였다. <code>peek()</code> 메서드는 배열의 마지막 원소, 즉 가장 최근에 들어온 데이터를 반환한다.</p>
<p><strong>2.1.3 push()</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="type">int</span> <span class="title function_">size</span><span class="params">()</span> &#123;</span><br><span class="line">	<span class="keyword">return</span> topIndex + <span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">push</span><span class="params">(E newItem)</span> &#123;</span><br><span class="line">	<span class="keyword">if</span>(size()==s.length) &#123;</span><br><span class="line">		resize(<span class="number">2</span>*s.length);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	s[size()] = newItem;</span><br><span class="line">	topIndex++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>resize() 메서드는 <a href="https://github.com/1Dohyeon/Study-DataStructure/blob/master/01_Lists_with_py/01_DynamicArray.py">1.1 Dynamic Array</a> 참고</p>
</blockquote>
<p><code>size()</code> 메서드는 배열의 크기를 그대로 반환해야 하기 때문에 <code>topIndex</code> 에 +1 한 값을 반환한다. 이 <code>size()</code> 값이 배열의 길이와 같다면 배열의 길이를 증가시킨다.</p>
<p>그 후 <code>size()</code> 값을 배열의 인덱스로 주게 되면 가장 최근에 들어온 값보다 한층 더 위에를 가리키게 된다. 이곳에 새로운 원소를 담아주면 된다. 추가로 데이터가 늘어났으니 <code>topIndex</code>의 값을 늘려준다.</p>
<p><strong>2.1.4 pop()</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> E <span class="title function_">pop</span><span class="params">()</span> &#123;</span><br><span class="line">	<span class="keyword">if</span>(isEmpty()) &#123;</span><br><span class="line">		<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">EmptyStackException</span>();</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="type">E</span> <span class="variable">item</span> <span class="operator">=</span> s[topIndex];</span><br><span class="line">	s[topIndex] = <span class="literal">null</span>;</span><br><span class="line">	topIndex--;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span>(size()&gt;<span class="number">0</span> &amp;&amp; size()==s.length/<span class="number">4</span>) &#123;</span><br><span class="line">		resize(s.length/<span class="number">2</span>);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> item;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>pop()</code> 메서드는 가장 최근에 들어온 데이터를 삭제하고 그 값을 그대로 반환한다. 따라서 삭제하기 전에 <code>item</code> 이라는 변수에 배열의 가장 마지막 원소를 저장한다.</p>
<p><code>topIndex</code> 를 인덱스로 하는 배열의 원소를 지워주고, <code>topIndex</code>의 값을 줄인다. 이때, <code>size</code>의 길이가 배열 길이의 1&#x2F;4 크기가 된다면 배열의 길이를 줄인다. 그 후 저장해둔 <code>item</code> 을 반환한다.</p>
<p>코드 전체 : <a href="https://github.com/1Dohyeon/Study-DataStructure/blob/master/02_stack_and_queue_with_java/D1_ArrayStack/ArrayStack.java">Code</a></p>
<h3 id="2-2-Queue-LinkedList"><a href="#2-2-Queue-LinkedList" class="headerlink" title="2.2 Queue (LinkedList)"></a>2.2 Queue (LinkedList)</h3><p><strong>2.2.1 개념</strong></p>
<p>Queue(큐) 는 직역과 같이 <strong>줄</strong>의 의미를 가지고 있다. 가장 먼저 줄을 섰던 사람이 먼저 들어갈 수 있는 것처럼 큐에 먼저 들어온 데이터가 가장 먼저 나갈 수 있다. 스택과는 반대의 개념이라고 생각하면 쉽다.</p>
<p>큐를 <a href="https://github.com/1Dohyeon/Study-DataStructure/blob/master/02_stack_and_queue_with_java/D1_ArrayStack/ArrayStack.java">2.1 Array Stack</a> 처럼 동적 배열로 만들게 된다면 배열의 0번째 인덱스, 즉 첫번째 원소를 삭제하게 되면 나머지 뒤의 원소를 다 당겨와야 하기 때문에 비효율적이다. 물론, 삭제된 인덱스 값의 +1한 값을 가지는 변수를 통해서 앞으로 안당겨오게 할 수도 있긴 하다.</p>
<p>하지만 나는 노드의 head를 삭제 후에 갱신하는 방식으로 <a href="https://github.com/1Dohyeon/Study-DataStructure/blob/master/01_Lists_with_py/02_SinglyLinkedList.py">1.2 Singly Linked List</a> 를 통해서 구성하였다.</p>
<p><strong>2.2.2 생성자</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ListQueue</span>&lt;E <span class="keyword">extends</span> <span class="title class_">Comparable</span>&lt;E&gt;&gt; &#123;</span><br><span class="line">    <span class="keyword">private</span> Node&lt;E&gt; head, last;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> size;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 큐 생성자</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">ListQueue</span><span class="params">()</span> &#123;</span><br><span class="line">        head = last = <span class="literal">null</span>;</span><br><span class="line">        size = <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 큐에 있는 항목의 수를 리턴</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">size</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> size;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 큐가 empty면 true를 리턴</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">isEmpty</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> (size == <span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// empty가 되면 front == rear</span></span><br></pre></td></tr></table></figure>

<p>노드는 원소를 담을 수 있고, 다음 노드를 가리킬 수 있다. <a href="https://github.com/1Dohyeon/Study-DataStructure/blob/master/01_Lists_with_py/02_SinglyLinkedList.py">1.2 Singly Linked List</a> 에서는 <code>head</code> 만 따로 설정했지만, 삽입할 때 시간복잡도를 줄이기 위하여 <code>last</code> 를 통해서 리스트의 마지막 노드도 저장할 수 있도록 하였다.</p>
<p><strong>2.2.3 add</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 큐 add 연산</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">add</span><span class="params">(E newItem)</span> &#123;</span><br><span class="line">    Node&lt;E&gt; newNode = <span class="keyword">new</span> <span class="title class_">Node</span>&lt;E&gt;(newItem, <span class="literal">null</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (isEmpty()) &#123;</span><br><span class="line">        head = newNode;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        last.setNext(newNode);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    last = newNode;</span><br><span class="line">    size++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>우선 삽입하고 싶은 데이터를 가지는 노드를 생성한다. 큐가 비어있다면 <code>head</code> 가 그 노드가 되는 것이고, 아니라면 마지막 노드를 가리키는 <code>last</code> 의 다음 노드에 새로운 노드를 연결한다. 그 후 <code>last</code> 는 다시 마지막 노드를 가리키도록 이동한다.</p>
<p><strong>2.2.4 remove</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 큐 remove 연산</span></span><br><span class="line">    <span class="keyword">public</span> E <span class="title function_">remove</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (isEmpty()) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">NoSuchElementException</span>();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="type">E</span> <span class="variable">frontItem</span> <span class="operator">=</span> head.getItem();</span><br><span class="line">        head = head.getNext();</span><br><span class="line"></span><br><span class="line">        size--;</span><br><span class="line">        <span class="keyword">if</span> (isEmpty()) &#123;</span><br><span class="line">            last = <span class="literal">null</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> frontItem;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>첫번째 노드의 원소를 반환하기 위해서 <code>frontItem</code> 이라는 변수에 저장해두고, <code>head</code> 는 기존 <code>head</code> 의 다음 노드를 가리키게 함으로써 기존 <code>head</code> 를 삭제한다.</p>
<p>코드 전체 : <a href="https://github.com/1Dohyeon/Study-DataStructure/blob/master/02_stack_and_queue_with_java/D4_ListQueue/ListQueue.java">Code</a></p>
]]></content>
      <categories>
        <category>data structure</category>
      </categories>
      <tags>
        <tag>study</tag>
        <tag>data structure</tag>
      </tags>
  </entry>
  <entry>
    <title>자료구조(data structure) - Hash</title>
    <url>/2024/01/09/dataStructure5/</url>
    <content><![CDATA[<p>O(logN) 시간보다 빠른 연산을 하기 위해서, 키와 1차원 배열의 인덱스 관계를 이용한다. Key가 22라면 1차원 배열의 22번째 인덱스에 value를 저장하는 것이다.</p>
<p>위 개념을 이용하는 자료구조가 바로 해시이다. 하지만 위처럼 key를 바로 인덱스로 매핑시키는 것이 아니라 해싱(Hashing)을 key값을 변환한다. Hashing이란 간단한 함수를 통해서 키를 변환 후 그 값을 인덱스로 하는 배열의 위치에 항목을 저장하는 것이다.</p>
<span id="more"></span>

<p><img src="https://github.com/1Dohyeon/blogIMG/blob/master/nest1-datastructure-algorithms/%ED%95%B4%EC%8B%B1%EC%98%88%EC%8B%9C%EC%9D%B4%EB%AF%B8%EC%A7%80.png?raw=true" alt="해싱 예시 이미지.png"></p>
<p>키를 변환하는 함수를 해시 함수라고 하고, 해시값(Hash Value) 가 해시 함수가 계산을 하여 반환한 값이다. 이 값을 인덱스로 하는 배열이 바로 해시테이블(Hash Table) 이다.</p>
<p>하지만 만약에 서로 다른 키들이 같은 해시값을 갖게 되면 어떻게 될까? 바로 충돌(Collision) 이 발생한다. 해시함수들은 키들을 균등하게 해시테이블의 인덱스로 변환하여 충돌을 줄여야한다. 5.1과 5.2에서는 충돌된 키를 처리하는 과정을 설명한다.</p>
<h3 id="5-1-Open-Addressing"><a href="#5-1-Open-Addressing" class="headerlink" title="5.1 Open Addressing"></a>5.1 Open Addressing</h3><p>Open Addressing 은 개방주소방식이라고 하며 해시테이블 전체를 열린 공간으로 가정하고 충돌된 키를 일정한 방식에 따라서 찾아낸 빈 공간에 저장한다.</p>
<p><strong>5.1.1 Linear Probing</strong></p>
<p>해시 함수 <code>h(key) = key % table.length</code> 의 형태를 갖고 있다고 가정하자. key 값을 해시 테이블의 길이로 나눈 나머지를 해시값으로 하는 것이다. 앞으로 첫 해시 결과는 이 방법을 이용한다.</p>
<p>Linear Probing(선형조사) 방식에서 충돌이 일어나게 된다면 바론 다음칸에 값을 집어넣는다. 또 그 자리에 이미 다른 값이 있다면 계속 1칸씩 이동하며 빈자리를 찾는다.</p>
<p><img src="https://github.com/1Dohyeon/blogIMG/blob/master/nest1-datastructure-algorithms/%EC%84%A0%ED%98%95%EC%A1%B0%EC%82%AC%EC%98%88%EC%8B%9C%EC%9D%B4%EB%AF%B8%EC%A7%80.png?raw=true" alt="선형조사 예시 이미지.png"></p>
<p>즉, 충돌시에 충돌이 일어난 위치부터 순차적으로 검색하여 빈 공간에 키를 저장하므로 <code>( h(key) + j ) % M</code> 형태를 가짐을 알 수 있다.</p>
<p>선형조사의 문제점은 바로 1차 군집화이다. 충돌된 키들은 순차적으로 찾은 빈공간에 저장하므로 동일 해시값을 갖는 키들은 한곳에 뭉치게 된다.</p>
<p><strong>5.1.2 Quad Probing</strong></p>
<p>Quad Probing(이차조사) 는 한칸씩 움직이는 것이 아닌 제곱칸씩 움직인다.(1칸, 4칸, 9칸 …) 따라서 <code>( h(key) + j^2 ) % M</code> 형태를 가짐을 알 수 있다.</p>
<p>이와 같은 방식도 <strong>2차 군집화(Secondary Clustering)</strong> 라는 문제점이 발생한다. 서로 같은 해시값을 갖는 다른 키들이 똑같은 점프 시퀀스(Jump Sequence) 를 따라 빈 공간을 찾고 저장하므로 발생하는 다른 형태의 군집화를 뜻한다.</p>
<p><strong>5.1.3 Random Probing</strong></p>
<p><code>( h(key) + rand() ) % M</code> 와 같이 Random Probing(랜덤 조사) 는 랜덤한 값으로 이동시켜 빈 공간을 찾아서 충돌을 처리하는 방법이다. 이 방법도 똑같은 점프 시퀀스에 따라 빈 공간을 찾아 키를 저장하므로, <strong>3차 군집화</strong>가 발생한다.</p>
<p>랜덤인데 어떻게 똑같은 점프 시퀀스가 나타나는지 궁금할 수도 있다. 첫 충돌이 있었던 키가 랜덤 값으로 3, 1, 4 만큼 이동하였다고 가정해보자. 다음 키도 충돌이 있다면 똑같이 3, 1, 4 만큼 이동하고 또 충돌이 있을 경우 다시 랜덤 크기만큼 점프를 한다.</p>
<p><strong>5.1.4 Double Hashing</strong></p>
<p>Double Hashing(이중 해싱) 은 말 그대로 두번 해싱을 하는 것이다. 충돌이 일어나면 점프할 다음 위치를 정하기 위해 제 2의 해시함수 <code>d(key)</code> 를 사용한다. <code>( h(key) + j*d(key) ) % M</code> 형태를 가지며 처음 충돌이 일어나면 <code>key</code> 값을 한번 더 해싱한 값을 더한 것을 <code>M</code> 으로 나누면 되고, 또 충돌이 일어난다면 <code>d</code> 해싱에 2를 곱하고 위 과정을 반복하면 된다.</p>
<p><a href="https://github.com/1Dohyeon/Study-DataStructure/tree/master/05_Hash_with_java/D1_OpenAddressing">전체 코드</a></p>
<h3 id="5-2-Closed-Addressing-Chaining"><a href="#5-2-Closed-Addressing-Chaining" class="headerlink" title="5.2 Closed Addressing : Chaining"></a>5.2 Closed Addressing : Chaining</h3><p>Closed Addressing 은 폐쇄주소방식이라고 하며 충돌한다면 빈 곳을 찾는게 아니라 충돌한 키들을 한 위치에 모아서 저장한다. 대표적인 방법으로는 chaining(체이닝)이 있다.</p>
<p>충돌이 일어나면 테이블의 각 인덱스 안에 <a href="https://github.com/1Dohyeon/Study-DataStructure/blob/master/01_Lists_with_py/02_SinglyLinkedList.py">1.2 Singly Linked List</a> 에서 언급한 연결리스트를 추가하는 것이다.</p>
<p><img src="https://github.com/1Dohyeon/blogIMG/blob/master/nest1-datastructure-algorithms/%EC%B2%B4%EC%9D%B4%EB%8B%9D%EC%98%88%EC%8B%9C%EC%9D%B4%EB%AF%B8%EC%A7%80.png?raw=true" alt="체이닝 예시 이미지.png"></p>
<p>위와 같이 충돌이 일어난다면 저장되어 있는 연결리스트의 head 부분으로 새로운 노드가 삽입된다.</p>
<p><a href="https://github.com/1Dohyeon/Study-DataStructure/blob/master/05_Hash_with_java/D2_ClosedAddressing/Chaining.java">전체 코드</a></p>
]]></content>
      <categories>
        <category>data structure</category>
      </categories>
      <tags>
        <tag>study</tag>
        <tag>data structure</tag>
      </tags>
  </entry>
  <entry>
    <title>NestJS-CRUD, HTTP 메서드</title>
    <url>/2024/01/06/nest1-crud-httpMethod/</url>
    <content><![CDATA[<h3 id="01-GET-POST"><a href="#01-GET-POST" class="headerlink" title="01. GET, POST"></a>01. GET, POST</h3><h4 id="1-1-HTTP"><a href="#1-1-HTTP" class="headerlink" title="1.1 HTTP"></a>1.1 HTTP</h4><p>GET과 POST에 대해서 공부하기 전에 HTTP의 구조에 대해서 간단하게 알 필요가 있다.</p>
<span id="more"></span>

<p>url 구조를 보면 <code>http://www.google.com</code> 과 같은 형태로 되어있음을 알 수 있다(이는 url 형태의 예시고 url의 구조는 <a href="https://velog.io/@1_dohyeon/HTTP-%EA%B8%B0%EB%B3%B8-%EA%B0%9C%EB%85%90">이곳</a>에서 확인할 수 있다). 위 url 예시를 보면 <code>http</code> 라는 문자가 보인다.</p>
<p><code>http</code>란 html 문서와 리소스들을 가져올 수 있도록 해주는 <strong>프로토콜</strong>(&#x3D; 컴퓨터 사이에서 데이터를 주고 받을 때 쓰이는 규칙 체계)이다.</p>
<p>이런 규칙 체계를 따라서 우리는 인터넷 웹 브라우저에서 요청을 할 때 단순히 데이터를 보내는 것이 아니라 http 요청 메시지를 서버에 보낸다. 서버는 이 메시지를 읽고 사용자가 원하는 리소스의 주소를 웹 브라우저에 보낸다.</p>
<h4 id="1-2-HTTP-메서드-GET-POST-…"><a href="#1-2-HTTP-메서드-GET-POST-…" class="headerlink" title="1.2 HTTP 메서드 : GET, POST, …"></a>1.2 HTTP 메서드 : GET, POST, …</h4><p>http 요청 메시지를 서버에 보낼 때 <code>get</code>, <code>post</code> 와 같은 http 메서드를 이용한다. http 메서드에는 다른 종류도 있는데, 이것은 다음에 따로 공부할 계획이다.</p>
<p><code>get</code> 은 리소스를 조회하는 역할로 호출해도 리소스를 변경하지 않는다. 따라서 한 번을 호출하든 100번을 호출하든 결과는 같기에 <strong>멱등</strong>이라고 불린다.</p>
<p><code>post</code> 는 body로 넘긴 요청 데이터를 처리한다. 주로 무언가를 등록할 때 사용된다.</p>
<h3 id="02-CRUD"><a href="#02-CRUD" class="headerlink" title="02. CRUD"></a>02. CRUD</h3><p>CRUD란 Create(생성), Read(읽기), Update(갱신), Delete(삭제)를 묶어서 일컫는 말이다.</p>
<p><code>post</code> 메서드를 이용하여 request body의 데이터를 참고하여 새로운 객체를 생성한다면 이를 Create 라고 한다.</p>
<p><code>get</code> 메서드를 이용하여 단순히 데이터를 조회하는 것을 Read 라고 한다.</p>
<p>Update 는 말 그대로 기존에 있는 리소스를 업데이트 한다. 따라서 <code>put</code> 과 <code>patch</code> 메서드를 이용할 수 있다.</p>
<p>데이터를 전부 업데이트하고 싶다면 <code>put</code> 메서드, 일부만 업데이트 하고 싶다면 <code>patch</code> 를 사용하면 된다.</p>
<p>Delete 는 기존에 있는 리소스를 삭제하는 개념으로 http 메서드로는 <code>delete</code> 를 사용한다.</p>
<p>이 http 메서드들의 개념은 다음 글에서 다루겠다.</p>
<h3 id="03-PUT-PATCH-DELETE"><a href="#03-PUT-PATCH-DELETE" class="headerlink" title="03. PUT, PATCH, DELETE"></a>03. PUT, PATCH, DELETE</h3><p><code>put</code> 이란 리소스를 대체할 때 사용된다. 해당 리소스가 없다면 생성하고 있다면 데이터를 덮어버린다. 이 또한 한 번 호출하든 100번 호출하든 결과가 덮인 상태 그대로이기 때문에 똑같다. &#x3D; <strong>멱등</strong></p>
<p><code>patch</code> 는 리소스의 일부분만 변경한다.</p>
<p><code>delete</code> 는 리소스를 삭제한다. 이것도 100번 호출해도 삭제된 결과로 동일하기에 멱등하다고 볼 수 있다.</p>
<p>이제 이 메서드들의 사용예제는 나중에 컨트롤러를 공부하면서 다루겠다.</p>
]]></content>
      <categories>
        <category>NestJS</category>
      </categories>
      <tags>
        <tag>study</tag>
        <tag>NestJS</tag>
      </tags>
  </entry>
  <entry>
    <title>자료구조(data structure) - Sort</title>
    <url>/2024/01/10/dataStructure6/</url>
    <content><![CDATA[<h3 id="6-1-Selection-선택-정렬"><a href="#6-1-Selection-선택-정렬" class="headerlink" title="6.1 Selection 선택 정렬"></a>6.1 Selection 선택 정렬</h3><p><strong>6.1.1 개념</strong></p>
<p>선택정렬이란 각 루프마다 최대 원소를 찾고, 최대 원소와 맨 오른쪽 원소를 교환하는 것이다. 그 다음으로는 맨 오른쪽 원소를 제외하고 위 과정을 반복한다.</p>
<p>똑같은 방법이지만 반대로 최솟값을 찾고 가장 왼쪽과 교환한 후 다음 루프에서는 가장 왼쪽 원소를 제외하고 하나의 원소만 남을 때까지 루프를 반복시키는 방법도 있다.</p>
<span id="more"></span>

<p><strong>6.1.2 sort 코드</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">sort</span><span class="params">(Comparable[] a)</span> &#123;</span><br><span class="line">       <span class="type">int</span> <span class="variable">N</span> <span class="operator">=</span> a.length;</span><br><span class="line">       System.out.println(N);</span><br><span class="line">       <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; N; i++) &#123;</span><br><span class="line">           <span class="type">int</span> <span class="variable">min</span> <span class="operator">=</span> i;</span><br><span class="line">           <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">j</span> <span class="operator">=</span> i + <span class="number">1</span>; j &lt; N; j++) &#123; <span class="comment">// min 찾기</span></span><br><span class="line">               <span class="keyword">if</span> (isless(a[j], a[min])) &#123;</span><br><span class="line">                   min = j;</span><br><span class="line">               &#125;</span><br><span class="line">           &#125;</span><br><span class="line">           swap(a, i, min); <span class="comment">// min과 a[i]의 교환</span></span><br><span class="line">       &#125;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>

<p>위 코드에서는 최솟값을 찾고 앞 원소와 교환하는 방법을 선택하였다. 이중 <code>for</code> 문을 돌기 때문에 시간복잡도는 O(N^2) 를 가진다.</p>
<p><a href="https://github.com/1Dohyeon/Study-DataStructure/blob/master/06_sort_with_java/D1_Sort/Selection.java">전체 코드</a></p>
<hr>
<h3 id="6-2-Insertion-삽입-정렬"><a href="#6-2-Insertion-삽입-정렬" class="headerlink" title="6.2 Insertion 삽입 정렬"></a>6.2 Insertion 삽입 정렬</h3><p><strong>6.2.1 개념</strong></p>
<p>배열이 정렬된 부분과 정렬되지 않은 부분으로 나뉘며, 정렬이 안된 부분의 가장 왼쪽 원소를 정렬된 부분에 삽입하는 방식의 알고리즘이다.</p>
<p><img src="https://github.com/1Dohyeon/blogIMG/blob/master/nest1-datastructure-algorithms/%EC%82%BD%EC%9E%85%EC%A0%95%EB%A0%AC.png?raw=true" alt="삽입정렬.png"></p>
<p>정렬 안된 부분의 가장 왼쪽 i 를 정렬된 원소들과 비교를 하면서 순서에 맞게 현재 원소를 삽입한다. 삽입한 원소를 정렬하는 것으로 입력에 따라서 시간복잡도가 달라진다.</p>
<p>즉, 입력이 이미 정렬된 경우는 탐색만 하면 되므로 O(N) 이라는 시간복잡도를 가지고, 입력이 역으로 정렬된 경우는 최악의 경우로 O(N^2) 이라는 시간복잡도를 가진다.</p>
<p>예를 들어, 10억개의 데이터 중 10개를 제외하고는 모두 정렬이 되어있을 때, 나머지 10개를 정렬하기 위해서 그 10개만 반복문을 돌기에 O(N) 이라는 짧은 시간이 걸린다.</p>
<p>또한 입력 순서가 랜덤인 평균 경우에도 O(N^2) 이라는 시간복잡도를 가진다.</p>
<p><strong>6.2.2 sort 코드</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">sort</span><span class="params">(Comparable[] a)</span> &#123;</span><br><span class="line">       <span class="type">int</span> <span class="variable">N</span> <span class="operator">=</span> a.length;</span><br><span class="line">       <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">1</span>; i &lt; N; i++) &#123; <span class="comment">// i는 현재 원소의 인덱스</span></span><br><span class="line">           <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">j</span> <span class="operator">=</span> i; j &gt; <span class="number">0</span>; j--) &#123; <span class="comment">// 현재 원소를 정렬된 앞부분에 삽입</span></span><br><span class="line">               <span class="keyword">if</span> (isless(a[j], a[j - <span class="number">1</span>])) &#123;</span><br><span class="line">                   swap(a, j, j - <span class="number">1</span>);</span><br><span class="line">               &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                   <span class="keyword">break</span>;</span><br><span class="line">               &#125;</span><br><span class="line">           &#125;</span><br><span class="line">       &#125;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>

<p>위 코드의 진행 과정을 예시를 통해서 설명하자면 아래 이미지와 같다 :</p>
<p><img src="https://github.com/1Dohyeon/blogIMG/blob/master/nest1-datastructure-algorithms/%EC%82%BD%EC%9E%85%EC%A0%95%EB%A0%AC%EC%A7%84%ED%96%89%EA%B3%BC%EC%A0%95.png?raw=true" alt="삽입정렬 진행과정.png"></p>
<p><a href="https://github.com/1Dohyeon/Study-DataStructure/blob/master/06_sort_with_java/D1_Sort/Insertion.java">전체 코드</a></p>
<hr>
<h3 id="6-3-Bubble-버블-정렬"><a href="#6-3-Bubble-버블-정렬" class="headerlink" title="6.3 Bubble 버블 정렬"></a>6.3 Bubble 버블 정렬</h3><p><strong>6.3.1 개념</strong></p>
<p>버블 정렬은 인접한 두 원소를 비교하면서 상대적으로 큰 원소를 뒤로 옮긴다. 이것을 계속 진행하다 보면 마지막 위치에는 가장 큰 원소가 있을 것이다. 이렇게 하나의 루프를 돌았다면 끝자리를 제외하고 다시 이 루프를 반복한다.</p>
<p><strong>6.3.2 sort 코드</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">bubbleSort</span><span class="params">(<span class="type">int</span>[] array)</span> &#123;</span><br><span class="line">       <span class="type">int</span> <span class="variable">n</span> <span class="operator">=</span> array.length;</span><br><span class="line">       <span class="type">boolean</span> swapped;</span><br><span class="line"></span><br><span class="line">       <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; n - <span class="number">1</span>; i++) &#123;</span><br><span class="line">           swapped = <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">           <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">j</span> <span class="operator">=</span> <span class="number">0</span>; j &lt; n - <span class="number">1</span> - i; j++) &#123;</span><br><span class="line">               <span class="keyword">if</span> (array[j] &gt; array[j + <span class="number">1</span>]) &#123;</span><br><span class="line">                   <span class="comment">// 두 원소의 위치 바꿈</span></span><br><span class="line">                   <span class="type">int</span> <span class="variable">temp</span> <span class="operator">=</span> array[j];</span><br><span class="line">                   array[j] = array[j + <span class="number">1</span>];</span><br><span class="line">                   array[j + <span class="number">1</span>] = temp;</span><br><span class="line">                   swapped = <span class="literal">true</span>;</span><br><span class="line">               &#125;</span><br><span class="line">           &#125;</span><br><span class="line"></span><br><span class="line">           <span class="comment">// 배열이 정렬되었다면 더 이상 반복할 필요가 없다.</span></span><br><span class="line">           <span class="keyword">if</span> (!swapped) &#123;</span><br><span class="line">               <span class="keyword">break</span>;</span><br><span class="line">           &#125;</span><br><span class="line">       &#125;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>

<p>이중 <code>for</code> 문의 형태를 하고 있으므로 시간복잡도는 O(N^2) 임을 알 수 있다. 또 위 코드에서는 <code>swapped</code> 이 일어나지 않았으면 반복문을 종료시킨다.</p>
<p>원소들이 <code>j</code> 반복문을 한바퀴 도는 동안 교환이 하나도 일어나지 않았다면, 이미 정렬되어 있음을 뜻하기 때문이다.</p>
<p><a href="https://github.com/1Dohyeon/Study-DataStructure/blob/master/06_sort_with_java/D1_Sort/Bubble.java">전체 코드</a></p>
<hr>
<h3 id="6-4-Heap-힙-정렬"><a href="#6-4-Heap-힙-정렬" class="headerlink" title="6.4 Heap 힙 정렬"></a>6.4 Heap 힙 정렬</h3><p><strong>6.4.1 개념</strong></p>
<p>힙 정렬을 알기 위해서는 Heap(힙)에 대해서 알아야한다.</p>
<ul>
<li>Heap : <a href="https://github.com/1Dohyeon/Study-DataStructure/tree/master/04_PriorityQ_with_java">Heap with Priority Queue</a></li>
</ul>
<p>최대 힙을 구성하게 되면 루트 원소에는 가장 큰 값이 저장될 것이다. 이제 그 루트 값과 마지막 인덱스 값을 교환하면 된다.</p>
<p>위와 같이 진행한다면 마지막 인덱스에는 가장 큰 값이 담기게 된다. 따라서 이 이후에는 바뀐 마지막 인덱스를 제외하고 위 과정을 다시 반복하면 된다.</p>
<p><strong>6.4.2 sort 코드</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">sort</span><span class="params">(Comparable[] a)</span> &#123;</span><br><span class="line">       <span class="type">int</span> <span class="variable">heapSize</span> <span class="operator">=</span> a.length - <span class="number">1</span>;</span><br><span class="line">       <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> heapSize / <span class="number">2</span>; i &gt; <span class="number">0</span>; i--) &#123; <span class="comment">// 힙 만들기</span></span><br><span class="line">           downHeap(a, i, heapSize);</span><br><span class="line">       &#125;</span><br><span class="line">       <span class="keyword">while</span> (heapSize &gt; <span class="number">1</span>) &#123; <span class="comment">// 힙 정렬</span></span><br><span class="line">           swap(a, <span class="number">1</span>, heapSize--);</span><br><span class="line">           downHeap(a, <span class="number">1</span>, heapSize);</span><br><span class="line">       &#125;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>

<p>우선 <code>for</code> 문을 통해서 힙을 만들어 준 후에 힙정렬을 실행해준다. 6.4.1 에서 설명 했듯이 마지막 원소와 마지막 원소를 <code>swap</code> 후에 다시 <code>downheap</code> 을 수행하는 것을 알 수 있다.</p>
<p>bottom up 방식으로 힙을 구성하는 것은 <code>O(N)</code> 의 시간복잡도를 가지고 루트와 힙의 마지막 노드를 교환하고, <code>downheap()</code> 을 수행하는 과정은 트리의 높이와 비례하므로 <code>O(logN)</code> 임을 알 수 있다.</p>
<p>따라서 힙 정렬은 <code>O(NlogN)</code> 시간이 걸린다.</p>
<p><a href="https://github.com/1Dohyeon/Study-DataStructure/blob/master/06_sort_with_java/D1_Sort/Heap.java">전체 코드</a></p>
<hr>
<h3 id="6-5-Quick-퀵-정렬"><a href="#6-5-Quick-퀵-정렬" class="headerlink" title="6.5 Quick 퀵 정렬"></a>6.5 Quick 퀵 정렬</h3><p><strong>6.5.1 개념</strong></p>
<p>퀵 정렬은 맨 왼쪽이나 오른쪽 원소를 택해서 피벗(Pivot)으로 둔다. 피벗보다 작은 원소들은 왼쪽 큰 원소들은 오른쪽에 둔다. 이렇게 되면 피벗의 왼쪽과 오른쪽 집합들로 나뉘게 될 것이다. 이 집합들에서도 따로 피벗을 구하여 위 과정을 반복하면 된다.</p>
<p><strong>6.5.2 sort 코드</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">sort</span><span class="params">(Comparable[] a, <span class="type">int</span> low, <span class="type">int</span> high)</span> &#123;</span><br><span class="line">       <span class="keyword">if</span> (high &lt;= low) &#123;</span><br><span class="line">           <span class="keyword">return</span>;</span><br><span class="line">       &#125; <span class="comment">// 정렬할게 없음</span></span><br><span class="line">       <span class="type">int</span> <span class="variable">j</span> <span class="operator">=</span> partition(a, low, high);</span><br><span class="line">       sort(a, low, j - <span class="number">1</span>); <span class="comment">// 피벗보다 작은 부분을 재귀호출</span></span><br><span class="line">       sort(a, j + <span class="number">1</span>, high); <span class="comment">// 피벗보다 큰 부분을 재귀호출</span></span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>

<p>6.5.1 에서 설명한 것처럼 <code>partition</code> 메서드를 이용하여 크기에 따라 왼쪽과 오른쪽 집합을 나눈 후 <code>sort</code> 를 왼쪽 오른쪽 불러온다. 따라서 <code>partiton</code> 메서드는 어떻게 작동하는지 알 필요가 있다.</p>
<p><strong>6.5.3 partition 코드</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="type">int</span> <span class="title function_">partition</span><span class="params">(Comparable[] a, <span class="type">int</span> pivot, <span class="type">int</span> high)</span> &#123;</span><br><span class="line">       <span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> pivot + <span class="number">1</span>;</span><br><span class="line">       <span class="type">int</span> <span class="variable">j</span> <span class="operator">=</span> high;</span><br><span class="line">       <span class="type">Comparable</span> <span class="variable">p</span> <span class="operator">=</span> a[pivot];</span><br><span class="line">       <span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">           <span class="keyword">while</span> (i &lt;= high &amp;&amp; !isless(p, a[i])) &#123;</span><br><span class="line">               i++;</span><br><span class="line">           &#125; <span class="comment">// 피벗보다 같거나 작으면</span></span><br><span class="line">           <span class="keyword">while</span> (j &gt;= pivot &amp;&amp; isless(p, a[j])) &#123;</span><br><span class="line">               j--;</span><br><span class="line">           &#125; <span class="comment">// 피벗보다 크면</span></span><br><span class="line">           <span class="keyword">if</span> (i &gt;= j) &#123;</span><br><span class="line">               <span class="keyword">break</span>;</span><br><span class="line">           &#125; <span class="comment">// i와 j가 교차되면 루프 나가기</span></span><br><span class="line">           swap(a, i, j);</span><br><span class="line">       &#125;</span><br><span class="line">       swap(a, pivot, j); <span class="comment">// 피벗과 a[j]교한</span></span><br><span class="line">       <span class="keyword">return</span> j; <span class="comment">// a[j]의 피벗이 자리 잡은 곳</span></span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="type">boolean</span> <span class="title function_">isless</span><span class="params">(Comparable i, Comparable j)</span> &#123; <span class="comment">// 키 비교</span></span><br><span class="line">       <span class="keyword">return</span> (i.compareTo(j) &lt; <span class="number">0</span>);</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>

<p><code>partition</code> 메서드는 매개변수로 비교할 배열과 피벗, 그리고 배열의 마지막 원소 인덱스를 받는다. 따라서 6.5.2에서 처음 <code>partition</code> 을 불러왔을 때는 <code>low</code> 를 피벗으로 매개변수로 둔 것을 알 수 있다.(<code>low</code>: 맨 왼쪽 원소, <code>high</code>: 맨 오른쪽 원소)</p>
<p><code>i</code>는 피벗의 다음 원소의 인덱스, <code>j</code>는 가장 오른쪽 원소의 인덱스로 지정한다. <code>p</code>는 <code>pivot</code> 으로 받은 인덱스의 원소이다.</p>
<p>우선 <code>i &lt;= high &amp;&amp; !isless(p, a[i])</code> 조건의 반복문을 통해서 <code>a[i]</code>가 <code>p</code>와 같거나 작은지 비교한다. <code>i</code>가 <code>high</code>(마지막 원소)보다 작고(앞으로 나아갈 곳이 있다는 뜻), <code>a[i] &lt; p</code> 라면 현재 원소는 비교할 필요가 없다. 따라서 <code>i++</code> 를 함으로써 다음 원소와 비교한다.</p>
<p>그다음은 거꾸로 <code>j</code>(끝에서부터 이동) 가 피벗보다 크고, 앞으로 이동할 곳이 있다면 <code>j--</code>를 함으로써 다음 원소와 비교한다.</p>
<p>만약 <code>a[i] &gt; a[j]</code> 라면 둘의 위치를 바꾸어 피벗보다 큰 값을 뒤로가게 한다. 이렇게 <code>i</code>와 <code>j</code>를 이동하게 되면 겹치는 자리가 있을 것이다. 그 자리에 기준이 되었던 피벗을 옮기게 되면 피벗보다 작은 원소는 왼쪽, 큰 원소는 오른쪽에 위치하게 된다.</p>
<p>만약 피벗이 가장 작거나, 가장 큰값이라고 가정한다면 원소의 이동이 많아질 것이다. 따라서 최악의 경우에는 모든 원소를 옮겨야하므로 <code>O(N^2)</code> 의 시간복잡도를 가진다. 하지만 평균의 경우는 <code>O(NlogN)</code> 의 시간복잡도를 가진다.</p>
<p><a href="https://github.com/1Dohyeon/Study-DataStructure/blob/master/06_sort_with_java/D1_Sort/Quick.java">전체 코드</a></p>
<hr>
<h3 id="6-6-Merge-합병-병합-정렬"><a href="#6-6-Merge-합병-병합-정렬" class="headerlink" title="6.6 Merge 합병(병합) 정렬"></a>6.6 Merge 합병(병합) 정렬</h3><p><strong>6.6.1 개념</strong></p>
<p>합병(병합) 정렬은 크기가 <code>N</code> 인 배열을 <code>1/2N</code> 크기를 갖는 배열로 분할하고, 각각에 대해서 재귀적으로 합병정렬을 또 실행한다. 그 후에 2개의 정렬된 부분을 합병하여 배열을 다시 완성시킨다. 따라서 합병 정렬은 배열을 다시 만드는 것이기 때문에 추가 공간으로 <code>N</code> 만큼을 요구한다.</p>
<p><strong>6.6.2 sort &amp; merge 코드</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">sort</span><span class="params">(Comparable[] a, Comparable[] b, <span class="type">int</span> low, <span class="type">int</span> high)</span> &#123;</span><br><span class="line">       <span class="keyword">if</span> (high &lt;= low) &#123;</span><br><span class="line">           <span class="keyword">return</span>;</span><br><span class="line">       &#125;</span><br><span class="line">       <span class="type">int</span> <span class="variable">mid</span> <span class="operator">=</span> low + (high - low) / <span class="number">2</span>;</span><br><span class="line">       sort(a, b, low, mid); <span class="comment">// 앞부분 재귀호출</span></span><br><span class="line">       sort(a, b, mid + <span class="number">1</span>, high); <span class="comment">// 뒷부분 재귀호출</span></span><br><span class="line">       merge(a, b, low, mid, high); <span class="comment">// 합병 수행</span></span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">sort</span><span class="params">(Comparable[] a)</span> &#123;</span><br><span class="line">       Comparable[] b = <span class="keyword">new</span> <span class="title class_">Comparable</span>[a.length];</span><br><span class="line">       sort(a, b, <span class="number">0</span>, a.length - <span class="number">1</span>);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">merge</span><span class="params">(</span></span><br><span class="line"><span class="params">	Comparable[] a,</span></span><br><span class="line"><span class="params">	Comparable[] b,</span></span><br><span class="line"><span class="params">	<span class="type">int</span> low, <span class="type">int</span> mid, <span class="type">int</span> high</span></span><br><span class="line"><span class="params">)</span> &#123;</span><br><span class="line">       <span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> low, j = mid + <span class="number">1</span>;</span><br><span class="line">       <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">k</span> <span class="operator">=</span> low; k &lt;= high; k++) &#123; <span class="comment">// 배열 a의 앞부분과 뒷부분을 합병하여 보조배열 b에 저장</span></span><br><span class="line">           <span class="keyword">if</span> (i &gt; mid) &#123;</span><br><span class="line">               b[k] = a[j++];</span><br><span class="line">           &#125; <span class="comment">// 앞부분이 먼저 소진된 경우</span></span><br><span class="line">           <span class="keyword">else</span> <span class="keyword">if</span> (j &gt; high) &#123;</span><br><span class="line">               b[k] = a[i++];</span><br><span class="line">           &#125; <span class="comment">// 뒷부분이 먼저 소진된 경우</span></span><br><span class="line">           <span class="keyword">else</span> <span class="keyword">if</span> (isless(a[j], a[i])) &#123;</span><br><span class="line">               b[k] = a[i++];</span><br><span class="line">           &#125; <span class="comment">// a[j]가 승자</span></span><br><span class="line">           <span class="keyword">else</span> &#123;</span><br><span class="line">               b[k] = a[i++];</span><br><span class="line">           &#125; <span class="comment">// a[i]가 승자</span></span><br><span class="line">       &#125;</span><br><span class="line">       <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">k</span> <span class="operator">=</span> low; k &lt;= high; k++) &#123;</span><br><span class="line">           a[k] = b[k];</span><br><span class="line">       &#125; <span class="comment">// 보조 배열 b를 a에 복사</span></span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>

<p>위 코드를 보면 알 수 있듯 <code>sort(Comparable[] a)</code> 를 호출하면 이 함수 내에서 <code>private</code> 로 선언된 <code>sort</code> 가 호출되고, 그 <code>sort</code> 안에서는 재귀적으로 앞과 뒷부분을 호출하고 합병을 수행하는 것을 알 수 있다. 위 과정을 이미지로 설명하자면 아래와 같다 :</p>
<p><img src="https://github.com/1Dohyeon/blogIMG/blob/master/nest1-datastructure-algorithms/%EB%B3%91%ED%95%A9%EC%A0%95%EB%A0%AC%EC%9D%B4%EB%AF%B8%EC%A7%80.png?raw=true" alt="병합정렬이미지.png"></p>
<p>나눌 수 있을 때까지 나누고 <code>merge</code> 를 호출하여 새로운 배열 <code>b[]</code> 에 순서에 맞게 원소를 집어넣어 합병한다.</p>
<p><a href="https://github.com/1Dohyeon/Study-DataStructure/blob/master/06_sort_with_java/D1_Sort/Merge.java">전체 코드</a></p>
]]></content>
      <categories>
        <category>data structure</category>
      </categories>
      <tags>
        <tag>study</tag>
        <tag>data structure</tag>
      </tags>
  </entry>
  <entry>
    <title>NestJS-Schema(스키마)</title>
    <url>/2024/01/12/nest10-schema/</url>
    <content><![CDATA[<h3 id="12-1-데이터베이스-Schema-정의"><a href="#12-1-데이터베이스-Schema-정의" class="headerlink" title="12.1 (데이터베이스)Schema 정의"></a>12.1 (데이터베이스)Schema 정의</h3><blockquote>
<p>데이터베이스에서의 스키마는 자료의 표현 방법으로 자료간의 관계를 형식 언어로 정의한 구조이다. - wikipedia</p>
</blockquote>
<span id="more"></span>

<p>어느 모듈이 있을 때 이 모듈에는 여러 데이터가 있을 것이다. 예를 들어서 <code>User</code> 라는 모듈이 있고, 이 모듈은 email, name, password 등 여러 데이터를 가지고 있다고 가정하자. 이때 스키마를 통해서 데이터의 형태를 정의할 수 있다.</p>
<h3 id="12-2-Schema-예제"><a href="#12-2-Schema-예제" class="headerlink" title="12.2 Schema 예제"></a>12.2 Schema 예제</h3><figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Prop</span>, <span class="title class_">Schema</span>, <span class="title class_">SchemaFactory</span> &#125; <span class="keyword">from</span> <span class="string">&quot;@nestjs/mongoose&quot;</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">IsEmail</span>, <span class="title class_">IsNotEmpty</span>, <span class="title class_">IsString</span> &#125; <span class="keyword">from</span> <span class="string">&quot;class-validator&quot;</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Document</span>, <span class="title class_">SchemaOptions</span> &#125; <span class="keyword">from</span> <span class="string">&quot;mongoose&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> <span class="attr">options</span>: <span class="title class_">SchemaOptions</span> = &#123;</span><br><span class="line">  <span class="attr">timestamps</span>: <span class="literal">true</span>,</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Schema</span>(options)</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">Cat</span> <span class="keyword">extends</span> <span class="title class_ inherited__">Document</span> &#123;</span><br><span class="line">  <span class="meta">@Prop</span>(&#123;</span><br><span class="line">    <span class="attr">required</span>: <span class="literal">true</span>,</span><br><span class="line">    <span class="attr">unique</span>: <span class="literal">true</span>,</span><br><span class="line">  &#125;)</span><br><span class="line">  <span class="meta">@IsEmail</span>()</span><br><span class="line">  <span class="meta">@IsNotEmpty</span>()</span><br><span class="line">  <span class="attr">email</span>: <span class="built_in">string</span>;</span><br><span class="line"></span><br><span class="line">  <span class="meta">@Prop</span>(&#123;</span><br><span class="line">    <span class="attr">required</span>: <span class="literal">true</span>,</span><br><span class="line">  &#125;)</span><br><span class="line">  <span class="meta">@IsString</span>()</span><br><span class="line">  <span class="meta">@IsNotEmpty</span>()</span><br><span class="line">  <span class="attr">name</span>: <span class="built_in">string</span>;</span><br><span class="line"></span><br><span class="line">  <span class="meta">@Prop</span>(&#123;</span><br><span class="line">    <span class="attr">required</span>: <span class="literal">true</span>,</span><br><span class="line">  &#125;)</span><br><span class="line">  <span class="meta">@IsString</span>()</span><br><span class="line">  <span class="meta">@IsNotEmpty</span>()</span><br><span class="line">  <span class="attr">password</span>: <span class="built_in">string</span>;</span><br><span class="line"></span><br><span class="line">  <span class="meta">@Prop</span>()</span><br><span class="line">  <span class="meta">@IsString</span>()</span><br><span class="line">  <span class="attr">imgUrl</span>: <span class="built_in">string</span>;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">readonly</span> <span class="attr">readOnlyData</span>: &#123; <span class="attr">id</span>: <span class="built_in">string</span>; <span class="attr">email</span>: <span class="built_in">string</span>; <span class="attr">name</span>: <span class="built_in">string</span> &#125;;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">const</span> <span class="title class_">CatSchema</span> = <span class="title class_">SchemaFactory</span>.<span class="title function_">createForClass</span>(<span class="title class_">Cat</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 필요한 데이터만 return할 수 있게 함.</span></span><br><span class="line"><span class="title class_">CatSchema</span>.<span class="title function_">virtual</span>(<span class="string">&quot;readOnlyData&quot;</span>).<span class="title function_">get</span>(<span class="keyword">function</span> (<span class="params"><span class="variable language_">this</span>: Cat</span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> &#123;</span><br><span class="line">    <span class="attr">id</span>: <span class="variable language_">this</span>.<span class="property">id</span>,</span><br><span class="line">    <span class="attr">email</span>: <span class="variable language_">this</span>.<span class="property">email</span>,</span><br><span class="line">    <span class="attr">name</span>: <span class="variable language_">this</span>.<span class="property">name</span>,</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>위 코드는 Cats 모듈이 가지고 있는 데이터를 정의한 것이다. 회원가입을 할 수 있는 User라고 생각하면 된다. Cats 모듈은 email, name, password, imgUrl 데이터를 가지고 있다.</p>
<p><code>class-validator</code> 를 통해서 데이터의 조건을 설정할 수 있다. 가장 많이 보이는 형태는 아래와 같은 데코레이터이다.</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Prop</span>(&#123;</span><br><span class="line">    <span class="attr">required</span>: <span class="literal">true</span>,</span><br><span class="line">    <span class="attr">unique</span>: <span class="literal">true</span>,</span><br><span class="line"> &#125;)</span><br></pre></td></tr></table></figure>

<p><code>required</code> 는 객체가 생성될 때 꼭 필요한 데이터임을 의미하고, <code>unique</code> 는 데이터베이스에서 딱 하나만 존재해야 된다는 뜻이다. 즉, 우리가 어느 웹사이트를 회원가입을 할 때 아이디 중복이 되지 않는 것처럼 중복 데이터를 가진 객체가 생성될 수 없다는 뜻이다.</p>
<p><code>@IsEmail()</code> 는 데이터가 이메일 형태로 되어있어야 함을 뜻한다. <code>@IsNotEmpty()</code> 는 데이터를 “” 와 같이 비어있으면 안되고 조건에 맞는 타입이 무조건적으로 작성되어야 함을 뜻한다.</p>
<p>또한 객체를 생성할 때 password는 반환되면 안되도록 <code>SchemaFactory.createForClass(Cat).virtual().get()</code> 을 통해서 필요한 정보만 반환하도록 한다.</p>
<p><strong>Schema</strong> 의 사용 목적을 간단하게 요약하자면 데이터의 구조 정의이다. 데이터 형식과 관계를 명시하기 위해서 사용한다. 즉, 데이터의 구조를 정의하고 유지하는 데 사용된다.</p>
]]></content>
      <categories>
        <category>NestJS</category>
      </categories>
      <tags>
        <tag>study</tag>
        <tag>NestJS</tag>
      </tags>
  </entry>
  <entry>
    <title>자료구조(data structure) - Graph</title>
    <url>/2024/01/11/dataStructure7/</url>
    <content><![CDATA[<p>그래프란 집합으로 설명하자면 정점 집합 <code>V</code>와 정점을 연결하는 간선 집합 <code>E</code>로 구성된 집합 <code>G</code> 이다.</p>
<p>지하철 노선도를 생각하면 쉽게 이해할 수 있다. 역들이 각 정점이고 역 사이를 연결하는 것이 바로 간선이다.</p>
<span id="more"></span>

<p><img src="https://github.com/1Dohyeon/blogIMG/blob/master/nest1-datastructure-algorithms/%EC%A7%80%ED%95%98%EC%B2%A0%EB%85%B8%EC%84%A0%EB%8F%84%EA%B7%B8%EB%9E%98%ED%94%84%EC%98%88%EC%8B%9C.png?raw=true" alt="지하철 노선도 그래프 예시.png"></p>
<h3 id="7-1-DFS"><a href="#7-1-DFS" class="headerlink" title="7.1 DFS"></a>7.1 DFS</h3><p><strong>7.1.1 개념</strong></p>
<p>DFS란 Depth-First Search 의 약자이다. 말 그대로 깊이를 우선적으로 탐색한다. 즉 더 이동할 수 있을 정도로 다 이동하고 이동할 곳이 없다면 다른 간선에서 이 과정을 반복한다.(아래 그림 참고)</p>
<p><img src="https://github.com/1Dohyeon/blogIMG/blob/master/nest1-datastructure-algorithms/dfs%EC%98%88%EC%A0%9C.png?raw=true" alt="dfs 예제.png"></p>
<p><strong>7.1.2 pseudocode</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">DFS(G)</span><br><span class="line">&#123;</span><br><span class="line">	<span class="keyword">for</span> each v ∈ V</span><br><span class="line">		visited[v] ← NO;</span><br><span class="line">	<span class="keyword">for</span> each v ∈ V</span><br><span class="line">		<span class="title function_">if</span> <span class="params">(visited[v] = NO)</span> then <span class="title function_">aDFS</span><span class="params">(v)</span>;</span><br><span class="line">&#125;</span><br><span class="line">aDFS (v)</span><br><span class="line">&#123;</span><br><span class="line">	visited[v] ← YES;</span><br><span class="line">	<span class="keyword">for</span> each x ∈ L(v) ▷ L(v) : 정점 v의 인접 리스트</span><br><span class="line">		<span class="keyword">if</span> (visited[x] = NO) then <span class="title function_">aDFS</span><span class="params">(x)</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>DFS(G) 함수</strong></p>
<ol>
<li><code>visited</code> 배열을 초기화한다. <code>visited[v]</code>는 정점 <code>v</code>가 방문되었는지 여부를 나타낸다. 초기에 모든 정점은 방문되지 않은 상태(NO)로 표시한다.</li>
<li>모든 정점에 대해서 다음을 반복한다. 1) 정점 <code>v</code>가 아직 방문되지 않았다면(<code>visited[v] = NO</code>), <code>aDFS(v)</code> 함수를 호출하여 해당 정점에서 깊이 우선 탐색을 시작한다.<br><strong>aDFS(v) 함수</strong></li>
<li>현재 정점 <code>v</code>를 방문했다고 표시한다.(<code>visited[v] ← YES</code>).</li>
<li>정점 <code>v</code>의 인접 리스트를 순회하면서, 방문하지 않은 인접 정점 <code>x</code>를 발견하면 해당 정점을 방문했다고 표시하고 <code>aDFS(x)</code> 함수를 재귀적으로 호출하여 깊이 우선 탐색을 계속 진행한다.</li>
</ol>
<p><strong>7.1.3 진행과정 예시</strong></p>
<p><img src="https://github.com/1Dohyeon/blogIMG/blob/master/nest1-datastructure-algorithms/dfs%EC%A7%84%ED%96%89%EA%B3%BC%EC%A0%95%EC%98%88%EC%8B%9C.png?raw=true" alt="dfs 진행 과정 예시.png"></p>
<ol>
<li>초기 노드 1의 <code>visited=YES</code> 로 설정한다.</li>
<li>1의 인접리스트(2-&gt;3-&gt;4) 중 작은 것부터 다시 aDFS(v)를 호출한다.</li>
<li>2의 인접리스트는 3이므로 2의 <code>visited=YES</code> 로 설정하고, 3을 탐색하러 간다.</li>
<li>3의 인접리스트(1-&gt;2-&gt;4-&gt;5) 중 작은 것부터 다시 aDFS(v)를 호출하는데, 1과 2의 <code>visited=YES</code> 임으로 3을 <code>visited=YES</code> 로 설정하고, 4에서 탐색을 한다.</li>
<li>4의 인접리스트는(1-&gt;3-&gt;6-&gt;7)에서 1과 3은 <code>visited=YES</code> 임으로 4를 <code>visited=YES</code> 로 설정하고, 재귀호출로 인해 6에서 다시 탐색을 한다.</li>
<li>6의 인접리스트(4-&gt;7-&gt;8)에서 4는 <code>visited=YES</code> 임으로 6을 <code>visited=YES</code> 로 설정하고, 재귀호출로 인하여 7을 탐색한다.</li>
<li>7의 인접리스트(4-&gt;6-&gt;8)에서 4와 6은 <code>visited=YES</code> 임으로 7을 <code>visited=YES</code> 로 설정하고, 8을 탐색한다.</li>
<li>8도 <code>visited=YES</code> 로 설정하고, 다시 3으로 돌아와서 <code>visited=NO</code>인 5를 탐색한다.<br>즉, 1-&gt;2-&gt;3-&gt;4-&gt;6-&gt;7-&gt;8-&gt;5 순으로 탐색한다.</li>
</ol>
<h3 id="7-2-BFS"><a href="#7-2-BFS" class="headerlink" title="7.2 BFS"></a>7.2 BFS</h3><p><strong>7.2.1 개념</strong></p>
<p>BFS는 Breadth First Search의 약자로 한글로는 너비 우선 탐색이라고 읽는다. DFS는 알고리즘은 갈림길에서 한곳만 깊게 탐색하지만 BFS는 갈림길이 있을 경우 그 길과 연결된 노드를 전부 탐색하고 다음 노드로 이동하여 반복한다.</p>
<p><strong>7.2.2 pseudocode</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">BFS(G, v)</span><br><span class="line">&#123;</span><br><span class="line">	<span class="keyword">for</span> each v ∈ V –&#123;s&#125;</span><br><span class="line">		visited[v] ← NO;</span><br><span class="line"></span><br><span class="line">	visited[s] ← YES; ▷ s: 시작 정점</span><br><span class="line">	enqueue( Q, s); ▷ Q: 큐</span><br><span class="line"></span><br><span class="line">	<span class="keyword">while</span> ( Q ≠ ϕ) &#123;</span><br><span class="line">		u ← dequeue( Q);</span><br><span class="line">		<span class="keyword">for</span> each v ∈ L(u) ▷ L(u): 정점 u의 인접 리스트</span><br><span class="line">			<span class="keyword">if</span> (visited[v] = NO) then</span><br><span class="line">				visited[ v] ← YES;</span><br><span class="line">				enqueue( Q, v);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ol>
<li>그래프의 노드 수만큼 방문 여부를 확인할 배열 <code>visited</code> 생성, <code>visited[v]</code>는 정점 <code>v</code>가 방문되었는지 여부를 확인함. 즉, 처음엔 전부 <code>NO</code></li>
<li>시작 정점 <code>s</code>는 이미 반복했다 치고 <code>YES</code>로 바꾸고 큐 <code>Q</code>에 삽입.</li>
<li><code>Q</code>가 비어있지 않은 동안 다음을 반복한다.<ol>
<li>큐에서 정점 <code>u</code>를 dequeue 한다.</li>
<li><code>u</code>의 인접 리스트를 순회하면서, 방문하지 않은 인접 정점 <code>v</code>를 발견하면 해당 정점을 방문했다고 표시하고 큐에 enqueue 한다.</li>
</ol>
</li>
</ol>
<p><strong>7.2.3 진행과정 예시</strong></p>
<p><img src="https://github.com/1Dohyeon/blogIMG/blob/master/nest1-datastructure-algorithms/bfs%EC%A7%84%ED%96%89%EA%B3%BC%EC%A0%95%EC%98%88%EC%8B%9C.png?raw=true" alt="bfs 진행 과정 예시.png"></p>
<ol>
<li>1을 Q 에 담음. (Q &#x3D; [1])</li>
<li>1을 <code>Q</code> 에서 제거하고 1의 인접리스트(2-&gt;3-&gt;4)를 큐에 담음.(<code>Q = [2, 3, 4]</code>)</li>
<li>2의 인접리스트는 1-&gt;3이지만, 1과 3은 visited&#x3D;<code>YES</code> 임. 따라서 탐색하지 않고 2는 <code>Q</code> 에서 삭제(<code>Q = [3, 4]</code>)</li>
<li>3의 인접리스트는 1-&gt;2-&gt;4-&gt;5이지만 5를 제외한 나머지는 visited&#x3D;<code>YES</code> 임. 따라서 3을 <code>Q</code> 에서 삭제 후 5를 큐에 담음.(<code>Q = [4, 5]</code>)</li>
<li>4의 인접리스트는 1-&gt;3-&gt;6-&gt;7임. 1, 3은 visited&#x3D;<code>YES</code> 임. 따라서 4를 <code>Q</code>에서 삭제 후 6과 7을 <code>Q</code> 에 담음.(<code>Q = [5, 6, 7]</code>)</li>
<li>5의 인접리스트 모두 visited&#x3D;<code>YES</code> 이므로 5를 <code>Q</code> 에서 삭제.(<code>Q = [6, 7]</code>)</li>
<li>6의 인접리스트 4-&gt;7-&gt;8 중에 8만 visited&#x3D;<code>NO</code> 임으로 6을 <code>Q</code> 에서 삭제 후 8을 <code>Q</code> 에 담음.(<code>Q = [7, 8]</code>)</li>
<li>7과 8의 인접리스트들은 모두 visited&#x3D;<code>YES</code> 임으로 <code>Q</code> 에서 둘 다 삭제.</li>
</ol>
]]></content>
      <categories>
        <category>data structure</category>
      </categories>
      <tags>
        <tag>study</tag>
        <tag>data structure</tag>
      </tags>
  </entry>
  <entry>
    <title>자료구조(data structure) - Priority Queue, Heap</title>
    <url>/2024/01/08/dataStructure4/</url>
    <content><![CDATA[<h3 id="4-1-Priority-Queue-Heap"><a href="#4-1-Priority-Queue-Heap" class="headerlink" title="4.1 Priority Queue &gt; Heap"></a>4.1 Priority Queue &gt; Heap</h3><p><strong>4.1.1 우선순위 큐 개념</strong></p>
<p>Priority Queue 는 <a href="https://1dohyeon.github.io/2024/01/06/dataStructure2/">2. Stack AND Queue</a> 와 비슷한 축약 자료형이다. 하지만 이들과는 다르게 순서가 있다기 보다는 각 데이터에 우선순위를 부여하는 것이다.</p>
<span id="more"></span>

<p>우선순위 큐는 완전이진트리의 형태로 구성된다. 부모의 우선순위가 자식의 우선순위보다 높은 자료구조이다. 따라서 노드의 key값이 작다면(순위의 숫자가 작을수록 높은 등급) 위로 보낸다.</p>
<p>자식들은 부모보다 key값이 크며 자식들끼리는 따로 비교하여 작은 자식을 왼쪽으로 옮기거나 하지는 않는다.</p>
<p><strong>4.1.2 Heap 개념</strong></p>
<p><strong>우선순위 큐 &#x3D; 힙</strong> 은 절대 아니다. 힙이란 우선순위 큐 자료구조를 구현하기 위해서 고안된 알고리즘이다.</p>
<p>힙도 두가지 종류가 있다 :</p>
<ul>
<li>최소 힙(Minimum Heap) : 키 값이 작을 수록 높은 우선 순위를 가지며 가장 작은 키가 루트에 저장된다.</li>
<li>최대 힙(Maximum Heap) : 키 값이 클 수록 더 높은 우선 순위를 가진다.</li>
</ul>
<p>여기서는 최소 힙을 공부하였다. 우선 순위가 높은(key 값이 가장 작은) 값을 빠르게 접근하기 위해서는 그 key값을 가진 노드를 트리의 root로 옮기는 것이다. 그렇다면 O(1) 시간에 우선 순위가 가장 높은 원소에 접근할 수 있다.</p>
<p><strong>4.1.3 최솟값 삭제(우선 순위 높은 값 삭제)</strong></p>
<p>우선 순위 큐는 앞서 언급했듯이 완전이진트리의 형태를 지닌다. 따라서 먼저 root(우선 순위가 가장 높은 노드) 를 삭제하고, 가장 마지막 노드를 root로 옮기면 된다. 이때 기존 root의 자식들과의 연결도 해준다.</p>
<p>하지만, 이렇게만 해놓으면 key값이 가장 낮은 값이 root에 있다는 보장을 할 수 없다. 따라서 downheap 이라는 연산을 해야한다.</p>
<p><strong>4.1.4 downheap</strong><br>완전이진트리의 형태라고 하였지만, 코드 설명은 <code>Key</code> 와 <code>Value</code> 로 구성된 <code>Entry</code> 배열 로 설명하겠다.</p>
<p>우선 설명하기 전에 배열의 구조부터 설명하겠다. 우선순위가 가장 높은 <code>root</code> 노드는 배열 <code>a</code> 의 1번 인덱스에 저장된다. <code>root</code> 의 왼쪽 자식은 2, 오른쪽은 3에 저장된다. 2번에 저장된 노드의 왼쪽 자식은 그 다음 순번인 4, 오른쪽 자식은 그 다음 순번인 5에 채워진다. 완전이진트리의 형태이기 때문에 중간이 빌 수가 없다. 따라서 아래와 같은 형태를 갖게 된다 :</p>
<p><img src="https://github.com/1Dohyeon/blogIMG/blob/master/nest1-datastructure-algorithms/%EC%9A%B0%EC%84%A0%EC%88%9C%EC%9C%84%ED%81%90%EC%98%88%EC%8B%9C%EC%9D%B4%EB%AF%B8%EC%A7%80.png?raw=true" alt="우선순위큐예시이미지.png"></p>
<p>아래는 downheap 을 구현한 자바 코드이다 :</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">BHeap</span>&lt;K <span class="keyword">extends</span> <span class="title class_">Comparable</span>&lt;K&gt;, V&gt; &#123; <span class="comment">// 이진(최소) 힙</span></span><br><span class="line">    <span class="keyword">private</span> Entry&lt;K, V&gt;[] a; <span class="comment">// a[0]은 사용 안함</span></span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> N; <span class="comment">// 힙의 크기</span></span><br><span class="line"></span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">downheap</span><span class="params">(<span class="type">int</span> i)</span> &#123;</span><br><span class="line">        <span class="keyword">while</span> (<span class="number">2</span> * i &lt;= N) &#123;</span><br><span class="line">            <span class="type">int</span> <span class="variable">k</span> <span class="operator">=</span> <span class="number">2</span> * i; <span class="comment">// i의 왼쪽 자식을 k</span></span><br><span class="line">            <span class="comment">// 오른쪽 자식이 있으면 오른쪽 자식과 비교</span></span><br><span class="line">            <span class="keyword">if</span> (k &lt; N &amp;&amp; greater(k, k + <span class="number">1</span>)) &#123;</span><br><span class="line">                k++;</span><br><span class="line">            &#125; <span class="comment">// 더 작은 값을 k로 설정</span></span><br><span class="line">            <span class="keyword">if</span> (!greater(i, k)) &#123;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            swap(i, k); <span class="comment">// i와 k 위치를 바꿈</span></span><br><span class="line">            i = k;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>downheap은 루트부터 시작한다. 즉, <code>a[1]</code> 부터 시작하여 왼쪽 오른쪽 자식의 크기를 비교한다. 위 그림에서는 <code>a[2]=15 &lt; a[3]=20</code> 의 값을 가지므로 더 작은 <code>a[2]</code> 가 승자가 되어 부모노드(root)와 위치를 바꾼다.</p>
<p>그 후 <code>i = k</code> 로 업데이트한다. 즉 바꾸는 기준이 된 root 노드를 계속 따라가서 비교하겠다는 의미이다. 위 과정을 반복하여 자신이 자식들보다 값이 작은 경우에는 <code>break</code> 하여 downheap 수행을 멈춘다.</p>
<p>전체 코드 -&gt; <a href="https://github.com/1Dohyeon/Study-DataStructure/blob/master/04_PriorityQ_with_java/D1_PriorityQ/BHeap.java">Priority Queue</a></p>
]]></content>
      <categories>
        <category>data structure</category>
      </categories>
      <tags>
        <tag>study</tag>
        <tag>data structure</tag>
      </tags>
  </entry>
  <entry>
    <title>NestJS-DTO</title>
    <url>/2024/01/12/nest11-dto/</url>
    <content><![CDATA[<h3 id="13-1-DTO-정의"><a href="#13-1-DTO-정의" class="headerlink" title="13.1 DTO 정의"></a>13.1 DTO 정의</h3><p>Schema 를 통해서 모듈이 가지는 데이터를 정의했다면 DTO 는 서비스 간의 데이터 전송 및 <strong>특정 작업을 위한 데이터 객체</strong>로 사용된다. 따라서 특정 작업을 위해 필요한 데이터만을 포함하기에 Schema 처럼 모든 데이터를 가지지 않는다.</p>
<span id="more"></span>

<p>따라서 DTO는 요청을 처리하기 위한 RequestDTO, 응답을 처리하기 위한 ResponseDTO 등으로 특정 잡업에 따라 나뉠 수도 있다.</p>
<h3 id="13-2-DTO-예제"><a href="#13-2-DTO-예제" class="headerlink" title="13.2 DTO 예제"></a>13.2 DTO 예제</h3><figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">IsEmail</span>, <span class="title class_">IsNotEmpty</span>, <span class="title class_">IsString</span> &#125; <span class="keyword">from</span> <span class="string">&quot;class-validator&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">CatRequestDto</span> &#123;</span><br><span class="line">  <span class="meta">@IsEmail</span>()</span><br><span class="line">  <span class="meta">@IsNotEmpty</span>()</span><br><span class="line">  <span class="attr">email</span>: <span class="built_in">string</span>;</span><br><span class="line"></span><br><span class="line">  <span class="meta">@IsString</span>()</span><br><span class="line">  <span class="meta">@IsNotEmpty</span>()</span><br><span class="line">  <span class="attr">name</span>: <span class="built_in">string</span>;</span><br><span class="line"></span><br><span class="line">  <span class="meta">@IsString</span>()</span><br><span class="line">  <span class="meta">@IsNotEmpty</span>()</span><br><span class="line">  <span class="attr">password</span>: <span class="built_in">string</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>위 코드를 보면 알 수 있듯이 로그인을 할 때는 email, name, password만 필요하므로 dto에는 딱 필요한 데이터만 포함한다.</p>
<p>그렇다면 이 DTO 를 어떻게 활용하는 것일까?</p>
<p>DTO 는 <strong>특정 작업</strong>을 위해서 필요한 데이터를 포함한다. 그리고 <strong>1.3.1 Service 정의</strong> 에서 설명했듯이 Service 는 실질적인 비즈니스 로직을 작성하는 것이다.</p>
<p>따라서 <strong>특정 작업</strong>은 Service로 부터의 작업이라고 유추할 수 있고, Service에서 DTO가 직접적으로 사용됨을 알 수 있다.</p>
<p><strong>만약 여러 기능에 의해서 DTO를 여러개 만들어야 할 때는 어떻게 할까?</strong></p>
<p>질문과 같은 상황이 되면 여러 DTO를 만들게 되면서 반복작업을 해야할 수도 있다. 하지만 이는 <code>PickType</code> 을 이용하여 처리할 수도 있다. 이는 다음에 JWT 를 공부하면서 다루겠다.</p>
<h3 id="13-3-계층간-DTO-이동"><a href="#13-3-계층간-DTO-이동" class="headerlink" title="13.3 계층간 DTO 이동"></a>13.3 계층간 DTO 이동</h3><p><img src="https://github.com/1Dohyeon/blogIMG/blob/master/nest1-datastructure-algorithms/DTO%EA%B3%84%EC%B8%B5%EC%9D%B4%EB%8F%99IMG.png?raw=true" alt="DTO계층이동IMG.png"></p>
<p>위 이미지 예시를 통해서 설명하자면 클라이언트가 body를 통해 data를 전달한다면, DTO 객체로 만들고 안전하게 컨트롤로 보낸다. 또 컨트롤러는 이 DTO를 서비스 로직에 넘기고, 서비스는 이 DTO를 DB로 전달한다.</p>
<p>위와 같은 형태로 data와 controller, service 가 상호작용을 하게된다. 하지만 위 과정이 완벽한 과정은 아니고 요약한 형태이다.</p>
<p>이 사이에 <strong>middleware</strong> 가 있고, <strong>middleware</strong> 양쪽을 연결하여 데이터를 주고 받을 수 있도록 매개역할을 한다.</p>
<h3 id="13-4-Service-회원가입-서비스"><a href="#13-4-Service-회원가입-서비스" class="headerlink" title="13.4 Service : 회원가입 서비스"></a>13.4 Service : 회원가입 서비스</h3><figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Injectable</span>, <span class="title class_">UnauthorizedException</span> &#125; <span class="keyword">from</span> <span class="string">&quot;@nestjs/common&quot;</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">InjectModel</span> &#125; <span class="keyword">from</span> <span class="string">&quot;@nestjs/mongoose&quot;</span>;</span><br><span class="line"><span class="keyword">import</span> * <span class="keyword">as</span> bcrypt <span class="keyword">from</span> <span class="string">&quot;bcrypt&quot;</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Model</span> &#125; <span class="keyword">from</span> <span class="string">&quot;mongoose&quot;</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Cat</span> &#125; <span class="keyword">from</span> <span class="string">&quot;./cats.schema&quot;</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">CatRequestDto</span> &#125; <span class="keyword">from</span> <span class="string">&quot;./dto/cats.request.dto&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Injectable</span>()</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">CatsService</span> &#123;</span><br><span class="line">  <span class="title function_">constructor</span>(<span class="params"><span class="meta">@InjectModel</span>(Cat.name) <span class="keyword">private</span> <span class="keyword">readonly</span> catModel: Model&lt;Cat&gt;</span>) &#123;&#125;</span><br><span class="line">  <span class="keyword">async</span> <span class="title function_">signUp</span>(<span class="params">body: CatRequestDto</span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> &#123; email, name, password &#125; = body;</span><br><span class="line">    <span class="keyword">const</span> isCatExist = <span class="keyword">await</span> <span class="variable language_">this</span>.<span class="property">catModel</span>.<span class="title function_">exists</span>(&#123; email &#125;);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (isCatExist) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">UnauthorizedException</span>(<span class="string">&quot;해당하는 고양이는 이미 존재합니다.&quot;</span>); <span class="comment">// = throw new HttpException(&#x27;해당하는 고양이는 이미 존재합니다.&#x27;, 403);</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> hashedPassword = <span class="keyword">await</span> bcrypt.<span class="title function_">hash</span>(password, <span class="number">10</span>);</span><br><span class="line">    <span class="keyword">const</span> cat = <span class="keyword">await</span> <span class="variable language_">this</span>.<span class="property">catModel</span>.<span class="title function_">create</span>(&#123;</span><br><span class="line">      email,</span><br><span class="line">      name,</span><br><span class="line">      <span class="attr">password</span>: hashedPassword,</span><br><span class="line">    &#125;);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> cat.<span class="property">readOnlyData</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>CatsService</code> 클래스에 로그인 기능인 <code>signUp</code> 메서드를 작성하였다. MongoDB와 함께 사용하기 위해서 <code>&#123; InjectModel &#125; from &#39;@nestjs/mongoose&#39;;</code> 를 import 하여 <code>@InjectModel</code> 를 사용하였다. 이 데코레이터는 <code>CatsService</code>에서 사용할 MongoDB 모델을 주입한다. <code>Cat.name</code>은 Mongoose 모델의 이름을 나타낸다.</p>
<p><code>signUp</code> 메서드는 앞에 <code>async</code> 을 작성함으로써 비동기 메서드로 작성하였다. 이때 파라미터로 받는 <code>body</code> 타입 <code>CatRequestDto</code> 는 회원가입에 필요한 데이터를 나타내는 DTO이다.</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> isCatExist = <span class="keyword">await</span> <span class="variable language_">this</span>.<span class="property">catModel</span>.<span class="title function_">exists</span>(&#123; email &#125;);</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (isCatExist) &#123;</span><br><span class="line">  <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">UnauthorizedException</span>(<span class="string">&quot;해당하는 고양이는 이미 존재합니다.&quot;</span>); <span class="comment">// = throw new HttpException(&#x27;해당하는 고양이는 이미 존재합니다.&#x27;, 403);</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>위 부분은 email 이 존재하는지 여부를 나타내며 존재한다면 에러 메시지를 반환한다.</p>
<p>password 는 <code>bcrypt</code> 를 통해서 해시화 하여 보안을 강화하고, <code>Model</code> 의 <code>create</code> 메서드를 이용하여 데이터를 생성하고 데이터베이스에 저장한다.</p>
<p>마지막으로 <code>return</code> 을 통해서 데이터를 반환하는데, 이때 2.1.2 예제 코드에서 적힌 <code>readOnlyData</code> 형태로 반환하여 password 를 걸러낸다.</p>
<blockquote>
<p>실습 코드 URL : <a href="https://github.com/1Dohyeon/Study-NestJS">https://github.com/1Dohyeon/Study-NestJS</a></p>
</blockquote>
]]></content>
      <categories>
        <category>NestJS</category>
      </categories>
      <tags>
        <tag>study</tag>
        <tag>NestJS</tag>
      </tags>
  </entry>
  <entry>
    <title>자료구조(data structure) - Binary Search Tree, Union, AVL 트리</title>
    <url>/2024/01/07/dataStructure3/</url>
    <content><![CDATA[<h3 id="3-1-Binary-Search-Tree"><a href="#3-1-Binary-Search-Tree" class="headerlink" title="3.1 Binary Search Tree"></a>3.1 Binary Search Tree</h3><p><strong>3.1.1 개념</strong></p>
<p>Binary Search Tree(이진 검색 트리) 는 <a href="https://github.com/1Dohyeon/Study-DataStructure/tree/master/01_Lists_with_py">1. DynamicArray AND LinkedList</a> 의 1.2.1 에서 설명한 노드를 <code>next</code>가 아닌 <code>left</code>, <code>right</code> <strong>두가지</strong>로 노드들을 연결한 자료구조이다.</p>
<span id="more"></span>

<p>즉, 데이터를 이진 검색 트리에 삽입한다면 아래와 같은 모양의 자료구조를 갖는다.</p>
<p><img src="https://github.com/1Dohyeon/blogIMG/blob/master/nest1-datastructure-algorithms/BST%EC%98%88%EC%A0%9C%EC%9D%B4%EB%AF%B8%EC%A7%80.png?raw=true" alt="BST 예제 이미지.png"></p>
<p>또한 이진 검색 트리는 위 그림에서처럼 부모 노드보다 크다면 <code>right</code> , 부모노드보다 작으면 <code>left</code> 로 이동하여 자리를 잡는다.</p>
<p><strong>3.1.2 class Node</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Node</span>&lt;Key <span class="keyword">extends</span> <span class="title class_">Comparable</span>&lt;Key&gt;, Value&gt; &#123;</span><br><span class="line">    <span class="keyword">private</span> Key id;</span><br><span class="line">    <span class="keyword">private</span> Value name;</span><br><span class="line">    <span class="keyword">private</span> Node&lt;Key, Value&gt; left, right;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 생략</span></span><br></pre></td></tr></table></figure>

<ul>
<li><code>id</code> : 각 노드의 고유값으로 노드를 배치하기 위해서 사용된다.</li>
<li><code>name</code> : 각 노드에 담을 데이터</li>
<li><code>left</code> : 현재 노드의 왼쪽 자식 노드를 가리킨다.</li>
<li><code>right</code> : 현재 노드의 오른쪽 자식 노드를 가리킨다.</li>
</ul>
<p>전체 코드 -&gt; <a href="https://github.com/1Dohyeon/Study-DataStructure/blob/master/04_BinarySearchTree_with_java/D2_BS_Tree/Node.java">Node Code</a></p>
<p><strong>3.1.3 get 탐색</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> Value <span class="title function_">get</span><span class="params">(Node&lt;Key, Value&gt; n, Key k)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (n == <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">        &#125; <span class="comment">// k를 발견 못함</span></span><br><span class="line"></span><br><span class="line">        <span class="type">int</span> <span class="variable">t</span> <span class="operator">=</span> n.getKey().compareTo(k);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (t &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> get(n.getLeft(), k);</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (t &lt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> get(n.getRight(), k);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123; <span class="comment">// k를 찾았을 경우 t==0</span></span><br><span class="line">            <span class="keyword">return</span> (Value) n.getValue();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>위 코드는 <code>root</code> 노드(부모가 없는 노드(최상위 노드))부터 시작하여 <code>id</code>(key)를 비교한다. 찾으려는 노드의<code>id</code>가 탐색 중인 현재 노드보다 더 클경우 <code>right</code> , 작을 경우 <code>left</code>로 이동한다. 같을 경우는 그 노드의 <code>value</code>를 반환한다.</p>
<p><strong>3.1.4 put 트리에 노드 삽입</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 트리에 노드 (k, v)를 삽입</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">put</span><span class="params">(Key k, Value v)</span> &#123;</span><br><span class="line">    root = put(root, k, v);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// n를 루트로 하는 (서브)트리에 노드 (k, v)를 삽입</span></span><br><span class="line"><span class="keyword">public</span> Node&lt;Key, Value&gt; <span class="title function_">put</span><span class="params">(Node&lt;Key, Value&gt; n, Key k, Value v)</span> &#123;</span><br><span class="line">    <span class="comment">// root부터 탐색을 시작해야 하므로 n 파라미터 자리에 처음에는 n이 삽입된다.</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (n == <span class="literal">null</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Node</span>&lt;Key, Value&gt;(k, v);</span><br><span class="line">    &#125; <span class="comment">// 비었을 경우 새로운 노드를 연결시킴</span></span><br><span class="line">    <span class="type">int</span> <span class="variable">t</span> <span class="operator">=</span> n.getKey().compareTo(k);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (t &gt; <span class="number">0</span>) &#123; <span class="comment">// n의 key가 새로운 노드의 key보다 클 경우 왼쪽으로</span></span><br><span class="line">        n.setLeft(put(n.getLeft(), k, v));</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (t &lt; <span class="number">0</span>) &#123; <span class="comment">// n의 key가 새로운 노드의 key보다 작을 경우 오른쪽으로</span></span><br><span class="line">        n.setRight(put(n.getRight(), k, v));</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123; <span class="comment">// 같을 경우는 key는 냅두고 value를 v로 대체한다.</span></span><br><span class="line">        n.setValue(v);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 마지막에 return n;을 해줌으로써, 삽입된 노드의 부모 노드에 대한 포인터를 반환하여 거슬러 올라가며 재 연결</span></span><br><span class="line">    <span class="keyword">return</span> n;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>3.1.1 에서 설명했듯이 <code>root</code> 노드부터 내려가며 <code>k</code>(key) 를 비교하여 작으면 <code>left</code>, 크면 <code>right</code>로 이동하여 빈자리가 나오면 노드를 삽입한다.</p>
<p><strong>3.1.5 delete</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">delete</span><span class="params">(Key k)</span> &#123;</span><br><span class="line">        root = delete(root, k);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> Node&lt;Key, Value&gt; <span class="title function_">delete</span><span class="params">(Node&lt;Key, Value&gt; n, Key k)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (n == <span class="literal">null</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> <span class="variable">t</span> <span class="operator">=</span> n.getKey().compareTo(k);</span><br><span class="line">    <span class="keyword">if</span> (t &gt; <span class="number">0</span>) &#123; <span class="comment">// if (k &lt; 노드 n의 id) 왼쪽 자식으로 이동</span></span><br><span class="line">        n.setLeft(delete(n.getLeft(), k));</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (t &lt; <span class="number">0</span>) &#123; <span class="comment">// if (k &gt; 노드 n의 id) 오른쪽 자식으로 이동</span></span><br><span class="line">        n.setRight(delete(n.getRight(), k));</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123; <span class="comment">// 삭제할 노드를 발견했을 경우</span></span><br><span class="line">        <span class="keyword">if</span> (n.getRight() == <span class="literal">null</span>) &#123; <span class="comment">// 오른쪽이 없을 경우는 왼쪽, 둘 다 없는 경우에도 어차피 null을 반환하는 것이므로 양쪽 다 null인 경우도 이 조건문이 해결</span></span><br><span class="line">            <span class="keyword">return</span> n.getLeft();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (n.getLeft() == <span class="literal">null</span>) &#123; <span class="comment">// 왼쪽이 없을 경우는 오른쪽을 반환. 반환 후 deleteMin처럼 수행</span></span><br><span class="line">            <span class="keyword">return</span> n.getRight();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        Node&lt;Key, Value&gt; target = n;</span><br><span class="line">        <span class="comment">// 삭제할 노드 자리로 옮겨올 노드 찾아서 n이 가리키게 함</span></span><br><span class="line">        n = min(target.getRight());</span><br><span class="line">        n.setRight(deleteMin(target.getRight()));</span><br><span class="line">        n.setLeft(target.getLeft());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> n; <span class="comment">// 부모 노드의 포인터를 갱신해야 하기 때문에 n을 반환</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>트리에서 노드를 삭제할 경우 세가지 경우를 생각해야한다. <strong>첫번째 경우</strong>는 자식 노드가 없는 경우는 그냥 그 노드를 삭제하면 된다.</p>
<p><strong>두번째 경우</strong>는 왼쪽이든 오른쪽이든 자식이 하나만 있는 노드이다. 하나만 있을 경우는 삭제하려는 노드<code>p</code> 라고 했을때, <code>p</code> 의 자식은 <code>q</code> , <code>p</code> 의 부모는 <code>x</code> 라고 가정한다.</p>
<p><code>p</code> 를 삭제하고 <code>q</code> 를 <code>x</code> 의 자식으로 붙이면 된다. <code>p</code> &gt; <code>x</code> 라면 <code>q</code> 는 이진 검색 트리의 특성상 <code>q</code> &gt; <code>x</code> 가 성립하기 때문이다.</p>
<p><strong>세번째 경우</strong>는 삭제하려는 노드 <code>p</code>가 왼쪽, 오른쪽 자식을 전부 갖고 있을 때이다. 다음과 같이 노드를 정의한다:</p>
<p><code>p</code> : 삭제하려는 노드<br><code>s</code> : <code>p</code> 의 부모 노드<br><code>x</code> : <code>p</code> 의 왼쪽 자식 노드<br><code>y</code> : <code>p</code> 의 오른쪽 자식 노드</p>
<p>위와 같이 정의할 경우 각 노드의 키값은 다음과 같이 정의된다 : <code>x</code> &lt; <code>p</code> &lt; <code>y</code>. 이때 <code>y</code>의 최솟값 노드의 키값 <code>y_min</code> 은 <code>x</code> 의 키값보다 크다.</p>
<p>즉, <code>p</code>의 키값보다 바로 다음단계로 큰 노드의 키값은 <code>y_min</code>이란 소리이다. 따라서 (아래 그림처럼) <code>y_min</code> 을 <code>p</code> 의 위치로 옮기고 <code>y_min</code> 의 <code>left</code> , <code>right</code> 를 기존 <code>p</code> 의 <code>left</code> , <code>right</code> 와 연결시키면 된다.</p>
<p><img src="https://github.com/1Dohyeon/blogIMG/blob/master/nest1-datastructure-algorithms/%EC%9D%B4%EC%A7%84%EA%B2%80%EC%83%89%ED%8A%B8%EB%A6%AC%EC%82%AD%EC%A0%9C%EC%98%88%EC%A0%9C.png?raw=true" alt="이진검색트리 삭제 예제.png"></p>
<ul>
<li>기존 <code>y_min</code> 은 삭제해야한다.</li>
</ul>
<p>전체 코드 -&gt; <a href="https://github.com/1Dohyeon/Study-DataStructure/blob/master/04_BinarySearchTree_with_java/D2_BS_Tree/BST.java">BST Code</a></p>
<h3 id="3-2-Union"><a href="#3-2-Union" class="headerlink" title="3.2 Union"></a>3.2 Union</h3><p><strong>3.2.1 개념</strong></p>
<p>상호배타적 집합들의 트리 표현으로 배열에서 모든 집합의 “원소를 인덱스로 사용” 한다. 그리고 그 배열의 value값은 부모 노드를 가리킨다.</p>
<p>union 메소드를 통해서 나눠져 있는 트리를 통합하고, find메소드를 통해서 경로압축(Path Compression)을 한다.</p>
<p><strong>3.2.2 union</strong></p>
<p>높이가 상대적으로 낮은 트리의 루트노드가 높이가 상대적으로 높은 트리의 루트노드의 자식이 되도록 통합한다.</p>
<p>즉, rank(높이)가 높은 루트가 union 후에도 승자(합쳐진 트리의 루트)가 되도록 한다.</p>
<p><strong>3.2.3 find</strong></p>
<p>find(n) 메소드를 실행하면 n index에서 부모노드를 따라 올라가 그 <strong>index들의 원소를 전부 트리의 루트노드로 지정</strong>하여 경로압축(Path Compression)을 한다.</p>
<p>전체 코드 -&gt; <a href="https://github.com/1Dohyeon/Study-DataStructure/blob/master/03_Tree_with_java/D2_Union/UnionFind.java">Union AND Find Code</a></p>
<hr>
<h3 id="3-3-AVL-트리"><a href="#3-3-AVL-트리" class="headerlink" title="3.3 AVL 트리"></a>3.3 AVL 트리</h3><p><strong>3.3.1 개념</strong></p>
<p>3.1 에서의 이진탐색트리에 15, 13, 11, 9, 7 과 같이 계속 작은 수를 삽입하게 되면 왼쪽으로 치우쳐진 형태의 트리 모습을 갖게 된다. 큰수를 계속 삽입한다면 반대로 오른쪽으로 치우쳐지게 될 것이다.</p>
<p>이러한 문제점을 해결하기 위해서 한쪽으로 치우쳐지면 트리를 회전시키는 것이 AVL 알고리즘이다. 이런 알고리즘을 지닌 트리를 AVL 트리 자료구조라고 한다.</p>
<p><img src="https://github.com/1Dohyeon/blogIMG/blob/master/nest1-datastructure-algorithms/LLcase.png?raw=true" alt="LLcase.png"></p>
<p>15 -&gt; 10 -&gt; 7 순으로 채워져서 트리가 왼쪽으로 치우쳐진 모양이라고 가정해보자. 이때 루트 노드이 왼쪽 자식인 10, 즉 중간 노드를 기준으로 오른쪽으로 회전시킨다. 우회전을 했다고 하여 Right Rotation 이라고 부른다.</p>
<p>하지만 이런 케이스는 왼쪽 자식의 왼쪽 자식 때문에 회전이 일어났으므로 Left Left Case, 즉 <strong>LL case</strong> 라고 부른다. 반대로 오른쪽으로 치우쳐져 있다면 위와 반대로 생각하면 쉽다.</p>
<p><img src="https://github.com/1Dohyeon/blogIMG/blob/master/nest1-datastructure-algorithms/LRcase.png?raw=true" alt="LRcase.png"></p>
<p>이번에는 15 -&gt; 10 -&gt; 12 순서로 들어왔다고 가정하자. 이때 왼쪽 자식의 오른쪽 자식에 의해서 트리 균형이 맞지 않음을 알 수 있다. 따라서 Left Right case, 줄여서 LR case 라고 한다. 오른쪽으로 치우쳐진 것을 회복하기 위해 왼쪽으로 먼저 회전시키고, 오른쪽으로 다시 회전시켜 균형을 맞춘다.</p>
<p><strong>3.3.2 Right Rotation</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> Node&lt;Key, Value&gt; <span class="title function_">rotateRight</span><span class="params">(Node&lt;Key, Value&gt; n)</span> &#123;</span><br><span class="line">       Node&lt;Key, Value&gt; x = n.left;</span><br><span class="line">       n.left = x.right;</span><br><span class="line">       x.right = n;</span><br><span class="line"></span><br><span class="line">       <span class="comment">// 높이 갱신</span></span><br><span class="line">       n.height = tallerHeight(height(n.left), height(n.right)) + <span class="number">1</span>;</span><br><span class="line">       x.height = tallerHeight(height(x.left), height(x.right)) + <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">       <span class="keyword">return</span> x;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>

<p><img src="https://github.com/1Dohyeon/blogIMG/blob/master/nest1-datastructure-algorithms/LLcase%EC%BD%94%EB%93%9C%EB%8C%80%EB%A1%9C%EC%8B%A4%ED%96%89%EC%98%88%EC%A0%9C.png?raw=true" alt="LLcase 코드대로 실행 예제.png"></p>
<p>3.3.1 에서 예를 든 것처럼 15 -&gt; 10 -&gt; 7 순서로 숫자가 들어왔다고 가정. 이때 15가 n 노드가 된다. <strong>n 노드에서 좌우 불균형을 발견</strong>했기 때문이다. 다음은 코드대로 실행하면 된다.</p>
<p>n의 왼쪽 자식을 x라고 두고, n의 왼쪽 자식 위치로 x의 오른쪽 자식을 옮긴다. x의 오른쪽 자식은 없으므로 null. 그다음 x의 오른쪽 자식에 n을 위치시키면 마치 트리가 회전한 것처럼 보인다.</p>
<p>왼쪽으로 회전은 위와 반대로 작동한다. <strong>tallerHeight()</strong> 메서드는 n과 x의 위치가 변했기 때문에 높이를 갱신해준 것이다. 왼쪽 오른쪽 자식의 높이차이가 2이상 나면 회전을 해야하기 때문이다.</p>
<p>전체 코드 -&gt; <a href="https://github.com/1Dohyeon/Study-DataStructure/blob/master/03_Tree_with_java/D3_AVL/AVL.java">AVL</a></p>
]]></content>
      <categories>
        <category>data structure</category>
      </categories>
      <tags>
        <tag>study</tag>
        <tag>data structure</tag>
      </tags>
  </entry>
  <entry>
    <title>NestJS-Guard And JWT</title>
    <url>/2024/01/14/nest13-guardANDjwt/</url>
    <content><![CDATA[<h3 id="15-1-Guard"><a href="#15-1-Guard" class="headerlink" title="15.1 Guard"></a>15.1 Guard</h3><p><strong>Guard</strong> 는 클라이언트와 Route Handler 사이에 있으며 클라이언트가 보내는 요청이 안전한지 판단한다. 안전하지 않은 요청이라면 차단한다. 따라서 사용자 인증이나 접근 제어를 구현하는데 쓰인다.</p>
<span id="more"></span>

<p><img src="https://docs.nestjs.com/assets/Guards_1.png"></p>
<p>위 이미지를 보면 <strong>Guard</strong> 는 Middleware와 같은 자리에 있는 것처럼 보이지만, Middleware 가 실행된 후에 <strong>Guard</strong> 가 실행된다. Express의 Middleware와 비슷하지만 차이점도 존재한다.</p>
<p>Middleware 의 <code>next()</code> 메서드가 실행된 후 어떤 핸들러가 실행되는지 Middleware 는 알 수 없다. 반면에 <strong>Guard는 다음에 실행될 내용을 알 수 있다</strong>.</p>
<h3 id="15-2-JWT"><a href="#15-2-JWT" class="headerlink" title="15.2 JWT"></a>15.2 JWT</h3><p><strong>15.2.1 JWT 정의</strong></p>
<p><strong>JWT(Json Web Token)</strong> 란 Json 객체에 인증이 필요한 정보들을 담아서 토큰으로 저장하는 방식이다. Secret Key 를 통해서 사용된다.</p>
<p>그렇다면 JWT은 어떤 형태로 생겼을지 의문점이 든다. 바로 다음과 같이 생겼다 : <code>XXXX.YYYY.ZZZZ</code> 첫번째부터 <code>header</code>, <code>payload</code>, <code>signature</code> 순서로 정의된다.</p>
<p><code>header</code> 는 토큰의 타입과 알고리즘을 인코딩한 것이고, <code>payload</code> 는 사용자의 정보가 들어가는 곳이다. 따라서 진짜 필요한 정보만 넣는 것이 중요하다. <code>signature</code> 은 이 두개를 조합하고 <strong>secret key</strong> 로 인코딩한 것이다.</p>
<p><strong>15.2.2 JWT 사용</strong></p>
<p>지금까지 JWT의 정의에 대해서 공부하였고 이제는 이것이 어떻게 사용되는지 작성하겠다. 우선 로그인을 하는 상황이라고 가정하자 :<br><img src="https://github.com/1Dohyeon/blogIMG/blob/master/nest1-datastructure-algorithms/Login%EC%98%88%EC%8B%9C.png?raw=true" alt="Login예시.png"></p>
<p>프론트 에서 로그인을 위해서 email 과 password 정보를 백엔드로 보낸다. 이때 백엔드에서 이를 받고 secret key 를 통해서 JWT 를 만들고, 이것을 다시 프론트로 전달한다. 프론트를 이를 받고 안전한 공간에 저장한다. 이 JWT는 프론트가 서비스에 접근하기 위해서 로그인을 했다는 인증서라고 생각하면 된다. 이 JWT를 통해서 프론트에서는 서비스 기능을 사용할 수 있다.</p>
<p>프론트가 JWT를 통해 서비스를 사용하는 예제는 다음과 같다:<br><img src="https://github.com/1Dohyeon/blogIMG/blob/master/nest1-datastructure-algorithms/%EA%B8%80%EC%93%B0%EA%B8%B0%EC%98%88%EC%8B%9Cjwt.png?raw=true" alt="글쓰기예시jwt.png"></p>
<p>프론트는 글쓰기 API를 백엔드로 보낼때 헤더에 JWT를 담아서 같이 보내준다. 백엔드는 이를 받고, Guard를 통해서 Strategy를 실행해준다. 여기서 secret key를 통해서 디코딩을 하고, 해당하는 user를 request.user에 저장해준다. 이 정보를 통해서 글쓰기 API의 비즈니스 로직이 실행되고 저장된다.</p>
<p><strong>15.2.3 JWT Guard</strong></p>
<p>위 15.2.2에서 JWT Guard가 언급되었다. 15.1에서 설명한 Guard 와 같은 역할을 한다.</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Injectable</span> &#125; <span class="keyword">from</span> <span class="string">&quot;@nestjs/common&quot;</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">AuthGuard</span> &#125; <span class="keyword">from</span> <span class="string">&quot;@nestjs/passport&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 자동으로 strategy 실행</span></span><br><span class="line"><span class="meta">@Injectable</span>()</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">JwtAuthGuard</span> <span class="keyword">extends</span> <span class="title class_ inherited__">AuthGuard</span>(<span class="string">&quot;jwt&quot;</span>) &#123;&#125;</span><br></pre></td></tr></table></figure>

<p>위 예시 코드를 보면 Guard는 의존성 주입이 가능한 클래스임을 알 수 있다. 또한 이 Guard는 <code>AuthGuard</code> 를 상속받은 것을 알 수 있는데, 이는 자동으로 strategy 를 실행해준다.</p>
<p><strong>15.2.4 JWT Strategy</strong></p>
<p>JWT Strategy는 JWT를 인증할 때 사용되는 클래스이다.</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Injectable</span> &#125; <span class="keyword">from</span> <span class="string">&#x27;@nestjs/common&#x27;</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">PassportStrategy</span> &#125; <span class="keyword">from</span> <span class="string">&#x27;@nestjs/passport&#x27;</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">ExtractJwt</span>, <span class="title class_">Strategy</span> &#125; <span class="keyword">from</span> <span class="string">&#x27;passport-jwt&#x27;</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">CatsRepository</span> &#125; <span class="keyword">from</span> <span class="string">&#x27;src/cats/cats.repository&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 인증을 할 때 사용</span></span><br><span class="line"><span class="meta">@Injectable</span>()</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">JwtStrategy</span> <span class="keyword">extends</span> <span class="title class_ inherited__">PassportStrategy</span>(<span class="title class_">Strategy</span>) &#123;</span><br><span class="line">  <span class="title function_">constructor</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="variable language_">super</span>(&#123;</span><br><span class="line">      <span class="attr">jwtFromRequest</span>: <span class="title class_">ExtractJwt</span>.<span class="title function_">fromAuthHeaderAsBearerToken</span>(),</span><br><span class="line">      <span class="attr">secretOrKey</span>: <span class="string">&#x27;secret&#x27;</span>, <span class="comment">// env로 교체 예정</span></span><br><span class="line">      <span class="attr">ignoreExpiration</span>: <span class="literal">false</span>,</span><br><span class="line">    &#125;);</span><br><span class="line">    <span class="keyword">async</span> <span class="title function_">validate</span>(<span class="params">payload</span>) &#123;&#125;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><code>jwtFromRequest: ExtractJwt.fromAuthHeaderAsBearerToken()</code> : 헤더의 토큰으로부터 추출한다는 의미이다.</li>
<li><code>secretOrKey</code> : secretkey를 담는 곳으로 일단은 보기 편하게 <code>&#39;secret&#39;</code> 으로 지정하였다.</li>
<li><code>ignoreExpiration</code> : JWT는 만료기간이 있는데, 서비스에 따라서 만료기간을 다르게 줄 수 있다. 일단은 <code>false 로 지정</code></li>
<li><code>async validate(payload) &#123;&#125;</code> : JWT를 인증하는 부분</li>
</ul>
<h3 id="15-3-LogIn-과정"><a href="#15-3-LogIn-과정" class="headerlink" title="15.3 LogIn 과정"></a>15.3 LogIn 과정</h3><p>15.2.2 그림에서도 알 수 있지만 글로 자세히 과정을 적어보려 한다.</p>
<ol>
<li>클라이언트가 <code>/login</code> 엔드포인트에 로그인 정보인 <code>email</code>, <code>password</code> 를 전달하였다.</li>
<li><strong>cat.controller.ts</strong> 는 아래 코드를 통해서 jwtLogIn을 불러온다</li>
</ol>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line">  <span class="meta">@ApiOperation</span>(&#123; <span class="attr">summary</span>: <span class="string">&#x27;로그인&#x27;</span> &#125;)</span><br><span class="line">  <span class="meta">@Post</span>(<span class="string">&#x27;login&#x27;</span>)</span><br><span class="line">  <span class="title function_">logIn</span>(<span class="params"><span class="meta">@Body</span>() data: LoginRequestDto</span>) &#123;</span><br><span class="line">    <span class="comment">// cats가 아닌 auth service 의 jwtLogIn 메서드를 통해서 토큰을 받고 클라이언트에 전달</span></span><br><span class="line">    <span class="keyword">return</span> <span class="variable language_">this</span>.<span class="property">authService</span>.<span class="title function_">jwtLogIn</span>(data);</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<ol start="3">
<li><code>jwtLogIn</code> 메서드가 있는 곳은 바로 <strong>auth.service.ts</strong> 이다. 여기서는 jwt 인증을 위한 service를 포함한다. 이 메서드는 <code>email</code>과 <code>password</code>가 올바른지 확인하고 다음을 반환한다 :</li>
</ol>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">return</span> &#123;</span><br><span class="line">  <span class="attr">token</span>: <span class="variable language_">this</span>.<span class="property">jwtService</span>.<span class="title function_">sign</span>(payload, &#123; <span class="attr">secret</span>: process.<span class="property">env</span>.<span class="property">JWT_SECRET</span> &#125;),</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<ol start="4">
<li><strong>cat.controller.ts</strong> 는 이 토큰을 받고 클라이언트에게 전달하여 클라이언트는 이 JWT를 통해서 서비스를 실행할 수 있다.</li>
</ol>
<h3 id="15-4-secret-key를-통해서-로그인-후-인증"><a href="#15-4-secret-key를-통해서-로그인-후-인증" class="headerlink" title="15.4 secret key를 통해서 로그인 후 인증"></a>15.4 secret key를 통해서 로그인 후 인증</h3><p><strong>15.4.1 payload.ts</strong></p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">type</span> <span class="title class_">Payload</span> = &#123;</span><br><span class="line">  <span class="attr">email</span>: <span class="built_in">string</span>;</span><br><span class="line">  <span class="attr">sub</span>: <span class="built_in">string</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>데이터를 전송할 때 payload를 통해서 다양한 요소를 함께 보내서 데이터 전송의 효율과 안정성을 높일 수 있다. 따라서 보내고자 하는 데이터 자체인 payload 를 만들어준다.</p>
<p><strong>15.4.2 인증을 위한 메서드 생성</strong></p>
<p>cats.repository.ts</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line">  <span class="keyword">async</span> <span class="title function_">findCatByIdWithoutPassword</span>(<span class="attr">catId</span>: <span class="built_in">string</span>): <span class="title class_">Promise</span>&lt;<span class="title class_">Cat</span> | <span class="literal">null</span>&gt; &#123;</span><br><span class="line">    <span class="keyword">const</span> cat = <span class="keyword">await</span> <span class="variable language_">this</span>.<span class="property">catModel</span>.<span class="title function_">findById</span>(catId).<span class="title function_">select</span>(<span class="string">&#x27;-password&#x27;</span>);</span><br><span class="line">    <span class="keyword">return</span> cat;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p><code>findCatByIdWithoutPassword()</code> 메서드는 cat의 id를 매개변수로 받아서 그 아이디에 맞는 cat 객체를 패스워드 없이 반환한다. 이떄 <code>catModel</code> 에서 제공되는 메서드를 이용한다. <code>select</code> 메서드를 통해서 원하는 필드를 제외할 수 있다.</p>
<p>jwt.strategy.ts</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line">  <span class="keyword">async</span> <span class="title function_">validate</span>(<span class="params">payload: Payload</span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> cat = <span class="keyword">await</span> <span class="variable language_">this</span>.<span class="property">catsRepository</span>.<span class="title function_">findCatByIdWithoutPassword</span>(</span><br><span class="line">      payload.<span class="property">sub</span>,</span><br><span class="line">    );</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (cat) &#123;</span><br><span class="line">      <span class="keyword">return</span> cat; <span class="comment">// request.user</span></span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">UnauthorizedException</span>(<span class="string">&#x27;접근 오류&#x27;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>strategy 파일에서 <code>validate()</code> 메서드를 통해서 cat 객체를 반환한다. 이때 <code>cats.repository.ts</code>에서 만든 <code>findCatByIdWithoutPassword</code> 메서드를 이용한다. cat을 찾을 수 없다면 에러를 발생시킨다.</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="meta">@ApiOperation</span>(&#123; <span class="attr">summary</span>: <span class="string">&#x27;현재 고양이 가져오기&#x27;</span> &#125;)</span><br><span class="line">  <span class="meta">@UseGuards</span>(<span class="title class_">JwtAuthGuard</span>)</span><br><span class="line">  <span class="meta">@Get</span>()</span><br><span class="line">  <span class="title function_">getCurrentCat</span>(<span class="params"><span class="meta">@Req</span>() req: Request</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> req.<span class="property">user</span>;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p><code>getCurrentCat()</code> 메서드를 통해서 로그인된 정보를 가져오는 기능을 가져올 수 있도록 컨트롤러에 가드를 연결시킨다.</p>
<p><code>@Get</code>을 통해 <code>getCurrentCat</code> 가 호출되면 <code>@UseGuards(JwtAuthGuard)</code> 를 통해서 가드가 실행되고, 이 가드가 <code>jwt.strategy.ts</code>를 실행시켜서 인증절차를 밟게 한다.</p>
<p><code>jwt.strategy.ts</code>의 <code>validate</code>가 실행되며 이때 payload는 라이브러리에서 자동으로 불러온다. 이 메서드를 통해 <code>cats.repository.ts</code>의 <code>findCatByIdWithoutPassword</code> 메서드가 실행된다. 따라서 원하는 결과를 반환받을 수 있게된다. 만약 jwt가 일치하지 않는다면 에러를 발생시킨다.</p>
]]></content>
      <categories>
        <category>NestJS</category>
      </categories>
      <tags>
        <tag>study</tag>
        <tag>NestJS</tag>
      </tags>
  </entry>
  <entry>
    <title>NestJS-Custom Decorators</title>
    <url>/2024/01/22/nest14-customDeco/</url>
    <content><![CDATA[<h3 id="16-1-커스텀-데코레이터-정의와-예시"><a href="#16-1-커스텀-데코레이터-정의와-예시" class="headerlink" title="16.1 커스텀 데코레이터 정의와 예시"></a>16.1 커스텀 데코레이터 정의와 예시</h3><p>커스텀 데코레이터란 말 그대로 내 맘대로 데코레이터를 정의하는 것이다. 데코레이터는 클래스, 메서드 또는 속성에 대해서 정의할 수 있다. <code>@</code> 를 통해 이용하며 아래 예시처럼 생성하여 사용할 수 있다.</p>
<span id="more"></span>

<p>user.decorator.ts</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; createParamDecorator, <span class="title class_">ExecutionContext</span> &#125; <span class="keyword">from</span> <span class="string">&quot;@nestjs/common&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">const</span> <span class="title class_">CurrentUser</span> = <span class="title function_">createParamDecorator</span>(</span><br><span class="line">  <span class="function">(<span class="params">data: <span class="built_in">unknown</span>, ctx: ExecutionContext</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">const</span> request = ctx.<span class="title function_">switchToHttp</span>().<span class="title function_">getRequest</span>();</span><br><span class="line">    <span class="keyword">return</span> request.<span class="property">user</span>;</span><br><span class="line">  &#125;</span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<p>request의 user 정보를 그대로 반환하는 함수를 데코레이터로 이용하기 위해서 위와 같이 정의하였다. 위 구조는 <a href="https://docs.nestjs.com/custom-decorators">NestJS 공식문서</a>에서 확인 가능하다.</p>
<p>이제 이 함수를 데코레이터로 <a href="https://1dohyeon.github.io/2024/01/14/nest13-guardANDjwt/#more">Guard, JWT</a>에서 다룬 예제에서 활용하자면 컨트롤러를 다음과 같이 수정해야한다 :</p>
<p>cats.controller.ts</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line">  <span class="meta">@ApiOperation</span>(&#123; <span class="attr">summary</span>: <span class="string">&#x27;현재 고양이 가져오기&#x27;</span> &#125;)</span><br><span class="line">  <span class="meta">@UseGuards</span>(<span class="title class_">JwtAuthGuard</span>)</span><br><span class="line">  <span class="meta">@Get</span>()</span><br><span class="line">  <span class="title function_">getCurrentCat</span>(<span class="params"><span class="meta">@CurrentUser</span>() cat</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> cat.<span class="property">readOnlyData</span>;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>기존에는 <code>@Req</code> 데코레이터를 통해서 express의 <code>Request</code> 를 이용하여 user 정보를 가져왔지만, 이미 <code>@CurrentUser()</code> 데코레이터에서 똑같은 방식으로 <code>request.user</code> 정보를 가져오기 때문에 <code>cat</code> 자체만 반환하면 된다.</p>
<p>또한 딱 필요한 정보만 가져오기 위해 <a href="https://1dohyeon.github.io/2024/01/12/nest10-schema/#more">Schema</a>만든 읽기 전용 데이터 형태(&#x3D; <code>readOnlyData</code>)로 불러온다.</p>
]]></content>
      <categories>
        <category>NestJS</category>
      </categories>
      <tags>
        <tag>study</tag>
        <tag>NestJS</tag>
      </tags>
  </entry>
  <entry>
    <title>NestJS-Image Uproad</title>
    <url>/2024/01/23/nest15-Image-Upload/</url>
    <content><![CDATA[<h3 id="17-1-multer"><a href="#17-1-multer" class="headerlink" title="17.1 multer"></a>17.1 multer</h3><p>multer 미들웨어 패키지는 http 요청을 통해서 파일을 업로드하는데 사용되는 형식으로 게시된 데이터를 처리한다.</p>
<p>아래 명령어를 이용하여 multer를 설치할 수 있다.</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">npm i -D @types/multer</span><br></pre></td></tr></table></figure>

<span id="more"></span>

<h3 id="17-2-파일-업로드"><a href="#17-2-파일-업로드" class="headerlink" title="17.2 파일 업로드"></a>17.2 파일 업로드</h3><p><strong>17.2.1 파일 코드 설명</strong></p>
<p>설치를 완료하면 컨트롤러를 아래와 같이 작성하면 연결이 된 것이다 :<br>cats.controller.ts</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line">  <span class="meta">@ApiOperation</span>(&#123; <span class="attr">summary</span>: <span class="string">&#x27;고양이 이미지 업로드&#x27;</span> &#125;)</span><br><span class="line">  <span class="meta">@UseInterceptors</span>(<span class="title class_">FileInterceptor</span>(<span class="string">&#x27;image&#x27;</span>))</span><br><span class="line">  <span class="meta">@Post</span>(<span class="string">&#x27;upload&#x27;</span>)</span><br><span class="line">  <span class="title function_">uploadCatImg</span>(<span class="params"><span class="meta">@UploadedFiles</span>() files: <span class="built_in">Array</span>&lt;Express.Multer.File&gt;</span>) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(files);</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&#x27;uploadImg&#x27;</span>;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><code>@UploadedFiles()</code> 데코레이터를 사용하여 업로드된 파일들을 <code>files</code> 매개변수로 받는다. 이 매개변수는 <code>Express.Multer.File</code> 타입의 배열이다.</li>
<li>함수는 업로드된 파일의 첫 번째 항목의 파일 이름을 사용하여 완전한 이미지 URL을 반환하고, 이 URL은 로컬 서버의 <code>http://localhost:8000/media/cats/</code> 경로에 업로드된 파일 이름을 추가하여 구성된다.</li>
</ul>
<p>또한 모듈에도 import를 해준다 :<br>cats.module.ts</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line">    <span class="title class_">MulterModule</span>.<span class="title function_">register</span>(&#123;</span><br><span class="line">      <span class="attr">dest</span>: <span class="string">&#x27;./upload&#x27;</span>,</span><br><span class="line">    &#125;),</span><br></pre></td></tr></table></figure>

<p>업로드할 파일을 저장할 수 있도록 <code>common/utils</code> 위치에 <code>multer.option.ts</code> 라는 파일을 만들었다. 이 파일의 코드는 <a href="https://github.com/1Dohyeon/Study-NestJS/blob/main/04_project1/src/common/utils/multer.options.ts">깃허브</a>에서 확인 가능하다.</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> storage = (<span class="attr">folder</span>: <span class="built_in">string</span>): multer.<span class="property">StorageEngine</span> =&gt; &#123;</span><br><span class="line">  <span class="title function_">createFolder</span>(folder);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> multer.<span class="title function_">diskStorage</span>(&#123;</span><br><span class="line">    <span class="title function_">destination</span>(<span class="params">req, file, cb</span>) &#123;</span><br><span class="line">      <span class="comment">//* 어디에 저장할 지</span></span><br><span class="line">      <span class="keyword">const</span> folderName = path.<span class="title function_">join</span>(__dirname, <span class="string">&quot;..&quot;</span>, <span class="string">`uploads/<span class="subst">$&#123;folder&#125;</span>`</span>);</span><br><span class="line">      <span class="title function_">cb</span>(<span class="literal">null</span>, folderName);</span><br><span class="line">    &#125;,</span><br><span class="line"></span><br><span class="line">    <span class="title function_">filename</span>(<span class="params">req, file, cb</span>) &#123;</span><br><span class="line">      <span class="comment">//* 어떤 이름으로 올릴 지</span></span><br><span class="line">      <span class="keyword">const</span> ext = path.<span class="title function_">extname</span>(file.<span class="property">originalname</span>);</span><br><span class="line">      <span class="keyword">const</span> fileName = <span class="string">`<span class="subst">$&#123;path.basename(</span></span></span><br><span class="line"><span class="subst"><span class="string">        file.originalname,</span></span></span><br><span class="line"><span class="subst"><span class="string">        ext</span></span></span><br><span class="line"><span class="subst"><span class="string">      )&#125;</span><span class="subst">$&#123;<span class="built_in">Date</span>.now()&#125;</span><span class="subst">$&#123;ext&#125;</span>`</span>;</span><br><span class="line">      <span class="title function_">cb</span>(<span class="literal">null</span>, fileName);</span><br><span class="line">    &#125;,</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>위 코드는 <code>multer.option.ts</code> 의 일부이다. <code>storage</code> 함수는 <code>folder</code>라는 매개변수를 받아, <code>multer</code>의 <code>StorageEngine</code>을 설정한다. 이 <code>StorageEngine</code>은 파일 저장 위치와 이름을 정의하는 데 사용된다.</p>
<p><code>createFolder(folder)</code>는 주어진 폴더가 존재하지 않을 경우 생성하는 로직이다. <code>fs.mkdirSync(path.join( ... ))</code> 을 이용한다.</p>
<p><code>multer.diskStorage</code>함수는 <code>multer</code>의 디스크 저장 엔진을 구성한다. 이 설정에는 두 부분이 있다.</p>
<ul>
<li><code>destination</code> :<ul>
<li><code>req</code>, <code>file</code>, <code>cb</code> (콜백)를 매개변수로 받는다.</li>
<li>파일이 저장될 경로를 설정한다. 여기서는 <code>__dirname</code>, <code>..</code>, <code>uploads/$&#123;folder&#125;</code>를 조합하여 경로를 생성하였다. 이때 <code>__dirname</code>은 현재 실행 중인 스크립트가 위치한 디렉토리를 나타낸다.</li>
<li><code>cb</code> 콜백 함수를 사용하여 에러가 없을 경우(<code>null</code>), 계산된 폴더 이름을 반환한다.</li>
</ul>
</li>
<li><code>filename</code> :<ul>
<li>이 함수 역시 <code>req</code>, <code>file</code>, <code>cb</code>를 매개변수로 받는다.</li>
<li>파일의 확장자(<code>ext</code>)를 추출한다.</li>
<li>파일 이름을 원본 파일 이름의 기본 이름(<code>basename</code>), 현재 시간(<code>Date.now()</code>), 그리고 파일 확장자를 결합하여 설정한다. 이는 각 파일에 고유한 이름을 부여하기 위함이다.</li>
<li><code>cb</code> 콜백을 통해 에러가 없을 경우, 계산된 파일 이름을 반환한다.</li>
</ul>
</li>
</ul>
<p>이 함수는 파일 업로드 기능을 구현하는 데 있어서 파일의 저장 위치와 이름을 다루는 데 중요한 역할을 한다. 사용자가 파일을 업로드할 때, 이 설정에 따라 파일이 서버의 특정 위치에 특정 이름으로 저장된다.</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">const</span> <span class="title function_">multerOptions</span> = (<span class="params">folder: <span class="built_in">string</span></span>) =&gt; &#123;</span><br><span class="line">  <span class="keyword">const</span> <span class="attr">result</span>: <span class="title class_">MulterOptions</span> = &#123;</span><br><span class="line">    <span class="attr">storage</span>: <span class="title function_">storage</span>(folder),</span><br><span class="line">  &#125;;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> result;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>위 메서드는 파일 업로드를 위한 <code>multer</code>의 설정 옵션을 생성한다. 이 함수는 <code>multer</code> 미들웨어가 파일을 처리하는 방식을 설정하는 데 사용된다. 각 부분에 대한 자세한 설명은 다음과 같다 :</p>
<ul>
<li><code>multerOptions</code> 함수는 <code>folder</code>라는 문자열 매개변수를 받고, 이 매개변수는 파일이 저장될 폴더의 이름을 나타낸다.</li>
<li><code>result</code>라는 변수는 <code>MulterOptions</code> 타입으로 선언되며 <code>MulterOptions</code>는 <code>multer</code>의 설정 옵션을 정의하는 타입으로, 여기에는 <code>storage</code>, <code>limits</code>, <code>fileFilter</code> 등 다양한 설정이 포함될 수 있다.(현재 코드에서는 <code>storage</code> 옵션만 설정되어 있음)</li>
<li><code>storage</code> 옵션은 앞서 설명한 <code>storage</code> 함수를 호출하여 설정된다. 이 함수는 <code>multer</code>의 디스크 저장 엔진을 구성하며, 파일이 서버에 어떻게 저장될지를 정의한다.</li>
<li>함수는 <code>result</code> 객체를 반환하고, 이 객체는 <code>multer</code> 미들웨어에 전달되어 파일 업로드 처리에 사용된다.</li>
</ul>
<p>이후 컨트롤러를 다음과 같이 작성한다 :</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line">  <span class="meta">@ApiOperation</span>(&#123; <span class="attr">summary</span>: <span class="string">&#x27;고양이 이미지 업로드&#x27;</span> &#125;)</span><br><span class="line">  <span class="meta">@UseInterceptors</span>(<span class="title class_">FilesInterceptor</span>(<span class="string">&#x27;image&#x27;</span>, <span class="number">10</span>, <span class="title function_">multerOptions</span>(<span class="string">&#x27;cats&#x27;</span>)))</span><br><span class="line">  <span class="meta">@Post</span>(<span class="string">&#x27;upload&#x27;</span>)</span><br><span class="line">  <span class="title function_">uploadCatImg</span>(<span class="params"><span class="meta">@UploadedFiles</span>() files: <span class="built_in">Array</span>&lt;Express.Multer.File&gt;</span>) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(files);</span><br><span class="line">    <span class="keyword">return</span> &#123; <span class="attr">image</span>: <span class="string">`http://localhost:8000/media/cats/<span class="subst">$&#123;files[<span class="number">0</span>].filename&#125;</span>`</span> &#125;;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>함수는 업로드된 파일의 첫 번째 항목의 파일 이름을 사용하여 완전한 이미지 URL을 반환한다. 이 URL은 로컬 서버의 <code>http://localhost:8000/media/cats/</code> 경로에 업로드된 파일 이름을 추가하여 구성된다.</p>
<p>간단히 말해, 이 함수는 사용자가 업로드한 고양이 이미지를 서버에 저장하고, 그 이미지를 인터넷에서 볼 수 있는 주소를 만들어서 돌려주는 역할이다.</p>
<p>main.ts 파일을 아래처럼 작성한다 :</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line">app.<span class="title function_">useStaticAssets</span>(path.<span class="title function_">join</span>(__dirname, <span class="string">&quot;./common&quot;</span>, <span class="string">&quot;uploads&quot;</span>), &#123;</span><br><span class="line">  <span class="attr">pefix</span>: <span class="string">&quot;./media&quot;</span>,</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>이미지를 저장할 때 데이터베이스는 이미지의 경로를 저장한다. 이때 이 middleware를 서버에 있는 static 파일들을 제공하기 위해서 사용한다.</p>
<ul>
<li><code>app.useStaticAssets</code>: 이 메서드는 Express 애플리케이션에서 정적 파일을 서빙하기 위해 사용된다.</li>
<li><code>path.join(__dirname, &#39;./common&#39;, &#39;uploads&#39;)</code>: 정적 파일이 위치할 디렉토리 경로이다. 여기서는 <code>./common/uploads</code> 폴더를 사용하였다.</li>
<li><code>prefix: &#39;./media&#39;</code>: 정적 파일을 서빙할 URL 경로의 접두사이다. 이 설정으로 인해, <code>http://localhost:8000/media/...</code> URL을 통해 업로드된 파일에 접근할 수 있다.</li>
</ul>
<p><strong>17.2.2 작동 방식</strong></p>
<ol>
<li>사용자가 <code>/cats/upload</code> 경로로 이미지를 업로드하면, <code>FilesInterceptor</code>가 <code>multerOptions</code>에 정의된 설정에 따라 파일을 <code>./common/uploads/cats</code> 폴더에 저장한다.</li>
<li>업로드가 완료되면, 컨트롤러는 업로드된 파일의 URL을 반환한다.</li>
<li><code>app.useStaticAssets</code> 설정 덕분에, 업로드된 이미지는 <code>http://localhost:8000/media/cats/파일명</code> URL을 통해 접근할 수 있다.</li>
</ol>
<p>이러한 구성은 이미지 업로드 및 파일 서빙을 위한 일반적인 패턴으로, 사용자가 이미지를 업로드하고 해당 이미지에 쉽게 접근할 수 있게 한다. 데이터베이스에 저장되는 것은 파일의 경로이며, 이를 통해 나중에 이미지를 검색하고 사용할 수 있다.</p>
<h3 id="17-3-파일-업로드-service"><a href="#17-3-파일-업로드-service" class="headerlink" title="17.3 파일 업로드 service"></a>17.3 파일 업로드 service</h3><p>cats.service.ts</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line">  <span class="keyword">async</span> <span class="title function_">uploadImg</span>(<span class="params">cat: Cat, files: Express.Multer.File[]</span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> fileName = <span class="string">`cats/<span class="subst">$&#123;files[<span class="number">0</span>].filename&#125;</span>`</span>;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(fileName);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> newCat = <span class="keyword">await</span> <span class="variable language_">this</span>.<span class="property">catsRepository</span>.<span class="title function_">findByIdAndUpdateImg</span>(</span><br><span class="line">      cat.<span class="property">id</span>,</span><br><span class="line">      fileName,</span><br><span class="line">    );</span><br><span class="line"></span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(newCat);</span><br><span class="line">    <span class="keyword">return</span> newCat;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p><code>uploadImg</code> 메서드는 비동기 함수이고, 이 함수는 이미지를 업로드하고 데이터베이스에 해당 정보를 업데이트 하는 로직이다.</p>
<ul>
<li><code>files</code> 매개변수는 <code>Express.Multer.File</code> 타입의 배열로, 업로드된 파일들의 정보를 포함한다.</li>
<li><code>const fileName = &#39;cats/&#39; + files[0].filename</code>: 업로드된 파일 중 첫 번째 파일의 이름을 가져와 <code>cats/</code> 디렉토리 경로와 결합하여 <code>fileName</code>을 설정한다. 이를 통해 파일의 저장 경로와 이름이 결정된다.</li>
<li><code>const newCat = await this.catsRepository.findByIdAndUpdateImg(cat.id, fileName)</code>: <code>catsRepository</code>의 <code>findByIdAndUpdateImg</code> 함수를 호출하여 데이터베이스에 고양이 이미지의 경로를 업데이트한다. 이 함수는 고양이의 ID(<code>cat.id</code>)와 파일 이름(<code>fileName</code>)을 사용하여 데이터베이스의 해당 레코드를 찾고 업데이트한다. <code>await</code> 키워드는 이 작업이 완료될 때까지 함수 실행을 일시 중지한다.</li>
<li><code>return newCat</code>: 업데이트된 고양이 객체를 반환한다.</li>
</ul>
<p>위에서 언급한 함수인 <code>findByIdAndUpdateImg</code> 는 <code>catsRepository</code>에서 정의한 함수이다 :<br>cats.repository.ts</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line">  <span class="keyword">async</span> <span class="title function_">findByIdAndUpdateImg</span>(<span class="params">id: <span class="built_in">string</span>, fileName: <span class="built_in">string</span></span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> cat = <span class="keyword">await</span> <span class="variable language_">this</span>.<span class="property">catModel</span>.<span class="title function_">findById</span>(id);</span><br><span class="line">    cat.<span class="property">imgUrl</span> = <span class="string">`http://localhost:8000/media.<span class="subst">$&#123;fileName&#125;</span>`</span>;</span><br><span class="line">    <span class="keyword">const</span> newCat = <span class="keyword">await</span> cat.<span class="title function_">save</span>();</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(newCat);</span><br><span class="line">    <span class="keyword">return</span> newCat.<span class="property">readOnlyData</span>;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p><code>findByIdAndUpdateImg</code> 함수는 데이터베이스에서 특정 고양이(<code>Cat</code>) 객체를 찾아 이미지 URL을 업데이트하고 저장한다.</p>
<ul>
<li><code>(id: string, fileName: string)</code>: 이 함수는 두 개의 매개변수를 받는다. <code>id</code>는 데이터베이스에서 찾을 고양이 객체의 식별자이고, <code>fileName</code>은 업로드된 이미지 파일의 이름이다.</li>
<li><code>const cat = await this.catModel.findById(id)</code>: <code>catModel</code>의 <code>findById</code> 메서드를 사용해 주어진 <code>id</code>를 가진 고양이 객체를 데이터베이스에서 찾는다.</li>
<li><code>cat.imgUrl = &#39;http://localhost:8000/media.&#39; + fileName</code>: 찾은 고양이 객체의 <code>imgUrl</code> 속성을 새로운 이미지 URL로 설정한다. 이 URL은 서버의 <code>media</code> 디렉토리에 저장된 파일을 가리키며, <code>fileName</code>을 사용해 구체적인 파일 위치를 지정한다.</li>
<li><code>const newCat = await cat.save()</code>: 변경된 고양이 객체를 데이터베이스에 저장한다.</li>
<li><code>return newCat.readOnlyData</code>: <code>newCat.readOnlyData</code>는 업데이트된 고양이 객체의 읽기 전용 버전을 반환한다.</li>
</ul>
<p>이후 이제 schema 파일로 가서 기본 데이터에 imgUrl만 추가 해야한다. 그리고 controller 파일로 돌아가서 리턴을 다음과 같이 바꾸어준다 :</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">return</span> <span class="variable language_">this</span>.<span class="property">catsService</span>.<span class="title function_">uploadImg</span>(cat, files);</span><br></pre></td></tr></table></figure>

<p>서비스에서 만든 함수의 결과를 반환하면 된다. 이로써 이미지를 업로드하는 로직을 완성하였다.</p>
]]></content>
      <categories>
        <category>NestJS</category>
      </categories>
      <tags>
        <tag>study</tag>
        <tag>NestJS</tag>
      </tags>
  </entry>
  <entry>
    <title>NestJS-Repository 디자인 패턴</title>
    <url>/2024/01/13/nest12-repository/</url>
    <content><![CDATA[<h3 id="14-1-Repository-디자인-패턴"><a href="#14-1-Repository-디자인-패턴" class="headerlink" title="14.1 Repository 디자인 패턴"></a>14.1 Repository 디자인 패턴</h3><p>지금까지 Service 에서 데이터베이스에 직접적으로 접근하여 비즈니스 로직을 수행해왔다. 하지만 데이터베이스에 접근하는 비즈니스 로직이 많아지면 실제 다른 비즈니스 로직을 정의하기가 힘들어진다.</p>
<span id="more"></span>

<p>따라서 Repository 라는 파일을 따로 두어서 데이터베이스에 직접적으로 접근하는 로직을 분리시키는 패턴이 바로 <strong>Repository 디자인 패턴</strong>이다.</p>
<h3 id="14-2-Repository-디자인-예제"><a href="#14-2-Repository-디자인-예제" class="headerlink" title="14.2 Repository 디자인 예제"></a>14.2 Repository 디자인 예제</h3><figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Injectable</span> &#125; <span class="keyword">from</span> <span class="string">&quot;@nestjs/common&quot;</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">InjectModel</span> &#125; <span class="keyword">from</span> <span class="string">&quot;@nestjs/mongoose&quot;</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Model</span> &#125; <span class="keyword">from</span> <span class="string">&quot;mongoose&quot;</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Cat</span> &#125; <span class="keyword">from</span> <span class="string">&quot;./cats.schema&quot;</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">CatRequestDto</span> &#125; <span class="keyword">from</span> <span class="string">&quot;./dto/cats.request.dto&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Injectable</span>()</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">CatsRepository</span> &#123;</span><br><span class="line">  <span class="title function_">constructor</span>(<span class="params"><span class="meta">@InjectModel</span>(Cat.name) <span class="keyword">private</span> <span class="keyword">readonly</span> catModel: Model&lt;Cat&gt;</span>) &#123;&#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">async</span> <span class="title function_">findCatByEmail</span>(<span class="attr">email</span>: <span class="built_in">string</span>): <span class="title class_">Promise</span>&lt;<span class="title class_">Cat</span> | <span class="literal">null</span>&gt; &#123;</span><br><span class="line">    <span class="keyword">const</span> cat = <span class="keyword">await</span> <span class="variable language_">this</span>.<span class="property">catModel</span>.<span class="title function_">findOne</span>(&#123; email &#125;);</span><br><span class="line">    <span class="keyword">return</span> cat;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">async</span> <span class="title function_">existsByEmail</span>(<span class="attr">email</span>: <span class="built_in">string</span>): <span class="title class_">Promise</span>&lt;<span class="built_in">boolean</span>&gt; &#123;</span><br><span class="line">    <span class="keyword">const</span> result = <span class="keyword">await</span> <span class="variable language_">this</span>.<span class="property">catModel</span>.<span class="title function_">exists</span>(&#123; email &#125;);</span><br><span class="line">    <span class="keyword">if</span> (result) <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">async</span> <span class="title function_">create</span>(<span class="attr">cat</span>: <span class="title class_">CatRequestDto</span>): <span class="title class_">Promise</span>&lt;<span class="title class_">Cat</span>&gt; &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">await</span> <span class="variable language_">this</span>.<span class="property">catModel</span>.<span class="title function_">create</span>(cat);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>cats.repository.ts 파일의 코드이다. 이 코드를 보면 직접적으로 데이터베이스에 접근하여 필요한 값을 리턴하는 메서드들이 있다는 것을 알 수있다.</p>
<p>예를 들어 <code>findCatByEmail</code> 메서드는 <code>email</code> 을 매개변수로 받아서 이 이메일을 갖고 있는 <code>cat</code> 객체를 그대로 리턴하는 메서드이다. 이제 서비스에서는 이 메서드들을 불러와서 데이터베이스에 접근을 하기 때문에 기존의 service 파일보다 더 가벼워지고 비즈니스 로직을 구분하기 쉬워진다.</p>
]]></content>
      <categories>
        <category>NestJS</category>
      </categories>
      <tags>
        <tag>study</tag>
        <tag>NestJS</tag>
      </tags>
  </entry>
  <entry>
    <title>NestJS-Controller(컨트롤러) &amp; DI(의존성주입)</title>
    <url>/2024/01/08/nest4-controllerANDdependencyInjection/</url>
    <content><![CDATA[<h3 id="6-1-controller-정의"><a href="#6-1-controller-정의" class="headerlink" title="6.1 controller 정의"></a>6.1 controller 정의</h3><p><img src="https://docs.nestjs.com/assets/Controllers_1.png"></p>
<span id="more"></span>

<p>컨트롤러 클래스는 특정 <strong>엔드포인트</strong>에 대한 요청을 처리하는 메서드를 포함한다. 컨트롤러는 <code>@Controller()</code> 데코레이터를 사용하여 정의된다.</p>
<p>이 데코레이터는 클래스로 필수 메타데이터와 연결하고 Nest가 라우팅 맵을 만들 수 있도록 한다. 라우팅 맵에 대해서는 나중에 따로 공부할 계획이다.</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Controller</span>(<span class="string">&#x27;cats&#x27;</span>)</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">CatsController</span> &#123; ... &#125;</span><br></pre></td></tr></table></figure>

<p>쉽게 말해서 컨트롤러는 클라이언트로부터 들어오는 요청에 대해서 어떻게 응답할 것인지를 정의한다. 위 용어에서 <strong>엔드 포인트</strong>란 <code>/cats</code> 와 같은 URL을 가리킨다.</p>
<p>위 예시를 보면 컨트롤러 데코레이터 안에 <code>&#39;cats&#39;</code> 를 작성한 것을 알 수 있다. 이 클래스는 기본적으로 <code>/cats</code> 라는 엔드 포인트에서 작동되는 기능을 다룬 컨트롤러라는 뜻이다.</p>
<p>컨트롤러에 들어가는 메서드는 보통 HTTP 요청에 대한 처리를 정의한다.</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Controller</span>(<span class="string">&#x27;cats&#x27;</span>)</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">CatsController</span> &#123;</span><br><span class="line"><span class="title function_">constructor</span>(<span class="params"><span class="keyword">private</span> <span class="keyword">readonly</span> catsService: CatsService</span>) &#123;&#125;</span><br><span class="line"></span><br><span class="line">  <span class="meta">@Get</span>()</span><br><span class="line">  <span class="title function_">getCurrentCat</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&#x27;current cat&#x27;</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">	...</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>위 컨트롤러 코드에서 정의된 get 메서드를 보면 <code>@Get()</code> 데코레이터로 정의된 것을 볼 수 있다. 이 데코레이터는 <code>getCurrentCat</code> 메서드가 HTTP GET 요청을 처리하는 메서드임을 나타낸다.</p>
<p>현재는 단순히 문자열을 반환만 하고 있다. 이 부분은 나중에 실제 db에서 데이터를 가져오거나 다른 비즈니스 로직을 수행하는 부분으로 대체될 수 있다. 이런 자세한 로직은 <code>catsService</code> 에서 작성한다.</p>
<h3 id="6-2-controller-예제"><a href="#6-2-controller-예제" class="headerlink" title="6.2 controller 예제"></a>6.2 controller 예제</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">nest g co controller-name</span><br></pre></td></tr></table></figure>

<p>위 명령어를 통해서 컨트롤러를 생성해주면 기본적으로 아래와 같은 코드 파일이 생성된다 :</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Controller</span> &#125; <span class="keyword">from</span> <span class="string">&quot;@nestjs/common&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Controller</span>()</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">CatsController</span> &#123;&#125;</span><br></pre></td></tr></table></figure>

<p>또한 모듈에서는 이 컨트롤러와 자동으로 연결된다 :</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Module</span>(&#123;</span><br><span class="line">  <span class="attr">controllers</span>: [<span class="title class_">CatsController</span>],</span><br><span class="line">  <span class="attr">providers</span>: [],</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<p>다시 컨트롤러 파일로 돌아와서 <code>@Controller()</code> 데코레이터 안에 공통적으로 들어갈 엔드포인트를 작성한다(꼭 필요하지 않음). 하지만 나는 <code>cats/</code> 라는 엔드포인트에서 http 메서드를 다룰 것이기에 <code>cats</code>를 작성하였다.</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Controller</span>(<span class="string">&#x27;cats&#x27;</span>)</span><br></pre></td></tr></table></figure>

<p>그리고 클래스 안에서 메서드를 작성해주었다. <code>getCurrentCat</code> 는 현재 Cat 정보를 단순히 리턴하고, <code>signUp</code> 은 회원가입, <code>logIn</code> 은 로그인을 하는 메서드이다. 아직 구현은 하지 않았고 단순히 문자열을 반환하는 상태로만 두었다.</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="title function_">constructor</span>(<span class="params"><span class="keyword">private</span> <span class="keyword">readonly</span> catsService: CatsService</span>) &#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Get</span>()</span><br><span class="line">  <span class="title function_">getCurrentCat</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&#x27;current cat&#x27;</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="meta">@Post</span>()</span><br><span class="line">  <span class="keyword">async</span> <span class="title function_">signUp</span>(<span class="params"><span class="meta">@Body</span>() body</span>) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(body);</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&#x27;signup&#x27;</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="meta">@Post</span>(<span class="string">&#x27;login&#x27;</span>)</span><br><span class="line">  <span class="title function_">logIn</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&#x27;login&#x27;</span>;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>생성자를 보면 <code>CatService</code> 가 의존성 주입으로 연결된 것을 알 수 있다. 의존성 주입은 바로 다음 6.3에서 다루고, Service는 다음 글에서 다루겠다.</p>
<h5 id="6-3-의존성-주입-Dependency-Injection"><a href="#6-3-의존성-주입-Dependency-Injection" class="headerlink" title="6.3 의존성 주입(Dependency Injection)"></a>6.3 의존성 주입(Dependency Injection)</h5><p>의존성 주입(Dependency Injection)이란?</p>
<p>의존성 주입은 필요한 객체를 외부에서 넣어주는 방식이다. 즉, 의존 관계를 외부에서 정의한다는 의미이다.</p>
<p>왜 이런 방법을 사용해야할까?</p>
<p>만약 모듈간의 의존성이 크다고 가정해보자. A라는 모듈이 변경된다면 B도 영향을 받게되고, 프로그램이 커진다면 도미노처럼 모든 모듈에 영향을 미치게 될 것이다.</p>
<p>예를 들어 아래와 같이 클래스의 생성자가 있다고 가정하자:</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">CatsController</span> &#123;</span><br><span class="line">  <span class="title function_">constructor</span>(<span class="params"><span class="keyword">private</span> <span class="keyword">readonly</span> catsService: CatsService</span>) &#123;&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>위는 클래스 내부에서 <code>CatsService</code> 클래스의 인스턴스를 생성하는 것이 아닌 생성자에서 파라미터를 받는 부분인 외부를 통해서 의존성 주입(Dependency Injection)을 하고 있다.</p>
<p>이 코드의 주요 이점은 <code>CatsController</code>가 <code>CatsService</code>에 직접적으로 의존하는 대신, 외부에서 주입된 의존성을 사용함으로써 코드의 유연성을 향상시키고, 테스트 용이성을 높이며, 의존하는 서비스를 손쉽게 교체할 수 있게 한다.</p>
<p>만약 내부에서 의존성 주입을 한다면 아래와 같이 코드를 짜게 될 것이다:</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">CatsController</span> &#123;</span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">readonly</span> <span class="attr">catsService</span>: <span class="title class_">CatsService</span>;</span><br><span class="line">  <span class="title function_">constructor</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="comment">// 의존성을 직접 생성하여 초기화</span></span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">catsService</span> = <span class="keyword">new</span> <span class="title class_">CatsService</span>();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>NestJS</category>
      </categories>
      <tags>
        <tag>study</tag>
        <tag>NestJS</tag>
      </tags>
  </entry>
  <entry>
    <title>NestJS-Route, Middleware(미들웨어)</title>
    <url>/2024/01/10/nest6-routeANDmiddleware/</url>
    <content><![CDATA[<h3 id="8-1-Route"><a href="#8-1-Route" class="headerlink" title="8.1 Route"></a>8.1 Route</h3><p><strong>Route</strong> 는 웹 애플리케이션에서 <strong>특정 URL 경로에 대한 요청을 처리하는 방법</strong>을 정의한다.</p>
<span id="more"></span>

<p>각 라우트는 특정 URL 패턴에 매칭되고, 해당 URL에 대한 요청이 있을 때 실행될 코드를 결정한다. 라우트는 서버에 들어오는 요청을 적절한 <strong>Route Handler</strong> 로 라우팅하는 역할을 한다.</p>
<h3 id="8-2-Route-Handler"><a href="#8-2-Route-Handler" class="headerlink" title="8.2 Route Handler"></a>8.2 Route Handler</h3><p><strong>Route Handler</strong> 는 특정 URL에 대한 요청을 처리하는 함수 또는 메서드이다. 각 라우트는 특정 URL에 대한 요청이 발생했을 때 실행될 라우트 핸들러를 지정한다.</p>
<p>즉, 라우터 핸들러는 단순히 특정 URL 경로에 대한 요청을 처리하는 함수 또는 메서드를 가리키는 일반적인 용어이다.</p>
<p>컨트롤러는 특정 URL에 대한 요청을 처리하는 역할을 수행하는데, 이때 해당 URL에 대한 처리 로직을 구현하는 부분이 컨트롤러 내의 메서드(라우트 핸들러)이다.</p>
<p>따라서 <strong>Controller는 라우트 핸들러(Route Handler)의 한 형태</strong>로 볼 수 있다.</p>
<h3 id="8-3-Routing-Map"><a href="#8-3-Routing-Map" class="headerlink" title="8.3 Routing Map"></a>8.3 Routing Map</h3><p><strong>라우팅 맵(Routing Map)</strong> 은 웹 애플리케이션에서 어떤 URL 경로에 대한 요청이 어떤 핸들러(컨트롤러 또는 함수)로 전달되어야 하는지를 나타내는 구조임을 알 수 있다.</p>
<h3 id="8-4-Middleware-정의"><a href="#8-4-Middleware-정의" class="headerlink" title="8.4 Middleware 정의"></a>8.4 Middleware 정의</h3><p><img src="https://docs.nestjs.com/assets/Middlewares_1.png"></p>
<p><img src="https://github.com/1Dohyeon/blogIMG/blob/master/nest1-datastructure-algorithms/middlewareIMG.png?raw=true" alt="middlewareIMG.png"></p>
<p><strong>미들웨어(Middleware)</strong> 는 클라이언트의 요청과 서버의 응답 사이에 위치하는 함수이다. 미들웨어는 요청이 라우트 핸들러에 도달하기 전에 중간에서 특정 작업을 수행할 수 있다.</p>
<p><strong>왜 바로 Router Handler 로 요청을 보내지 않고 번거롭게 Middleware 를 거쳐야할까?</strong></p>
<p>미들웨어는 클라이언트의 요청이 라우트 핸들러에 도달하기 전에 실행되기 때문에 이를 통해 요청을 전처리하고, 필요한 변경을 가하거나 검증을 수행할 수 있다는 장점이 있다.</p>
<p>또한 response 를 클라이언트에게 보내기 전에 middleware 를 통하여 응답을 변조하거나 수정할 수 있다.</p>
<p>예를 들어, 요청 로깅, 인증, 데이터 변환 등의 작업을 미들웨어에서 처리할 수 있다. 이는 웹 애플리케이션의 구조와 로직을 조직화하고 유연하게 처리하기 위해 사용된다.</p>
<p>위 그림 외에도 아래 그림처럼 여러 단계를 거치게 된다.</p>
<p><img src="https://blog.logrocket.com/wp-content/uploads/2023/03/incoming-request-flow-guards.png" alt="Where guards fit into the incoming request flow"><br><a href="https://blog.logrocket.com/understanding-guards-nestjs/">이미지 출처 URL</a></p>
<p>위 그림에는 Exception filters 이 없어서 NestJS 의 Request lifecycle 을 작성해보았다. 이 사이클에 있는 Guard, Interceptor, pipes, Exception filters 에 대해서는 다음에 공부할 계획이다.</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">1. Incoming request</span><br><span class="line"></span><br><span class="line">2. Middleware</span><br><span class="line">  2.1. Globally bound middleware</span><br><span class="line">  2.2. Module bound middleware</span><br><span class="line"></span><br><span class="line">3. Guards</span><br><span class="line">  3.1 Global guards</span><br><span class="line">  3.2 Controller guards</span><br><span class="line">  3.3 Route guards</span><br><span class="line"></span><br><span class="line">4. Interceptors (pre-controller)</span><br><span class="line">  4.1 Global interceptors</span><br><span class="line">  4.2 Controller interceptors</span><br><span class="line">  4.3 Route interceptors</span><br><span class="line"></span><br><span class="line">5. Pipes</span><br><span class="line">  5.1 Global pipes</span><br><span class="line">  5.2 Controller pipes</span><br><span class="line">  5.3 Route pipes</span><br><span class="line">  5.4 Route parameter pipes</span><br><span class="line"></span><br><span class="line">6. Controller (method handler)</span><br><span class="line"></span><br><span class="line">7. Service (if exists)</span><br><span class="line"></span><br><span class="line">8. Interceptors (post-request)</span><br><span class="line">  8.1 Route interceptor</span><br><span class="line">  8.2 Controller interceptor</span><br><span class="line">  8.3 Global interceptor</span><br><span class="line"></span><br><span class="line">9. Exception filters</span><br><span class="line">  9.1 route</span><br><span class="line">  9.2 controller</span><br><span class="line">  9.3 global</span><br><span class="line"></span><br><span class="line">10. Server response</span><br></pre></td></tr></table></figure>

<p><a href="https://docs.nestjs.com/faq/request-lifecycle">참고 URL</a></p>
<h3 id="8-5-Middleware-예제"><a href="#8-5-Middleware-예제" class="headerlink" title="8.5 Middleware 예제"></a>8.5 Middleware 예제</h3><blockquote>
<p>Nest 에서 middleware 는 express 에서의 middleware와 같다.</p>
</blockquote>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">nest g middleware name</span><br></pre></td></tr></table></figure>

<p>위 명령어를 통해서 middleware 파일을 생성할 수 있다. 기본적으로는 아래와 같이 생성된다 :</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Injectable</span>, <span class="title class_">NestMiddleware</span> &#125; <span class="keyword">from</span> <span class="string">&#x27;@nestjs/common&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Injectable</span>()</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">LoggerMiddleware</span> <span class="keyword">implements</span> <span class="title class_">NestMiddleware</span> &#123;</span><br><span class="line">  <span class="title function_">use</span>(<span class="params">req: <span class="built_in">any</span>, res: <span class="built_in">any</span>, next: () = &gt; <span class="built_in">void</span></span>) &#123;</span><br><span class="line">    <span class="title function_">next</span>();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>이제 위 파일 내용을 <code>express</code> 의 <code>&#123;Request, Response, Nextfunction&#125;</code> 타이핑 처리를 해준다 :</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Injectable</span>, <span class="title class_">NestMiddleware</span> &#125; <span class="keyword">from</span> <span class="string">&quot;@nestjs/common&quot;</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">NextFunction</span>, <span class="title class_">Request</span>, <span class="title class_">Response</span> &#125; <span class="keyword">from</span> <span class="string">&quot;express&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Injectable</span>()</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">LoggerMiddleware</span> <span class="keyword">implements</span> <span class="title class_">NestMiddleware</span> &#123;</span><br><span class="line">  <span class="title function_">use</span>(<span class="params">req: Request, res: Response, next: NextFunction</span>) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(req.<span class="property">ip</span>);</span><br><span class="line">    <span class="title function_">next</span>();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>참고로 <code>Request</code> 에는 요청하는 모든 정보가 담겨있다. 이제 middleware를 연결해주어야 한다. 하지만 middleware 는 <code>@Module</code> 에서 연결을 할 수 없다. 따라서 <code>configure()</code> 모듈 클래스의 메서드를 사용하여 설정해야 한다. 아래 코드는 <code>app.module.ts</code> 의 일부이다. 이곳에서 <code>configure</code> 메서드를 통해서 사용할 수 있다.</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">AppModule</span> <span class="keyword">implements</span> <span class="title class_">NestModule</span> &#123;</span><br><span class="line">  <span class="title function_">configure</span>(<span class="params">consumer: MiddlewareConsumer</span>) &#123;</span><br><span class="line">    consumer.<span class="title function_">apply</span>(<span class="title class_">LoggerMiddleware</span>).<span class="title function_">forRoutes</span>(<span class="string">&quot;cats&quot;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>위 코드에서 <code>NestModule</code> 는 인터페이스이다.</p>
<ul>
<li><code>MiddlewareConsumer</code> : <code>import &#123; MiddlewareConsumer, Module, NestModule &#125; from &#39;@nestjs/common&#39;;</code> 를 통해서 가져온 소비자 타입이다.</li>
<li><code>consumer.apply(LoggerMiddleware).forRoutes(&#39;cats&#39;);</code> : 위에서 만든 <code>LoggerMiddleware</code> 클래스를 연결하고 <code>cats</code>라우터에 바인딩을 시켜준 것이다. 따라서 <code>cats</code> 엔드포인트에 대해서 <code>LoggerMiddleware</code>가 실행이 된다는 의미이다.</li>
</ul>
<p>이제 <code>LoggerMiddleware</code> 활용할 수 있도록 작성해준다 :</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Injectable</span>, <span class="title class_">Logger</span>, <span class="title class_">NestMiddleware</span> &#125; <span class="keyword">from</span> <span class="string">&quot;@nestjs/common&quot;</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">NextFunction</span>, <span class="title class_">Request</span>, <span class="title class_">Response</span> &#125; <span class="keyword">from</span> <span class="string">&quot;express&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Injectable</span>()</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">LoggerMiddleware</span> <span class="keyword">implements</span> <span class="title class_">NestMiddleware</span> &#123;</span><br><span class="line">  <span class="keyword">private</span> logger = <span class="keyword">new</span> <span class="title class_">Logger</span>(<span class="string">&quot;HTTP&quot;</span>);</span><br><span class="line"></span><br><span class="line">  <span class="title function_">use</span>(<span class="params">req: Request, res: Response, next: NextFunction</span>) &#123;</span><br><span class="line">    res.<span class="title function_">on</span>(<span class="string">&quot;finish&quot;</span>, <span class="function">() =&gt;</span> &#123;</span><br><span class="line">      <span class="variable language_">this</span>.<span class="property">logger</span>.<span class="title function_">log</span>(</span><br><span class="line">        <span class="string">`<span class="subst">$&#123;req.ip&#125;</span> <span class="subst">$&#123;req.method&#125;</span> <span class="subst">$&#123;res.statusCode&#125;</span>`</span>,</span><br><span class="line">        req.<span class="property">originalUrl</span></span><br><span class="line">      );</span><br><span class="line">    &#125;);</span><br><span class="line"></span><br><span class="line">    res.<span class="property">statusCode</span>;</span><br><span class="line">    <span class="title function_">next</span>();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><code>private logger = new Logger(&#39;HTTP&#39;);</code> : NesJS 에서는 로깅을 할 때 <code>Logger</code> 라는 클래스를 사용한다. 이 코드는 <code>HTTP</code> 에 관한 로거를 사용할 수 있도록 한다. 이를 이용해서 <code>console.log</code> 대신 <code>logger.log</code>를 사용할 수 있다.</li>
<li><code>res.on(&#39;finish&#39;, () =&gt; &#123; ... &#125;)</code> : 응답이 완료 되었을 때 <code>&#123; ... &#125;</code> 실행. 위 코드에서는 이 안에서 <code>req</code>의 ip주소, 메서드, url 이 작성되고 응답 상태 코드가 작성된다.</li>
</ul>
]]></content>
      <categories>
        <category>NestJS</category>
      </categories>
      <tags>
        <tag>study</tag>
        <tag>NestJS</tag>
      </tags>
  </entry>
  <entry>
    <title>NestJS-Singleton(싱글톤)</title>
    <url>/2024/01/06/nest2-singleton/</url>
    <content><![CDATA[<h3 id="04-Singleton-정의"><a href="#04-Singleton-정의" class="headerlink" title="04. Singleton 정의"></a>04. Singleton 정의</h3><p>Singleton(싱글톤) 패턴이란 클래스의 인스턴스가 1개만 생성하는 것을 보장하는 패턴이다. 보통 웹 어플리케이션 서비스에서 여러 고객이 동시에 요청하는 경우가 있다. 이때마다 객체를 계속 생성한다면 메모리 낭비가 심해질 것이다.</p>
<span id="more"></span>

<p>이런 문제점을 해결하기 위해서 사용되는 방법이 바로 싱글톤 패턴이다. 하지만 이런 싱글톤 패턴도 단점이 있다. 구현하기 위해서 코드가 많이 쓰이고, 테스트하기가 어렵다. 또한 유연성이 떨어지는 경우도 있다.</p>
<p>하지만 Nest 프레임워크에서 이런 문제점을 해결하고 인스턴스를 싱글톤 패턴으로 관리할 수 있다.</p>
]]></content>
      <categories>
        <category>NestJS</category>
      </categories>
      <tags>
        <tag>study</tag>
        <tag>NestJS</tag>
      </tags>
  </entry>
  <entry>
    <title>NestJS-Module(모듈)</title>
    <url>/2024/01/08/nest3-module/</url>
    <content><![CDATA[<h3 id="5-1-module-정의"><a href="#5-1-module-정의" class="headerlink" title="5.1 module 정의"></a>5.1 module 정의</h3><p><img src="https://docs.nestjs.com/assets/Modules_1.png"></p>
<span id="more"></span>

<p>NestJS 에서 모듈은 <code>@Module()</code> 데코레이터를 사용하여 정의된다. <a href="https://docs.nestjs.com/modules">공식문서</a> 에서 모듈은 nest가 애플리케이션 구조를 만들 때 사용할 수 있는 메타데이터를 제공해주는 역할을 한다고 말한다.</p>
<p>간단하게 말하자면 Module 은 관련된 기능을 하나로 묶어주는 단위이다. 애플리케이션을 여러 모듈로 나누어서 관리하면 유지보수가 편리하기에 사용하는 방법이다.</p>
<p>예를 들어 <code>app.ts</code> 파일에 여러 모듈(User, Order 등)들이 있다고 가정해보자. 이 파일은 수천줄이 넘어갈 것이고, 관리하기 힘들어질 것이다. 따라서 모듈화를 통해서 여러 기능들을 분리고 캡슐화를 한다.<br>(캡슐화란 모듈 안에 속한 코드와 데이터를 외부로부터 감추고, 외부에서는 모듈의 인터페이스만 이용할 수 있게 하여서 모듈간의 의존성을 낮추고 독립적으로 작동할 수 있게 한다. 즉, 외부로부터 접근을 제한하는 것을 의미한다.)</p>
<p>따라서 우리는 모듈을 나눈 상태에서 서로의 리소스를 공유하는 패턴을 유지하는 것이 중요하다.</p>
<p><img src="https://docs.nestjs.com/assets/Shared_Module_1.png"></p>
<p>또한 모듈은 싱글톤 패턴이기 때문에 nest에서는 여러 모듈간에 쉽게 공급자의 동일한 인스턴스를 공유할 수 있다.</p>
<h3 id="5-2-module-예제"><a href="#5-2-module-예제" class="headerlink" title="5.2 module 예제"></a>5.2 module 예제</h3><p>NestJS에서 모듈을 생성할 때 다음과 같은 명령어를 이용할 수 있다 :</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">nest g mo module-name</span><br></pre></td></tr></table></figure>

<p>위 명령어로 <code>cats</code> 라는 모듈을 생성하면 처음은 아래 코드처럼 주어진다.</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Module</span> &#125; <span class="keyword">from</span> <span class="string">&quot;@nestjs/common&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// nest g mo cats 로 자동으로 모듈 생성</span></span><br><span class="line"><span class="meta">@Module</span>(&#123;&#125;)</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">CatsModule</span> &#123;&#125;</span><br></pre></td></tr></table></figure>

<p>위 설명처럼 <code>@Module</code>이라는 데코레이터를 이용하여 <code>CatsModule</code> 클래스는 <code>cats</code> 의 모듈임을 알 수 있다.</p>
<p>지금 위에는 작성되어 있지 않지만 나중에 각 모듈에 맞는 컨트롤러와 서비스를 생성한다면 아래와 같이 <code>@Module</code> 데코레이터를 통해서 연결할 수 있다 :</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Module</span>(&#123;</span><br><span class="line">  <span class="attr">controllers</span>: [<span class="title class_">CatsController</span>],</span><br><span class="line">  <span class="attr">providers</span>: [<span class="title class_">CatsService</span>],</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>NestJS</category>
      </categories>
      <tags>
        <tag>study</tag>
        <tag>NestJS</tag>
      </tags>
  </entry>
  <entry>
    <title>NestJS-HttpExceptionFilter</title>
    <url>/2024/01/10/nest7-filter/</url>
    <content><![CDATA[<h3 id="9-1-HttpException"><a href="#9-1-HttpException" class="headerlink" title="9.1 HttpException"></a>9.1 HttpException</h3><p>Nest에서 제공하는 <code>HttpException</code> 를 이용해서 에러 처리를 할 수 있다. 아래 코드는 <code>cats</code>에 들어갔을 때 에러를 띄우는 예시이다 :</p>
<span id="more"></span>

<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"> <span class="comment">// cats/</span></span><br><span class="line"> <span class="meta">@Get</span>()</span><br><span class="line">  <span class="title function_">getCurrentCat</span>(<span class="params"></span>) &#123;</span><br><span class="line"><span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">HttpException</span>(<span class="string">&#x27;api is broken&#x27;</span>, <span class="number">401</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&#x27;current cat&#x27;</span>;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>하지만 서비스의 형태에 따라 에러를 커스텀해야할 때가 있다. 또한 이때 에러 메시지의 재사용성을 활용하기 위해서 <strong>HttpExceptionFilter</strong> 를 이용할 수 있다.</p>
<h3 id="9-2-HttpExceptionFilter"><a href="#9-2-HttpExceptionFilter" class="headerlink" title="9.2 HttpExceptionFilter"></a>9.2 HttpExceptionFilter</h3><p>우선 나는 src&#x2F;common&#x2F;exception 폴더 위치에 <code>http-exception.filter.ts</code> 파일을 만들었다 :</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123;</span><br><span class="line">  <span class="title class_">ArgumentsHost</span>,</span><br><span class="line">  <span class="title class_">Catch</span>,</span><br><span class="line">  <span class="title class_">ExceptionFilter</span>,</span><br><span class="line">  <span class="title class_">HttpException</span>,</span><br><span class="line">&#125; <span class="keyword">from</span> <span class="string">&quot;@nestjs/common&quot;</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Request</span>, <span class="title class_">Response</span> &#125; <span class="keyword">from</span> <span class="string">&quot;express&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Catch</span>(<span class="title class_">HttpException</span>)</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">HttpExceptionFilter</span> <span class="keyword">implements</span> <span class="title class_">ExceptionFilter</span> &#123;</span><br><span class="line">  <span class="keyword">catch</span>(<span class="attr">exception</span>: <span class="title class_">HttpException</span>, <span class="attr">host</span>: <span class="title class_">ArgumentsHost</span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> ctx = host.<span class="title function_">switchToHttp</span>();</span><br><span class="line">    <span class="keyword">const</span> response = ctx.<span class="property">getResponse</span>&lt;<span class="title class_">Response</span>&gt;();</span><br><span class="line">    <span class="keyword">const</span> request = ctx.<span class="property">getRequest</span>&lt;<span class="title class_">Request</span>&gt;();</span><br><span class="line">    <span class="keyword">const</span> status = exception.<span class="title function_">getStatus</span>();</span><br><span class="line">    <span class="keyword">const</span> error = exception.<span class="title function_">getResponse</span>() <span class="keyword">as</span></span><br><span class="line">      | <span class="built_in">string</span></span><br><span class="line">      | &#123; <span class="attr">error</span>: <span class="built_in">string</span>; <span class="attr">statusCode</span>: <span class="built_in">number</span>; <span class="attr">message</span>: <span class="built_in">string</span> | <span class="built_in">string</span>[] &#125;; <span class="comment">// 분기처리</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">typeof</span> error === <span class="string">&quot;string&quot;</span>) &#123;</span><br><span class="line">      response.<span class="title function_">status</span>(status).<span class="title function_">json</span>(&#123;</span><br><span class="line">        <span class="attr">success</span>: <span class="literal">false</span>, <span class="comment">// 예외처리가 발생된 것이므로 false</span></span><br><span class="line">        <span class="attr">timestamp</span>: <span class="keyword">new</span> <span class="title class_">Date</span>().<span class="title function_">toISOString</span>(),</span><br><span class="line">        <span class="attr">path</span>: request.<span class="property">url</span>,</span><br><span class="line">        error,</span><br><span class="line">      &#125;);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="comment">// nest 자체에서 발생하는 에러</span></span><br><span class="line">      response.<span class="title function_">status</span>(status).<span class="title function_">json</span>(&#123;</span><br><span class="line">        <span class="attr">success</span>: <span class="literal">false</span>,</span><br><span class="line">        <span class="attr">timestamp</span>: <span class="keyword">new</span> <span class="title class_">Date</span>().<span class="title function_">toISOString</span>(),</span><br><span class="line">        ...error,</span><br><span class="line">      &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>위 코드는 <a href="https://docs.nestjs.com/exception-filters">NestJS 공식문서</a>에서 작성된 <code>http-exception.filter.ts</code> 파일내의 코드를 수정한 코드이다.</p>
<p>아래 코드는 위 코드의 일부분을 가져온 것이다 :</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> error = exception.<span class="title function_">getResponse</span>() <span class="keyword">as</span></span><br><span class="line">  | <span class="built_in">string</span></span><br><span class="line">  | &#123; <span class="attr">error</span>: <span class="built_in">string</span>; <span class="attr">statusCode</span>: <span class="built_in">number</span>; <span class="attr">message</span>: <span class="built_in">string</span> | <span class="built_in">string</span>[] &#125;;</span><br></pre></td></tr></table></figure>

<p>위 코드에서 <code>error</code>는 예외처리시 응답을 받는데 이때 <code>string</code> 으로 받을 수 있지만, nest 자체에서 JSON 으로 예외처리를 일으키므로 따로 타입을 지정하였다.</p>
<p>이 타입에 따라서 에러 상태를 json으로 전달하기 위해서 아래 코드와 같이 분기처리를 해주었다 :</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 분기처리</span></span><br><span class="line"><span class="keyword">if</span> (<span class="keyword">typeof</span> error === <span class="string">&quot;string&quot;</span>) &#123;</span><br><span class="line">  response.<span class="title function_">status</span>(status).<span class="title function_">json</span>(&#123;</span><br><span class="line">    <span class="attr">success</span>: <span class="literal">false</span>, <span class="comment">// 예외처리가 발생된 것이므로 false</span></span><br><span class="line">    <span class="attr">timestamp</span>: <span class="keyword">new</span> <span class="title class_">Date</span>().<span class="title function_">toISOString</span>(),</span><br><span class="line">    <span class="attr">path</span>: request.<span class="property">url</span>,</span><br><span class="line">    error,</span><br><span class="line">  &#125;);</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">  <span class="comment">// nest 자체에서 발생하는 에러</span></span><br><span class="line">  response.<span class="title function_">status</span>(status).<span class="title function_">json</span>(&#123;</span><br><span class="line">    <span class="attr">success</span>: <span class="literal">false</span>,</span><br><span class="line">    <span class="attr">timestamp</span>: <span class="keyword">new</span> <span class="title class_">Date</span>().<span class="title function_">toISOString</span>(),</span><br><span class="line">    ...error,</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>이제 이 필터를 사용하기 위해서는 <code>@UseFilters()</code> 라는 데코레이터를 이용하여 컨트롤러와 연결할 수 있다. 물론 글로벌 설정도 할 수 있다.</p>
<p>우선 아래 코드는 데코레이터를 활용하여 컨트롤러에 연결한 모습이다 :</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="meta">@UseFilters</span>(<span class="title class_">HttpExceptionFilter</span>)</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">CatsController</span> &#123; ... &#125;</span><br></pre></td></tr></table></figure>

<p>컨트롤러 내에서 설정된 exception 인자를 <code>HttpExceptionFilter</code> 에서 받고, 이 클래스에서 설정한대로 에러 내용을 전달한다.</p>
<p>만약 클래스 내에서 <code>throw new HttpException(&#39;api is broken&#39;, 401);</code> 라는 인자를 넘기게 되면, <code>HttpExceptionFilter</code> 내에서 <code>string</code> 을 처리하는 부분을 통해서 에러 처리를 한다.</p>
<p>글로벌 설정은 다음과 같이 할 수 있다 :</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">bootstrap</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> app = <span class="keyword">await</span> <span class="title class_">NestFactory</span>.<span class="title function_">create</span>(<span class="title class_">AppModule</span>);</span><br><span class="line"></span><br><span class="line">  app.<span class="title function_">useGlobalFilters</span>(<span class="keyword">new</span> <span class="title class_">HttpExceptionFilter</span>());</span><br><span class="line"></span><br><span class="line">  ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>위 코드는 <code>main.ts</code> 파일이고, 글로벌 설정을 통해서 따로 예외처리를 안한 곳에서 예외가 나면 nest가 자동으로 JSON 형태로 인자를 넘기게 된다.</p>
]]></content>
      <categories>
        <category>NestJS</category>
      </categories>
      <tags>
        <tag>study</tag>
        <tag>NestJS</tag>
      </tags>
  </entry>
  <entry>
    <title>NestJS-Service(서비스)</title>
    <url>/2024/01/09/nest5-service/</url>
    <content><![CDATA[<h3 id="7-1-Service-정의"><a href="#7-1-Service-정의" class="headerlink" title="7.1 Service 정의"></a>7.1 Service 정의</h3><p>1.2.2 에서 언급했듯이 service는 비즈니스 로직을 정의하는 파일이다. <code>Injectable()</code> 데코레이터를 사용하여 정의되며 클래스 형태로 작성된다.</p>
<span id="more"></span>

<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Injectable</span>()</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">CatsService</span> &#123; ... &#125;</span><br></pre></td></tr></table></figure>

<p>즉, 서비스는 컨트롤러로부터 받은 요청에 대한 실질적인 작업을 수행하는 역할을 한다. 데이터베이스와의 상호 작용, 외부 API 호출 등 실질적인 비즈니스 로직을 담당한다.</p>
<p>이때 서비스를 인터페이스로 만들고 세부 구현은 클래스를 통해 만들어두면 나중에 기능을 변경하는 과정을 유연하게 할 수 있을 것이다.</p>
<h3 id="7-2-Service-예제"><a href="#7-2-Service-예제" class="headerlink" title="7.2 Service 예제"></a>7.2 Service 예제</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">nest g service service-name</span><br></pre></td></tr></table></figure>

<p>위 명령어를 이용하여 서비스 파일을 생성할 수 있다. 기본적인 형태는 아래와 같다 :</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Injectable</span> &#125; <span class="keyword">from</span> <span class="string">&quot;@nestjs/common&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Injectable</span>()</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">CatsService</span> &#123;&#125;</span><br></pre></td></tr></table></figure>

<p>모듈에는 아래와 같이 자동으로 연결된다 :</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Module</span>(&#123;</span><br><span class="line">  <span class="attr">controllers</span>: [<span class="title class_">CatsController</span>],</span><br><span class="line">  <span class="attr">providers</span>: [<span class="title class_">CatsService</span>],</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<p>7.1 에서 말했듯이 서비스에는 실제 비즈니스 로직이 담긴다. 따라서 6.2 의 controller 파일에 있는 <code>signUp</code> 메서드의 기능은 여기서 구현된다</p>
<p>이를 또 제대로 구현하기 위해서는 데이터베이스 연결과 <code>Schema</code>, <code>DTO</code> 파일들이 필요하다. 이 내용은 각각 12, 13 에서 다룬다.</p>
<h3 id="7-3-Module-Controller-Service-를-따로-나누어서-작성하는-이유"><a href="#7-3-Module-Controller-Service-를-따로-나누어서-작성하는-이유" class="headerlink" title="7.3 Module, Controller, Service 를 따로 나누어서 작성하는 이유"></a>7.3 Module, Controller, Service 를 따로 나누어서 작성하는 이유</h3><p>모듈을 따로 나누는 이유는 1.1.1에서 언급하였다. 그렇다면 컨트롤러와 서비스까지 따로 작성하는 이유는 무엇일까?</p>
<p>모듈을 분리하는 이유와 똑같다. 코드의 유지 보수, 관리를 용이하게 하기 위해서 위 세가지를 분리하여 따로 관리한다.</p>
<p>그렇다면 Client(browser) 에서 요청을 한다면 controller와 service는 어떻게 계층적으로 작동할까?</p>
<p>이 문제에 대해서는 다음 포스트에서 DTO를 다루면서 설명하겠다. - <a href="https://1dohyeon.github.io/posts/nest2/">2.2.3</a></p>
<blockquote>
<p>실습 코드 URL : <a href="https://github.com/1Dohyeon/Study-NestJS">https://github.com/1Dohyeon/Study-NestJS</a></p>
</blockquote>
]]></content>
      <categories>
        <category>NestJS</category>
      </categories>
      <tags>
        <tag>study</tag>
        <tag>NestJS</tag>
      </tags>
  </entry>
  <entry>
    <title>NestJS-Pipes(파이프)</title>
    <url>/2024/01/11/nest8-pipes/</url>
    <content><![CDATA[<h3 id="10-1-Pipe-정의"><a href="#10-1-Pipe-정의" class="headerlink" title="10.1 Pipe 정의"></a>10.1 Pipe 정의</h3><p>파이프는 클라이언트 요청에서 들어오는 <strong>데이터를 유효성 검사 및 변환</strong>을 수행하여 서버가 원하는 데이터를 얻을 수 있도록 도와주는 클래스이다.</p>
<span id="more"></span>

<h3 id="10-2-Pipe-사용-예제"><a href="#10-2-Pipe-사용-예제" class="headerlink" title="10.2 Pipe 사용 예제"></a>10.2 Pipe 사용 예제</h3><p>컨트롤러에 다음과 같은 <code>@GET</code> 메서드가 있다고 가정한다 :</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Get</span>(<span class="string">&#x27;:id&#x27;</span>)</span><br><span class="line"><span class="keyword">async</span> <span class="title function_">findOne</span>(<span class="params"><span class="meta">@Param</span>(<span class="string">&#x27;id&#x27;</span>, ParseIntPipe) id: <span class="built_in">number</span></span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="variable language_">this</span>.<span class="property">catsService</span>.<span class="title function_">findOne</span>(id);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>특정 cat을 찾는 메서드인데 이때 파라미터로 <code>id</code> 값을 받는다. 원래였으면 <code>string</code> 타입으로 id 값을 받았을 것이다. 하지만 이것을 <code>number</code> 타입으로 받고 싶다면 <code>ParseIntPipe</code> 를 통해서 <code>string -&gt; number</code> 로 타입 변환을 해준다.</p>
<p>그리고 만약 <code>number</code> 로 변환할 수 없는 <code>string</code>(ex: abc22) 가 파라미터로 들어왔다면 validation 에러를 자동으로 띄워준다.</p>
]]></content>
      <categories>
        <category>NestJS</category>
      </categories>
      <tags>
        <tag>study</tag>
        <tag>NestJS</tag>
      </tags>
  </entry>
  <entry>
    <title>NestJS-Interceptors(인터셉터)</title>
    <url>/2024/01/11/nest9-interceptors/</url>
    <content><![CDATA[<h3 id="11-1-Interceptors-정의"><a href="#11-1-Interceptors-정의" class="headerlink" title="11.1 Interceptors 정의"></a>11.1 Interceptors 정의</h3><p>인터셉터는 <code>@Injectable()</code> 데코레이터가 달린 클래스이다. 인터셉터는 <code>NestInterceptor</code> 인터페이스를 구현해야한다.</p>
<span id="more"></span>

<p>컨트롤러를 각 모듈별로 각각 나누어서 개발했지만, 공통되는 기능이 필요할 수가 있다. 예를 들자면 logging 기능이 있다. 이는 middleware 를 통해서 만들었다. Interceptor도 middleware처럼 각 컨트롤러에서 공통된 기능을 구현해야할 때 사용된다. 따라서 logging 기능을 Interceptor 에서도 사용할 수 있다.</p>
<p>하지만 Interceptor는 middleware와 달리 컨트롤러 시작 후에도 실행될 수 있다. 여러 과정을 생략하긴 했지만 대체로 <code>request -&gt; middleware -&gt; Interceptor -&gt; controller -&gt; Interceptor -&gt; response</code> 순서와 같이 controller 시작 전, 후 두번 거치게 된다.</p>
<p>controller 시작 전에 실행되는 것을 <strong>Interceptors (pre-controller)</strong> 라고 부르고, controller 후에 실행되는 것을 <strong>Interceptors (post-request)</strong> 라고 부른다.</p>
<p><a href="https://docs.nestjs.com/interceptors">NestJS 공식문서</a>에서 Interceptor로 logging 기능을 구현하는 것을 찾아볼 수 있다.</p>
<h3 id="11-2-Interceptors-예제"><a href="#11-2-Interceptors-예제" class="headerlink" title="11.2 Interceptors 예제"></a>11.2 Interceptors 예제</h3><p>예외처리가 났을 때 [[09. HttpExceptionFilter]] 에서 공부했듯 필터링을 하여 공통적으로 에러처리를 하였다.</p>
<p>이번에는 성공을 했을 때 <code>succes: true</code> 를 알리는 기능을 공통적으로 개발하기 위해서 Interceptors 를 사용하였다.</p>
<p>src&#x2F;common&#x2F;interceptors 폴더 안에 success.interceptor.ts 파일을 생성하였다 :</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123;</span><br><span class="line">  <span class="title class_">CallHandler</span>,</span><br><span class="line">  <span class="title class_">ExecutionContext</span>,</span><br><span class="line">  <span class="title class_">Injectable</span>,</span><br><span class="line">  <span class="title class_">NestInterceptor</span>,</span><br><span class="line">&#125; <span class="keyword">from</span> <span class="string">&quot;@nestjs/common&quot;</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Observable</span> &#125; <span class="keyword">from</span> <span class="string">&quot;rxjs&quot;</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; map &#125; <span class="keyword">from</span> <span class="string">&quot;rxjs/operators&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Injectable</span>()</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">SuccessInterceptor</span> <span class="keyword">implements</span> <span class="title class_">NestInterceptor</span> &#123;</span><br><span class="line">  <span class="title function_">intercept</span>(<span class="attr">context</span>: <span class="title class_">ExecutionContext</span>, <span class="attr">next</span>: <span class="title class_">CallHandler</span>): <span class="title class_">Observable</span>&lt;<span class="built_in">any</span>&gt; &#123;</span><br><span class="line">    <span class="comment">// controller 이전</span></span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;before...&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// controller 이후</span></span><br><span class="line">    <span class="keyword">return</span> next.<span class="title function_">handle</span>().<span class="title function_">pipe</span>(</span><br><span class="line">      <span class="title function_">map</span>(<span class="function">(<span class="params">data</span>) =&gt;</span> (&#123;</span><br><span class="line">        <span class="attr">success</span>: <span class="literal">true</span>,</span><br><span class="line">        data,</span><br><span class="line">      &#125;))</span><br><span class="line">    );</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>코드에 대해서 설명하기 전에 데코레이터를 이용하여 위 Interceptor를 controller에 연결한다 :</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Controller</span>(<span class="string">&quot;cats&quot;</span>)</span><br><span class="line"><span class="meta">@UseInterceptors</span>(<span class="title class_">SuccessInterceptor</span>)</span><br><span class="line"><span class="meta">@UseFilters</span>(<span class="title class_">HttpExceptionFilter</span>)</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">CatsController</span> &#123;</span><br><span class="line">  <span class="meta">@Get</span>()</span><br><span class="line">  <span class="title function_">getCurrentCat</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&quot;current cat&quot;</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>map((data) =&gt; (&#123; ... &#125;))</code> : <code>rxjs</code> 의 라이브러리의 <code>map</code> 메서드를 이용하여 data를 객체형태로 만들어준다. 이렇게 되면 컨트롤러에서 return 한 data가 매핑되어 컨트롤러 이후에 매핑된 객체를 반환한다.</p>
<p>위 컨트롤러 예제에서 <code>getCurrentCat()</code> 가 실행된다면 <code>&#39;current cat&#39;</code> 이 전달되어 다음과 같은 객체를 반환받게 된다 :</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;success&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;data&quot;</span><span class="punctuation">:</span> <span class="string">&quot;current cat&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>이렇게 되면 성공한 응답인지 확인하기 쉬워진다. 모든 컨트롤러가 성공된다면 각 메서드의 리턴값을 가공하여 공통적인 데이터 구조로 보여줄 수 있게된다.</p>
]]></content>
      <categories>
        <category>NestJS</category>
      </categories>
      <tags>
        <tag>study</tag>
        <tag>NestJS</tag>
      </tags>
  </entry>
</search>
